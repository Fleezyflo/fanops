# CODEOWNERS — centralizes MERGE AUTHORITY (the orchestrator/owner lands; lanes never self-merge).
#
# This file only becomes BINDING once branch protection on `main` has
# "Require review from Code Owners" enabled (Settings > Branches). Until then it is a review-routing
# hint that pairs with the prose rule in .agents/_shared-guardrails.md ("you do NOT merge").
#
# Confirm the handle below is correct for this repo (owner/team). Adjust if you add a dedicated
# orchestrator/bot reviewer.

# Everything: owner review required so no lane agent can self-merge its own PR.
*                       @Fleezyflo

# Orchestration governance is especially sensitive — same owner, called out explicitly.
/.agents/               @Fleezyflo
/.cursor/agents/        @Fleezyflo
/.github/workflows/     @Fleezyflo
/scripts/lane_guard.py          @Fleezyflo
/scripts/pr_collision_guard.py  @Fleezyflo
