Codemap update — 2026-06-16
Scan: 58 src modules (src/fanops/ + studio/), ~70 test files.

Modules added since last codemap (2026-06-14):
  + hookedit.py    feed-aware vision hook editor (chunked gates, cross-feed dedup)
  + keyframes.py   source-frame extraction (the vision editor's eyes)
  + hashtags.py    vet_hashtags — ≤4 reach-vetted hashtag selection
  + (hookcheck.py / vocals.py already present; now grouped under Hook+hashtag quality)

Architecture changes:
  * Pipeline: NEW hookedit stage between moments_decided and clip render
    (Moment.decided -> [hookedit] -> hook_edited -> clip burns the retention hook).
  * On-screen text reframed: RETENTION hook (not transcript caption); burn_subs default OFF.
  * Caption stage: hashtags now hard-capped at 4 and restricted to a reach-vetted set.
  * prompts.py: one shared _hook_spec (retention-only, no artist hype) across
    moment/hookedit/caption.
  * New sourced knowledge artifact: .claude/skills/fanops-hook-hashtag/SKILL.md.

Added section: "Output levers" — the control surface (every flag/prompt/knowledge
file/threshold that changes the produced clips/posts).

Diff magnitude: additive, < 30% -> updated architecture.md in place.
data.md / dependencies.md: no structural change (ledger model + external deps
unchanged; claude -p now also passes image frames to the vision hook editor).

Staleness: none > 90 days.
