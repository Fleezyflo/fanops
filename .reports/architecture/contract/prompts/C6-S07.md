# C6-S07 — Reconcile and publish are gated on the same predicate

**Class:** REMEDIATION · **Root cause:** `RC-3b` · **Severity: HIGH** · **Prereq: S02** · **Co-required with: S04**
**PR title must carry:** `(Unit: reconcile-publish-gate-parity)`

**Supersedes** `prompts/C4-SLICE-07.md`. Self-contained.

---

## 0. Objective

**The producer of a state and its SOLE consumer must be gated on the SAME predicate.** *(`INT-4`.)*

---

## 1. 🔴 READ THIS BEFORE YOU EDIT ANYTHING

> ## ❌ **DO NOT ALSO REMOVE THE `is_live_backend` GATE FROM `_learn_pass`** ([cli.py:965](src/fanops/cli.py:965)).

That gate is the **only** thing standing between a not-live-backend deployment and **`adjust.retire`**
([adjust.py:95](src/fanops/adjust.py:95)) — which writes **`MomentState.retired`**, a state that
**`reconcile_moments` REFUSES to undo** ([ledger.py:636-642](src/fanops/ledger.py:636)).
**IT IS STRUCTURALLY IRREVERSIBLE.**

> 🔴 **"Unifying the gating" — THE MOST NATURAL-LOOKING CLEANUP IN THIS DIFF — would run PERMANENT MOMENT
> RETIREMENT on a deployment where learning was previously frozen.**
>
> **And `PD-3` — the question of whether that retirement policy is even CORRECT — IS UNANSWERED.**

**This is the single most dangerous scope expansion in the entire program.** It is `IR-7` in the risk register,
and **one test prevents it** (§7).

*(Live tree, 2026-07-14: **0 moments retired.** The actuator has never fired. That **limits** the damage of a
mistake. **It does not excuse one.**)*

---

## 2. What is broken

```python
# pipeline.py:311-338
def _reconcile_safe(cfg, log):
    if cfg.is_live_backend:          # :318   ← GATED
        reconcile_due(cfg)

def _publish_safe(cfg, log):
    publish_due(cfg, now=None)       # :334   ← NOT GATED
```

**`reconcile` is the SOLE READER of `submitting`** — `publish_due` iterates `queued` only
([run.py:442](src/fanops/post/run.py:442)).

> 🔴 **So the sole reader of a state can be switched off while the writer keeps producing it.**

When every channel's backend is malformed (or credential-less, or provider-less), `live_ready_channels()` returns
`[]` → `is_live_backend` is `False` → **reconcile never runs while publish still CLAIMS posts into
`submitting`.** The post is stranded **permanently AND unlabeled** — no escalation, no `error_reason`, no terminal
state, no digest bucket.

**A producer and the sole consumer of its output state are gated on different predicates. Either both are gated,
or neither is.**

---

## 3. The fix — Option A (recommended): **remove the gate from `_reconcile_safe`**

**`reconcile_due` is already per-post safe.** Its **own docstring** says so
([pipeline.py:315-316](src/fanops/pipeline.py:315)):

> *"…resolves each post's provider via `effective_provider` and **skips dryrun/provider-less posts**."*

**The gate is redundant defence that became an active hazard.** Removing it restores the invariant with the least
code.

### 🔴 The pre-flight that DECIDES the option

> **Confirm `reconcile_due` makes ZERO network calls on a dryrun-only deployment.**
> 🔴 **THIS IS AN ACCEPTANCE CRITERION, NOT A NICETY.** If it does **not** hold, take **Option B**.

**Option B (fallback):** gate `_publish_safe` on `is_live_backend` too. **Strictly safer** — but it also stops
**dryrun previews** from being written on a not-live system ([run.py:458-467](src/fanops/post/run.py:458)) — **a
real feature regression.** Prefer A unless the pre-flight fails.

---

## 4. Mutation boundary

| File | Permitted region |
|---|---|
| `src/fanops/pipeline.py` | **`_reconcile_safe` / `_publish_safe` (:311-338) — EXACTLY ONE of the two gates.** |

**Nothing else.**

---

## 5. Preservation boundary

- 🔴 **`_learn_pass`'s `is_live_backend` gate (cli.py:965). DO NOT TOUCH IT.** *(§1.)*
- `AuthError` **re-raise symmetry** in **both** `_reconcile_safe` and `_publish_safe`.
- The **swallow-with-log** for any other hiccup — *a status-API hiccup must not wedge the pass.*
- **Dryrun preview writing.**
- The five-phase `advance()` shape.
- **`_quarantine`** — it is the failure **mechanism**, not a swallow.

### 🔴 Mechanical budgets — CI enforces these today

| | |
|---|---|
| **`pipeline.py` `print()`** | 🔴 **ZERO PERMITTED.** `pipeline.py` is in `_INTERNAL_MODULES`; the assertion is `offenders == []`. |
| **`pipeline.py` silent broad `except`** | **Baseline 13. MUST NOT EXCEED.** Every new `except` must **log or fail-open**. |

### Global boundaries

**`GB-1`** (no lazy-import hoist) · **`GB-2`** (no deletion on "zero callers") · **`GB-3`** (no `extra="forbid"`)
· **`GB-4`** (no new unguarded door to `published`/`analyzed`).

---

## 6. Acceptance criteria

1. With `is_live == True` and `live_ready_channels() == []`: **either both** `_reconcile_safe` and `_publish_safe`
   run, **or neither** does.
2. 🔴 **`reconcile_due` makes ZERO network calls on a dryrun-only deployment** *(the pre-flight, pinned by a
   test)*.
3. **Dryrun preview writing is not regressed.**
4. 🔴 **`_learn_pass`'s gate is UNTOUCHED — and a test proves it.**

## 7. Tests

| Test | Class | Must fail before? |
|---|---|---|
| `test_reconcile_and_publish_gate_parity` | INVARIANT | ✅ |
| `test_reconcile_makes_no_network_calls_when_not_live` | PRESERVATION *(the pre-flight)* | ⚪ |
| `test_dryrun_preview_writing_not_regressed` | PRESERVATION | ⚪ |
| 🔴 **`test_learn_pass_is_still_gated_on_is_live_backend`** | 🔴 **CONTRACT** | ⚪ |

> 🔴 **That last test is the cheapest possible guard against the worst outcome this program can produce.**
> **WRITE IT.**

## 8. Required evidence in the PR

- **Every** reader of `cfg.is_live_backend` ([config.py:459](src/fanops/config.py:459)). *Expect:
  `_reconcile_safe`, `_learn_pass`, and the health/doctor surfaces.*
- 🔴 **State explicitly which ONE this slice changes, and which it MUST NOT.**
- The **pre-flight result** (zero network calls on dryrun-only) and therefore **which option you took**.

## 9. 🔴 Prohibited

- ❌ 🔴 **Removing the `is_live_backend` gate from `_learn_pass`.** *(§1.)*
- ❌ Touching `accounts.py` — that is **S02**.
- ❌ Touching `reconcile.py` (**S04**) or `post/run.py` (**S03**).
- ❌ Adding **another doctor warning.** The half-live banner **already exists**
  ([doctor.py:322](src/fanops/doctor.py:322)) and is **suppressed whenever one valid channel remains**
  (`C3-OBS-5`). **A second warning on the same broken predicate adds noise, not safety.**

## 10. Process

**CI:** `unit`. **NEVER run the suite locally.** Replay both AST ratchets.
**Self-merge: NO. Verifier: REQUIRED — and 🔴 the verifier MUST specifically confirm `_learn_pass`'s gate is
untouched.** *(Written into the approval requirement, not left to be noticed.)*
**Rollback:** CODE-REVERSIBLE. 🔴 **Post-rollback validation: CONFIRM `_learn_pass`'s gate (cli.py:965) SURVIVED
THE REVERT.** *A dirty revert that takes it too would ARM IRREVERSIBLE RETIREMENT. This is the highest-consequence
post-rollback check in the program.*
**Co-requirement:** `S04`'s terminal ladder is **unreachable** until this slice lands. **`INT-2` holds only when
both are merged.** *(Neither must precede the other.)*
**State remaining unknowns honestly.**
