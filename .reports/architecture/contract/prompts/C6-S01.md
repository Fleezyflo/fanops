# C6-S01 — `restore_snapshot` writes the snapshot IN PLACE, under the ledger's own lock

**Class:** REMEDIATION · **Root causes:** `RC-4` + `RC-5` · **Severity: CRITICAL** · **Prereqs: none** · **SHIPS FIRST**
**PR title must carry:** `(Unit: restore-in-place-under-ledger-lock)`

**Supersedes** `prompts/C4-SLICE-01.md`. Self-contained — you need read nothing else.

---

## 0. Objective

**Replacing the ledger's contents must be mutually exclusive with every active writer, and must never orphan an
inode a writer holds open.** *(`INT-5`.)*

---

## 1. Before you edit anything

**Re-resolve every `file:line` below against current source.** *(All 16 were verified correct at `fcffa73`. Yours
may have rotted.)* If one does not resolve, **STOP and re-derive it** — do not guess.

**Then state, in your own words, the root cause.** If your statement is *"restore_snapshot takes the wrong
lock,"* 🔴 **you have not understood the defect and you will ship an insufficient fix.** Read §3.

---

## 2. What is broken

`Ledger.transaction` ([ledger.py:484](src/fanops/ledger.py:484)) serializes every ledger writer on
`store.lock()` — SQLite **`BEGIN IMMEDIATE`**.

`Ledger.restore_snapshot` ([ledger.py:551](src/fanops/ledger.py:551)) serializes on
`_file_lock(cfg.lock_path)` — an **`fcntl.flock` on a different file**.

**The two primitives are mutually invisible.** `restore` then `os.replace()`s the database file
([ledger_sqlite.py:151](src/fanops/ledger_sqlite.py:151)), **orphaning the inode a live writer holds open.**

### Two consequences, both proven by execution

**(a) The writer's `commit()` succeeds and its data is silently discarded.** Measured: the restorer was **never
blocked** — it completed in **0.001 s**.

**(b) 🔴 It deletes real media files.** `Ledger.transaction` runs:

```python
led._save_unlocked()            # ← commits into the ORPHANED inode. SUCCEEDS. No exception.
led._drain_deferred_unlinks()   # ← os.remove() on real .mp4 paths (ledger.py:518-525). PROCEEDS.
```

The deferred-unlink design is documented as *"correct: a rolled-back txn never deletes a file it did not
drop."* **True against a rollback.** In a restore race the txn is **not** rolled back — it **commits into an
orphan** — **so the unlinks run.**

**Measured: ledger rows restored from the snapshot; the `.mp4` gone from disk.**

### 🔴 And this is not hypothetical — it is the procedure this operator runs

**Live tree, `00_control/`, measured 2026-07-14:** **eleven** snapshot/backup artifacts spanning
**2026-06-20 → 2026-07-10** — including a genuine `ledger.snapshot.20260705T103618Z.1d5a6ffe.json` and a
`ledger.json.prewipe-20260629-000912`. **Roughly monthly, with a launchd daemon installed.**
[`ledger_wipe.py:246`](src/fanops/ledger_wipe.py:246) advertises `restore_snapshot` as **THE** rollback path.

---

## 3. 🔴 The fix you will be tempted to ship, and why it is **wrong**

> **Tempting:** `ledger.py:551` — change `with _file_lock(cfg.lock_path):` to `with store.lock():`.
> One line. And `store.restore()` **already supports** it
> ([ledger_sqlite.py:131](src/fanops/ledger_sqlite.py:131): `relock = self._conn is not None`).

**IT DOES NOT WORK.**

> 🔴 **SQLite's write lock is held on the *inode*, via an open file descriptor. A concurrent writer that opened
> the db *before* the `os.replace` still holds that inode. NO lock acquired before an `os.replace` can protect
> it.**

Taking the right lock **narrows the window; it does not close it.** This is exactly the shallow fix the audit
brief names: *"adding another lock that does not exclude SQLite writers."*

**If you ship Option A, you will have made the race rarer and left it in the codebase.**

---

## 4. The root fix — **do not replace the file**

Write the snapshot's **contents** into the live database, **in place**, inside the **same `BEGIN IMMEDIATE`
transaction every other writer uses.** Same inode ⇒ no orphan ⇒ no lost commit ⇒ no stray unlink.

**Nothing is invented.** `SqliteLedgerStore.write_raw` ([ledger_sqlite.py:59](src/fanops/ledger_sqlite.py:59))
**already** performs a full-document `DELETE` + `INSERT`, and when `self._conn` is already set (`own = False`)
it **reuses the caller's locked connection without committing** — exactly the semantics required.

```python
# ledger_sqlite.py — extract. ~8 lines. ZERO behaviour change.
def read_raw_from(path: Path) -> dict | None:  ...   # the body of read_raw, parametrized on path
def read_raw(self):  return read_raw_from(self.db_path)

# ledger.py:546 — restore_snapshot
snap_doc = read_raw_from(src)
if snap_doc is None:
    raise ControlFileError(...)
with store.lock():                  # ← BEGIN IMMEDIATE: THE SAME DOMAIN EVERY WRITER USES
    store.write_raw(snap_doc)       # ← in-place DELETE+INSERT. SAME INODE.
_snapshot_restore_control_files(cfg, src)
```

### The corrupt-db fallback is **required** — do not omit it

`store.restore()`'s contract says it *"must work even when live db is corrupt"* — and that is **not decoration.**
**Measured:** the in-place path raises `DatabaseError: file is not a database` on a corrupt live db, while
today's `os.replace` path **succeeds**.

**So the fix is two-path:**

| Live DB | Route |
|---|---|
| **openable** *(the case that actually bites — the documented rollback with the daemon running)* | **in-place under `store.lock()`** — fully serialized |
| **corrupt / unopenable** *(`store.lock()` cannot open it)* | **fall back to the existing `os.replace` route** |

**Name the residual in your decision record:** a writer that opened the db *before* it became corrupt could still
hold an fd. That window is genuinely unavoidable without a system-wide lock, and it is **far narrower than
today's.**

---

## 5. 🔴 The test that must be rewritten — and why the verifier must be told

[`tests/test_ledger_sqlite_store.py:161-186`](tests/test_ledger_sqlite_store.py:161)
**`test_restore_snapshot_serializes_with_transaction`**

```python
# :183
time.sleep(0.1)      # restorer blocked on flock held by writer      ← FALSE. It is NEVER blocked.
# :186
assert store.read_raw() == doc      # "restore wins; no orphan srcX"
```

**This test asserts the data loss and calls it correct.** It passes **because** the writer's committed row was
destroyed. **It is a regression lock on the bug.**

> 🔴 **YOUR FIX WILL TURN THIS TEST RED. THAT IS THE PROOF IT WORKED.**
> **A reviewer who does not know this will read *your fix* as the regression.** Say so, loudly, **at the top of
> the PR description**, and **confirm the independent verifier has been briefed on `RC-5` before they review.**

**Rewrite it** to assert what the property actually requires: the restorer **blocks** on `BEGIN IMMEDIATE` until
the writer commits, and the writer's commit is **visible** (not orphaned) before the restore supersedes it.

---

## 6. Mutation boundary — **what you MAY change**

| File | Permitted region |
|---|---|
| `src/fanops/ledger.py` | **`Ledger.restore_snapshot` ONLY** (~:546-553) |
| `src/fanops/ledger_sqlite.py` | extract `read_raw_from(path)`; retain `restore()` as the corrupt-db fallback |
| `tests/test_ledger_sqlite_store.py` | the restore test (:161-186) — **REWRITE** + 3 new tests |

**Nothing else. A diff touching any other file is out of contract.**

---

## 7. Preservation boundary — **what MUST NOT change**

- 🔴 **`Ledger.transaction`, `_save_unlocked`, `_drain_deferred_unlinks` — ALREADY CORRECT. DO NOT TOUCH.**
- 🔴 **`Ledger.snapshot` ([ledger.py:540](src/fanops/ledger.py:540)) — it ALREADY takes `store.lock()`. It is the
  MODEL your fix copies, not a target.**
- `_snapshot_restore_control_files` (the `accounts.json` / `personas.json` sidecar restore).
- The `ControlFileError` typing on a missing/unreadable snapshot.
- **The snapshot FILE FORMAT** — a post-fix snapshot **must restore on pre-fix code.** *(This is what makes the
  revert safe.)*
- WAL · `synchronous=FULL` · `chmod 0600` after every write.

### 🔴 Global boundaries (bind every slice)

- **`GB-1`** — do **not** hoist a lazy `fanops` import to module level, and do **not** add a module-level import
  to an equal-or-higher layer level. *(56 strictly-upward lazy imports exist; hoisting one breaks the process at
  start, and it looks like a cleanup.)*
- **`GB-2`** — do **not** delete a symbol on "zero callers." *(`providers.py` has six lazy factories a call graph
  reports as dead. All six are live.)*
- **`GB-3`** — do **not** set `extra="forbid"` on a ledger model.
- **`GB-4`** — do **not** create a new write path to `PostState.published`/`analyzed` without an explicit
  non-empty `public_url` guard.

### 🔴 Mechanical budgets — **CI enforces these today**

| | |
|---|---|
| **`ledger.py` `print()`** | 🔴 **ZERO PERMITTED.** `ledger.py` is in `_INTERNAL_MODULES` (`tests/test_internal_prints_routed.py`); the assertion is `offenders == []`. |
| **`ledger.py` / `ledger_sqlite.py` silent broad `except`** | 🔴 **ZERO PERMITTED.** Neither is in the swallow-ratchet baseline — **any** new one triggers `new_files != []` and **fails CI immediately.** Every new `except` must **log or fail-open**. |

---

## 8. Acceptance criteria

1. A writer holding `BEGIN IMMEDIATE` **blocks** a concurrent restore until it commits.
   **Measurable: with the writer holding for 0.4 s, the restore must take > 0.3 s. Today: 0.001 s.**
2. The writer's commit is **visible** — not orphaned — before the restore supersedes it.
3. 🔴 **A cascade transaction's deferred media unlinks NEVER delete a file whose ledger row the restore brings
   back.** *(The severity driver. Test it explicitly.)*
4. A restore against a **corrupt** live db **still succeeds** via the fallback.
5. The **snapshot file format is unchanged.**
6. 🔴 **A two-PROCESS contention test passes — NOT threads.** SQLite locking is per-inode and **cross-process**;
   every experiment in this audit used threads and is **indicative, not sufficient.** **THIS IS A MERGE GATE.**

## 9. Tests

| Test | Class | Action | Must fail before? |
|---|---|---|---|
| `test_restore_snapshot_serializes_with_transaction` | **REGRESSION-LOCK REWRITE** | 🔴 **REWRITE** | ✅ *it passes today **because of** the loss* |
| `test_restore_does_not_orphan_media_unlinks` | INVARIANT | NEW | ✅ |
| `test_restore_two_process_contention` | INVARIANT | NEW | ✅ |
| `test_restore_falls_back_on_corrupt_db` | PRESERVATION | NEW | ⚪ *pins the fallback* |

## 10. Required evidence in the PR

- Every caller of `Ledger.restore_snapshot` *(expect: **zero in production**; `ledger_wipe.py:246` advertises it
  in a docstring; tests call it)*.
- Every caller of `SqliteLedgerStore.restore` / `.write_raw` / `.read_raw`.
- Every writer that takes `store.lock()` — **confirm none is changed by this slice.**
- **No schema change. No on-disk format change.**
- The **two-process test result.**
- 🔴 **Whether any operator has ALREADY run a restore against a live daemon.** *(11 snapshot artifacts exist. If
  a restore was run with the daemon up, **media may already be missing — and the ledger will not say so.**)*

## 11. 🔴 Prohibited

- ❌ **Option A** (swap the lock, keep `os.replace`). **It leaves the race.**
- ❌ Touching `Ledger.transaction` or any other writer.
- ❌ "Unifying" the L1↔L2 lock domains (`COUP-01`).
- ❌ Fixing the wipe's `MOL-71` preview gap.
- ❌ **Deleting `ledger.lock`** — *even though this fix makes it genuinely dead for the first time.* Separate,
  trivial cleanup; **bundling it obscures the diff.**

## 12. Process

**CI:** `unit`. 🔴 **NEVER run the suite locally.** Replay both AST ratchets before pushing.
**Self-merge: NO.** **Verifier: REQUIRED — and they MUST be briefed on `RC-5`**, or they will reject the fix for
turning a green test red.
**Rollback:** CODE-REVERSIBLE. `git revert`. Format unchanged. *(Reverting re-opens the race. A restore performed
under the fixed code produced a correct outcome; the revert does not corrupt it.)*
**Behavioural note:** a restore now **blocks** (up to the 30 s `LockBusyError` bound) while a writer holds the
ledger. **Today it silently wins. Blocking is CORRECT** — a restore that cannot serialize must **fail loudly, not
corrupt.** If an operator reports *"the restore got slower,"* **that is the fix working.**
**State remaining unknowns honestly.**
