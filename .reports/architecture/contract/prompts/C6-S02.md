# C6-S02 — Canonical backend normalization at the READ boundary

**Class:** REMEDIATION · **Root cause:** `RC-3` · **Severity: HIGH** · **Prereqs: none** · **Ordering: before S07**
**PR title must carry:** `(Unit: backend-normalize-at-read-boundary)`

**Supersedes** `prompts/C4-SLICE-02.md`. Self-contained.

---

## 0. Objective

**A live account with an unsupported provider value must FAIL BEFORE a post is claimed.** *(`INT-3`.)*

---

## 1. 🔴 Cycle 6 DISARMED Cycle 4's mandatory mitigation. Read this first.

Cycle 4 wrote:

> *"**CRITICAL INTERACTION: S02 ↔ S10.** If S02 normalizes a typo'd backend, a previously-dark channel goes live
> → `is_live_backend` flips `True` → `_learn_pass` starts running — INCLUDING the IRREVERSIBLE `retire()` — on a
> deployment where it previously did not. **Fixing a typo could silently begin permanently retiring moment
> lineages.** **Mandatory if `PD-3` is unanswered:** log the `is_live_backend` transition loudly…"*

**Cycle 4 asked for the read-only check and never ran it. Cycle 6 ran it.**

**MEASURED (2026-07-14, live `00_control/accounts.json`):**

| Handle | Platform | Backend |
|---|---|---|
| `markmakmouly` · `perca.late` · `cisumwolfhom` | instagram | `postiz` |
| `backlikeineverleft` · `hrmny-blog` | tiktok | `zernio` |

> 🔴 **MALFORMED BACKEND VALUES: 0.**
> **S02 CANNOT flip `is_live_backend` False→True by normalizing a typo — BECAUSE THERE IS NO TYPO TO NORMALIZE.**
> **The `S02`↔`S10` hazard is NOT REACHABLE on this deployment. The mandatory mitigation is DISARMED.**

### ⚠️ AND IT IS RE-ARMED AS A **MERGE GATE**

**`accounts.json` is a mutable file, and the operator hand-edits it by design.**

> 🔴 **YOUR PR MUST RE-RUN THIS CHECK AND STATE THE RESULT.** If a malformed value has appeared since
> 2026-07-14, **the Cycle-4 mitigation RE-ARMS IN FULL**: a loud `is_live_backend` transition log, **and** an
> explicit operator briefing that fixing a malformed backend **can unfreeze the learning pass**, including the
> **irreversible** `retire()`.
>
> **A collapsed risk that is not re-verified at merge is a risk that was merely not looked at.**

**None of this makes `RC-3` less real.** The **load** boundary has no validation, and hand-editing is the
**documented** operator channel (`AR-09`). **It is reachable by the next hand-edit.** Ship the fix.

---

## 2. What is broken

`accounts.set_backend` ([accounts.py:412](src/fanops/accounts.py:412)) is the **only** normalizer:
`.strip().lower()` + membership in `_VALID_BACKENDS`. **It guards the Studio/CLI *write* path only.**

- `Account.backends` is `dict[str, str]` — **unvalidated at load** ([accounts.py:91](src/fanops/accounts.py:91)).
- `Accounts.validate()` checks the integration/backend **pairing**
  ([accounts.py:241-250](src/fanops/accounts.py:241)) — 🔴 **never the VALUE.**
- `accounts.json` is **explicitly hand-editable** — the *documented* operator channel
  ([accounts.py:112-113](src/fanops/accounts.py:112)).

**So a raw string reaches five consumers with four different unknown-value behaviours:**

| Resolver | On an unknown value |
|---|---|
| `get_poster` ([post/\_\_init\_\_.py:28-29](src/fanops/post/__init__.py:28)) | 🔴 **silently returns `DryRunPoster` ON A LIVE SYSTEM** |
| `get_media_uploader` ([:40-41](src/fanops/post/__init__.py:40)) | 🔴 silently returns the dryrun `file://` uploader |
| `_post_provider` ([run.py:158](src/fanops/post/run.py:158)) | passes the raw string through |
| `compress.publish_backend_for_post` | passes through (`COUP-16`) |
| `Config.poster_backend` ([config.py:241](src/fanops/config.py:241)) | warns → dryrun |

**The `get_poster` mechanism:** its live-guard at `:19` is case-**insensitive** on the literal `"dryrun"`;
`PROVIDERS.get()` ([providers.py:56](src/fanops/post/providers.py:56)) is case-**sensitive**. **They disagree.**
So `"Postiz"` passes the guard, misses the registry, and **falls through to `DryRunPoster` on a live system.**

**Worst variant:** `"postiz "` (trailing space) is **visually identical** to `"postiz"` in any UI, JSON dump, or
diff.

> **`providers.py:53-55` says: *"no live account routes to an unknown backend … so this path is a defensive
> default."*** 🔴 **That comment IS the defect.** An assumption stated as a fact.

---

## 3. 🔴 The fix you will be tempted to ship, and why it is **insufficient**

> **Tempting:** harden `get_poster`'s guard to raise when `cfg.is_live and get_provider(...) is None`.

It closes the **publish** door and **leaves four siblings divergent.** `get_media_uploader` **still** silently
returns the dryrun uploader. **This is the brief's explicitly-forbidden shallow fix:** *"tightening only one
provider resolver while leaving sibling resolvers divergent."*

---

## 4. The root fix — a malformed value must not be able to **exist in memory**

Extract `normalize_backend(s) -> str | None` — **the exact rule `set_backend` already applies**
(`.strip().lower()`; `None` if not in `_VALID_BACKENDS`) — and apply it at the **READ** boundary
(`Accounts.load`).

- **Case/whitespace variants NORMALIZE.** `"Postiz"` → `"postiz"`; the channel publishes correctly. *(Today it
  silently does not.)*
- **A genuinely-unknown name is SKIPPED and FLAGGED** via the **existing `skipped_rows` channel**
  ([accounts.py:141-145](src/fanops/accounts.py:141)), which `validate()` **already** promotes to a visible
  doctor/health problem ([accounts.py:230-231](src/fanops/accounts.py:230)).
- Add the **value** check to `Accounts.validate()`'s existing pairing check.
- `set_backend` **calls** `normalize_backend` (delete its inline copy). **`_VALID_BACKENDS` collapses to ONE
  home** (`COUP-05`: it is defined **twice** — `config.py:72` and `settings.py:18` — one guarding the *write*
  boundary and one the *read*. **If they drift, the two boundaries disagree about what a legal backend is.
  That is the gap `RC-3` exploits.**)
- Harden `get_poster`'s guard **as defence in depth** — with `load` normalizing, it becomes **unreachable**,
  which is the point.

> **This is not new policy. It moves an existing rule to the door that lacks it.**
> **All five divergent resolvers consume the LOADED `Accounts` object — so fixing the load boundary fixes all
> five WITHOUT touching five files.**

---

## 5. Mutation boundary

| File | Permitted region |
|---|---|
| `src/fanops/accounts.py` | NEW `normalize_backend`; `Accounts.load`; `Accounts.validate`; `set_backend` |
| `src/fanops/post/__init__.py` | `get_poster` (:19) and `get_media_uploader` (:38-41) guards — **defence in depth** |
| `src/fanops/settings.py` | 🔴 **`_VALID_BACKENDS` CONSOLIDATION ONLY.** Nothing else. |

---

## 6. Preservation boundary

- The legacy **`FANOPS_POSTER` bridge** ([accounts.py:196-201](src/fanops/accounts.py:196)).
- 🔴 **The `MOL-79` per-row leniency posture — ONE BAD ROW MUST NEVER CRASH THE REGISTRY.** *(Pinned by a test.)*
- The other **9** `accounts.py` mutators.
- `go_live`'s past-due-backlog gate.
- 🔴 **`Settings`' strict/lenient split from `Config` is INTENTIONAL, not drift (`INV-05`). `Settings` is a
  DOCTOR-ONLY strict validator; `Settings.runtime_load` has ZERO callers. DO NOT "unify" them.**

### 🔴 Global boundaries — **two of these are LIVE for this slice**

- 🔴 **`GB-1`** — **`accounts` is LEVEL 2. `config` — LEVEL 0, FAN-IN 82, the most-depended-on module in the
  system — reaches UP to it via a LAZY import.** Do **not** add a module-level import that inverts this. *(56
  strictly-upward lazy imports exist; hoisting one **looks like a cleanup** and **breaks the process at start.**)*
- 🔴 **`GB-2`** — do **not** delete anything in the `PROVIDERS` resolution chain on "zero callers."
  **`COUP-10`: six lazy in-function import LAMBDAS dispatched from a dict. A name-based call graph flags ALL SIX
  as uncalled. ALL SIX ARE LIVE.**
- **`GB-3`** — see §10.
- **`GB-4`**, **`GB-5`**.

### Mechanical budgets

| | |
|---|---|
| `accounts.py` silent broad `except` | **baseline 3 — MUST NOT EXCEED** |
| `settings.py`, `post/__init__.py` | 🔴 **NOT IN THE BASELINE → ZERO ALLOWED. Any new silent broad `except` FAILS CI IMMEDIATELY.** |

---

## 7. Acceptance criteria

1. The executed Cycle-2 matrix — `"postiz"`, `"Postiz"`, `"POSTIZ"`, `"postiz "`, `" postiz"`, `"blotato"`,
   `"dryrun"`, `"DryRun"` — across `{Accounts.load, validate, effective_provider, get_poster,
   get_media_uploader}`: 🔴 **NO CELL may construct a `DryRunPoster` while `cfg.is_live`.**
2. A case/whitespace variant **normalizes** and the channel publishes correctly.
3. `"blotato"` **skips the channel** and **appears in `Accounts.validate()`'s problems.**
4. `normalize_backend` is the **single home** for the rule; `set_backend` calls it; `_VALID_BACKENDS` has **one**
   definition.
5. **Existing valid `accounts.json` normalizes to itself** — byte-identical behaviour. *(Live tree: this is the
   **only** case.)*
6. 🔴 **`MOL-79` per-row leniency survives** — pinned by a test.
7. 🔴 **The live `accounts.json` probe is re-run and its result stated in the PR.**

## 8. Tests

| Test | Class | Must fail before? |
|---|---|---|
| `test_backend_normalization_matrix` | INVARIANT | ✅ |
| `test_unknown_backend_is_surfaced_not_silent` | INVARIANT | ✅ |
| `test_valid_accounts_json_is_unchanged` | PRESERVATION | ⚪ |
| `test_per_row_leniency_survives` | PRESERVATION *(pins the rejection of Option C)* | ⚪ |

## 9. Required evidence in the PR

- Every reader of `Account.backends`, `resolve_backend`, `effective_provider`, `live_ready_channels`,
  `_VALID_BACKENDS` (**both** definitions). Confirm each is fixed by the load boundary **or is explicitly out of
  scope**.
- 🔴 **The live `accounts.json` check, re-run.**
- **Operational:** *a deployment currently relying on a malformed value to stay in dryrun would start
  publishing.* **Say this in the PR.** *(Live tree: no such deployment. Re-verify.)*

## 10. 🔴 Prohibited

- ❌ 🔴 **Making `Account.backends` a typed enum.** *(`GB-3`.)* **pydantic would then REFUSE TO LOAD a legacy
  registry** — converting a soft skip into a **hard `ControlFileError`**. **That is the exact failure mode
  `SHIM-005` (forward-compat `extra="ignore"`) exists to prevent**, and it breaks the `MOL-79` posture.
- ❌ Hardening **only** `get_poster` and leaving the four siblings divergent. *(§3.)*
- ❌ Adding CSRF to `/golive/account/backend` — `F-C` is a **recorded, accepted** decision.
- ❌ Touching the legacy `FANOPS_POSTER` bridge.
- ❌ Touching `pipeline.py` — that is **S07**.

## 11. Process

**CI:** `unit`. **NEVER run the suite locally.** Replay both AST ratchets.
**Self-merge: NO. Verifier: REQUIRED** — this changes **which channels publish live**; the blast radius is a real
**external** side effect.
**Product gate:** `PD-4` *(recommendation: normalize case/whitespace; skip-and-flag an unknown name)*.
**Rollback:** **WORLD-IRREVERSIBLE (conditional)** — *if it normalizes a typo and a dark channel publishes, no
revert unpublishes.* 🔴 **Live tree: the condition does NOT fire (0 malformed values). Effective class today:
CODE-REVERSIBLE. RE-VERIFY AT MERGE.** *`accounts.json` on disk is **not rewritten** — normalization happens at
load, in memory — so the registry is clean w.r.t. the revert.*
**State remaining unknowns honestly.**
