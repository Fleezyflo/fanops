# C6-S08 — Daemon health: `alive` must not mean `succeeding`

**Class:** REMEDIATION · **Root:** `RC-6` · **Severity: MEDIUM** · **Prereqs: none** · **Buildable in parallel**
**PR title must carry:** `(Unit: daemon-alive-vs-succeeding)`

**Supersedes** `prompts/C4-SLICE-08.md`. Self-contained.

---

## 0. Objective

**A verdict named `alive` must not imply successful work unless a successful-pass timestamp is independently
reported.** *(`INT-6`.)*

---

## 1. What is broken

`daemon.status` ([daemon.py:462-476](src/fanops/daemon.py:462)):

```python
elif not stale:
    verdict = "alive"                     # :463  fresh loop heartbeat
else:
    alive_mid, progress_line, snap = daemon_progress(cfg)
    if alive_mid:                         # :467  "the newest run.log line of ANY kind is fresh"
        verdict = "alive"                 # :468  ← and no heartbeat has been written in HOURS
```

**A pass that HALTS still writes `run.log` lines** — every stage logs before the halt. So `alive_mid` is `True`
and the verdict is **`alive`** — while:

- **no heartbeat** has been written (`_heartbeat` fires only `if s is not None`,
  [cli.py:1306](src/fanops/cli.py:1306)),
- **no ledger state** has changed,
- **nothing has published.**

**The classic trigger:** a rotated Postiz key → `AuthError` → the pass halts **every tick, forever**
([cli.py:947-949](src/fanops/cli.py:947), [:1311-1312](src/fanops/cli.py:1311)).

> 🔴 **The operator asks the system's own liveness question and is told `alive` while nothing is publishing.**

### The root

**Six distinct facts are collapsed into one word:**

`process alive` · `loop ticking` · **`pass started`** · **`pass SUCCEEDED`** · **`state ADVANCED`** ·
**`publishing healthy`**

**`status` reports the first two and names the result `alive`, which the operator reads as the fourth.**

---

## 2. The fix — the data **already exists**; the verdict discards it

🔴 **`heartbeat_age_s` is ALREADY in the returned dict**, and the heartbeat
([cli.py:1306](src/fanops/cli.py:1306)) fires **only when a pass succeeds** — **it already *IS* the success
signal.** It is simply **not read as one**: *"it no longer governs the verdict on its own."*

**Mint the missing SECOND word.**

- `health_model.py`: add **`last_success_age_s`** alongside `heartbeat_age_s`.
- `daemon.py status`: **keep `alive` keyed on `daemon_progress`** — see §3 — and **ADD** a second, orthogonal
  verdict, e.g. `alive (no successful pass in {N}m)` when the process is live but `last_success_age_s` exceeds a
  multiple of the interval.

---

## 3. 🔴 The project memory you must not violate

> **Project memory `liveness-verdict-single-owner`:** `daemon_progress` is **THE** mid-pass liveness owner.
> *"the newest run.log line of any kind = alive; fix there, not per-surface; **don't touch `_heartbeat_age_s`**."*

**The `alive_mid` override is CORRECT for its stated purpose:** *"that heartbeat only lands after a whole pass
finishes, so it must NEVER flip a fast-logging pass to stale."* **That property must survive this slice.**

> 🔴 **THIS SLICE ADDS A SIGNAL. IT MOVES NONE.**
> **Do not change who owns liveness. Do not change `_heartbeat_age_s`'s role in the alive/stale decision.**

---

## 4. Mutation boundary

| File | Permitted region |
|---|---|
| `src/fanops/health_model.py` | add `last_success_age_s` |
| `src/fanops/daemon.py` | `status` (:437-479) — **ADD** a second verdict |
| `src/fanops/cli.py` | 🔴 **the daemon-loop / `_heartbeat` region ONLY. `S08` owns this region EXCLUSIVELY among the slices.** |

---

## 5. Preservation boundary

- 🔴 **`daemon_progress` remains the mid-pass liveness owner.**
- 🔴 **`_heartbeat_age_s`'s role in the alive/stale decision — UNCHANGED.**
- 🔴 **The `alive_mid` override's protective property** — a fast-logging pass is **never** flipped to `stale`.
  *(Pinned by a test.)*
- **`heartbeat_age_s` stays in the returned dict.**
- The `not loaded` → **ALARM**, `stage stuck`, and `not running` verdicts.
- The launchd plist's **baked full `PATH`** — *a stale one once killed every gate for three days.*

### Global boundaries
**`GB-1`** · **`GB-2`** · **`GB-3`** · **`GB-4`**.

### 🔴 Mechanical budgets — **`S08` shares `cli.py` with `S09` and `S10`**

| | |
|---|---|
| 🔴 **`cli.py` `print()` count** | **`_CLI_PRINT_COUNT = 147`, asserted with EXACT EQUALITY.** **THREE SLICES TOUCH `cli.py`.** 🔴 **DO NOT CHANGE THE COUNT.** Route new operator output through **`get_logger`**, or reuse an existing print. *(If two slices land with a bumped constant, the second's rebase yields a wrong count and CI goes red for a reason unrelated to its change.)* |
| `cli.py` silent broad `except` | **baseline 3 — SHARED. Do not raise it.** |
| `daemon.py` / `health_model.py` | **baseline 5 each — MUST NOT EXCEED.** |

---

## 6. Acceptance criteria

1. `status(cfg)` returns **`last_success_age_s`**, distinct from `heartbeat_age_s`.
2. A daemon whose **every pass raises** does **NOT** return a bare verdict `alive`.
3. 🔴 **`daemon_progress` remains the mid-pass liveness owner.**
4. 🔴 **A healthy fast-logging pass is still NOT flipped to `stale`.**
5. 🔴 **`cli.py`'s `print()` count is unchanged (147).**

## 7. Tests

| Test | Class | Must fail before? |
|---|---|---|
| `test_daemon_status_distinguishes_alive_from_succeeding` | INVARIANT | ✅ |
| `test_healthy_fast_pass_is_not_flipped_stale` | PRESERVATION *(the `alive_mid` property)* | ⚪ |
| `test_daemon_progress_remains_the_liveness_owner` | CONTRACT | ⚪ |

## 8. Required evidence in the PR

Every reader of `daemon.status()`'s returned dict — the CLI `daemon status`, the Studio Home banner, `/healthz`,
`/home/daemon-health`.

> 🔴 **CONFIRM no consumer parses the verdict STRING for equality with `"alive"`.** If one does, **adding a new
> verdict string BREAKS IT — and you must say so.**

## 9. 🔴 Prohibited

- ❌ 🔴 **Touching `_heartbeat_age_s`'s role in the alive/stale decision** *(project memory)*.
- ❌ 🔴 **Merely RE-WORDING the verdict strings without adding the new SIGNAL.** The brief names this exactly:
  *"changing `daemon status` wording without separating progress signals."* **One signal is answering two
  questions — that is the defect.**
- ❌ Fixing the half-live banner suppression (`C3-OBS-5`). Different surface, separate finding.
- ❌ Stopping the daemon from retrying an unrecoverable `AuthError` — **that is arguably correct** (the key may be
  rotated back). **This slice makes it VISIBLE, which is the actual gap.**
- ❌ 🔴 **Adding a `print()` to `cli.py`.**

## 10. Process

**CI:** `unit`. **NEVER run the suite locally.** Replay both AST ratchets.
**Self-merge on green: YES.** **Verifier: not required.**
**Rollback:** CODE-REVERSIBLE (the new field is **additive**).
**State remaining unknowns honestly.**
