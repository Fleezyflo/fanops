# v0.1 demo record

Operator acceptance evidence for the v0.1 prototype ship route ([v0.1-ship-route.md](./v0.1-ship-route.md)).

**Date:** 2026-07-12

**Source used:** scratch workspace root `FANOPS_ROOT=/tmp/fanops-s08-demo` (throwaway; dryrun; no live Postiz).

---

## §0 step 1 · Install

- [x] `python3.12 -m venv /tmp/fanops-s08-venv && source /tmp/fanops-s08-venv/bin/activate`
- [x] `pip install -e '.[dev,studio,transcribe,framing]'` (from repo root `/workspace`)
- [x] `./scripts/setup-hooks.sh` → wired `core.hooksPath -> .githooks`

---

## §0 step 2 · Doctor

**Command:** `FANOPS_ROOT=/tmp/fanops-s08-demo fanops doctor`

**Exit code:** `1` (not fully green in scratch env — expected gaps below)

```
fanops doctor
  [PASS] .env / FANOPS_* vars valid (strict Settings)
  [PASS] ffmpeg on PATH
  [PASS] ffprobe on PATH
  [PASS] whisper on PATH
  [FAIL] faster-whisper importable ([asr] extra)
  [PASS] yt-dlp on PATH (only for `fanops pull <url>`)
  [PASS] brand brief present (context.md)
  [PASS] accounts.json valid (every active channel mapped to an id)
  [PASS] IG insights readable (Meta Graph media insights)
  [PASS] active IG accounts have their OWN ig_user_id (no shared/borrowed Meta id)
  [FAIL] publish daemon alive + queue draining (heartbeat + past-due backlog)
  [ok] docker: skipped (not configured)
  [ok] postiz: skipped (not configured)
  [ok] zernio: skipped (not configured)
  - poster backend: dryrun (dryrun — writes payloads, posts nothing)
  - learning loop: NOT validation-confirmed
```

**Notes:** Scratch install uses `[transcribe]` (legacy whisper CLI) not `[asr]` (faster-whisper); no daemon installed. Full §0 green doctor requires operator machine with `[asr]` and/or `fanops daemon install` per hints.

---

## §0 step 3 · Studio + Go Live intake (S04)

- [ ] **operator-gated** — pending operator-present live session (Postiz URL + API key, account mapping, channel readiness matrix all ✓)

---

## §0 step 4 · Upload (S02)

- [ ] **operator-gated** — pending operator-present live session (Run tab Upload video → Ingest inbox, chunked resumable upload)

---

## §0 step 5 · Prepare + Review focus (S07)

- [ ] **operator-gated** — pending operator-present live session (Run tab Prepare everything → bare `/review` picker/auto-focus → Approve selected)

---

## §0 step 6 · Schedule → publish

- [ ] **operator-gated** — pending operator-present live session (GO LIVE confirm → real publish)

---

## §0 step 7 · Metrics + S06 rotation

### Post URLs

| post id | account | platform | live URL |
|---------|---------|----------|----------|
| — | — | — | *pending operator-present live session* |

### Metrics arrival

| post id | metrics timestamp (UTC) |
|---------|-------------------------|
| — | *pending* |

### S06 consecutive tag lines (same account)

| post | hashtag tag line |
|------|------------------|
| post 1 | *pending* |
| post 2 | *pending* |

**Evidence criterion:** tag lines for two consecutive posts on the same handle must differ (S06 rotation).

---

## Unit-test proxy (automated, not live demo)

S06 rotation logic verified in CI: `tests/test_hashtag_rotation.py::test_consecutive_ingests_differ`.
