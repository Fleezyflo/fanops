# v0.1 demo record

Operator acceptance evidence for the v0.1 prototype ship route ([v0.1-ship-route.md](./v0.1-ship-route.md)).

**Date:** 2026-09-08

**Source used:** operator workspace `FANOPS_ROOT=/Users/molhamhomsi/FanOps` (live Postiz + Zernio; `FANOPS_LIVE=1`).

---

## §0 step 1 · Install

- [x] `python3.12 -m venv .venv && source .venv/bin/activate`
- [x] `pip install -e '.[dev,studio,asr,framing]'` (from repo root)
- [x] `./scripts/setup-hooks.sh` → wired `core.hooksPath -> .githooks`

---

## §0 step 2 · Doctor

**Command:** `FANOPS_ROOT=/Users/molhamhomsi/FanOps fanops doctor`

**Exit code:** `0` (after operator queue cleanup + markmakmouly re-spread; 2026-09-08)

```
fanops doctor
  [PASS] .env / FANOPS_* vars valid (strict Settings)
  [PASS] ffmpeg on PATH
  [PASS] ffprobe on PATH
  [PASS] faster-whisper importable ([asr] extra)
  [PASS] yt-dlp on PATH (only for `fanops pull <url>`)
  [PASS] FANOPS_RESPONDER valid (llm-only)
  [WARN] claude on PATH  -> claude is on PATH but that is NOT proof it is logged in
  [PASS] brand brief present (context.md)
  [PASS] accounts.json valid (every active channel mapped to an id)
  [PASS] personas.json valid (malformed rows skipped loud)
  [PASS] POSTIZ_URL + POSTIZ_API_KEY set (Postiz routed)
  [PASS] Postiz learning ready (key + channels mapped + cutover validated)
  [PASS] live route exists (FANOPS_LIVE=1 actually publishes)
  [PASS] hashtag Layer A scrape session present
  [PASS] Postiz backend reachable (real /integrations probe, not the nginx health-check)
  [PASS] Zernio backend auth ok (authenticated /accounts read)
  [PASS] publish daemon alive + queue draining (heartbeat + past-due backlog)
  [PASS] launchd sibling daemon keeper loaded (StartInterval 300s poll-timer)
  [PASS] Studio resident loaded (KeepAlive, localhost cockpit)
  - poster backend: dryrun (LIVE)
  - learning loop: validation-confirmed (lift fields reconciled by cutover)
```

**Notes:**

- Ship doctor has zero Meta Graph checks (no token, insights scope, or hashtag quota). IG insights scope surfaces via Studio Home strip (`insights_blocked.json`) and `pull_imported_insights` — not any doctor command.
- Queue was greened by operator ops: 12 retired `molhamhomsi` past-grace rows discarded (`bulk-send-to-review` → Reject); 232 `markmakmouly` queued posts re-spread from now (`reschedule_bucket` scoped to handle). Census after ops: `drainable=0`, `total_queued=1194`.
- R2 mirror configured (`FANOPS_MEDIA_PUBLIC_BASE` + `R2_*`). Daemon `com.fanops.run` loaded.

---

## §0 step 3 · Studio + Go Live intake (S04)

- [x] Postiz URL + API key connected (doctor: Postiz reachable + learning ready)
- [x] Accounts present in `accounts.json` (7 handles; IG/TikTok/YouTube platforms)
- [x] Channel mappings saved; readiness matrix green at go-live (`FANOPS_LIVE=1`, live route exists)

---

## §0 step 4 · Upload (S02)

- [x] Ingest path exercised — ledger holds **61 sources** (inbox → pipeline catalogue)

---

## §0 step 5 · Prepare + Review focus (S07)

- [x] Prepare + review path exercised — ledger holds **1748 clips**; approval gate active (**17** posts `awaiting_approval` at capture)

---

## §0 step 6 · Schedule → publish

- [x] Live publish confirmed — **249** posts with live permalinks (`published` / `analyzed`); R2 public media mirror in use for Postiz fetch

---

## §0 step 7 · Metrics + S06 rotation

Shipped post metrics arrive via **Postiz/Zernio** (`pull_metrics` → `PostizMetricsClient` / `ZernioMetricsClient`). Meta Graph is not on the authored-post metrics path (#1196).

### Post URLs

| post id | account | platform | live URL |
|---------|---------|----------|----------|
| post_28668caf4049 | cisumwolfhom | instagram | https://www.instagram.com/reel/Dc_keZ9FG56/ |
| post_284d52bcd84d | cisumwolfhom | instagram | https://www.instagram.com/reel/Dc_SBiEDcH4/ |
| post_2f9362f23ad4 | backlikeineverleft | tiktok | https://www.tiktok.com/@backlikeineveeft/video/7682838746106187029 |
| post_2e291eb4c368 | backlikeineverleft | tiktok | https://www.tiktok.com/@backlikeineveeft/video/7682793467298499860 |

### Metrics arrival

| post id | metrics timestamp (UTC) |
|---------|-------------------------|
| post_28668caf4049 | 2026-09-08T05:00:44.205920Z |
| post_284d52bcd84d | 2026-09-08T14:24:04.047987Z |
| post_2f9362f23ad4 | 2026-09-08T06:38:52.755339Z |
| post_2e291eb4c368 | 2026-09-08T14:24:04.047987Z |

*Captured via `fanops track --window 7d` on 2026-09-08 (`metrics_series` `captured_at`; exit 0, `analyzed=249 series_rows+=2`).*

### S06 consecutive tag lines (same account)

| post | hashtag tag line |
|------|------------------|
| post_28668caf4049 (newer) | `#wildclips #legendary #boldstatement #hiphopnews` |
| post_284d52bcd84d (older) | `#pressconference #legendary #hiphopnews #realrap` |

**Evidence criterion:** tag lines for two consecutive posts on the same handle differ (S06 rotation) — **PASS** (cisumwolfhom IG pair above).

---

## Unit-test proxy (automated, not live demo)

S06 rotation logic verified in CI: `tests/test_hashtag_rotation.py::test_consecutive_ingests_differ`.
