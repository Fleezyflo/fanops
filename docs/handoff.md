# HANDOFF — MOH FLOW FAN OPS

This file is the cross-session source of truth. It is **rewritten each session** (the §Now block is point-in-time, not an append-only log). Frozen provenance lives in `docs/archive/handoff-history.md` (created on first archive). The build plan is `docs/superpowers/plans/2026-05-31-fanops-real-build-v2.md`. Deviations from that plan made during the build are recorded in the auto-memory file `fanops-build-deviations.md`.

## §State (re-verify the Check commands before quoting)

- **Branch:** `main` (canonical line) @ **`33103f2`**. The **end-to-end finalization MVP (M1+M2+M3)** is MERGED — the full upload→publish→LEARN loop runs on Postiz. M1 browser ingestion (PR #24 `963c48b`), M2 Postiz learning loop (PR #25 `9c522cf`), M3 Postiz-aware cutover + Studio Validate-learning (PR #26 `33103f2`), atop the review-first Studio + Postiz go-live work (PRs #18–#22). **M4 (setup/runbook docs + doctor Postiz-learning check) is in flight on `feat/m4-setup-runbook`.** Check: `git branch --show-current` / `gh pr list --repo Fleezyflo/fanops --state open`.
- **FanOps Studio (on main, PR #17):** a local Flask+Jinja+HTMX content cockpit — `fanops studio` → `127.0.0.1:8787`; tabs **Review · Schedule · Lift** — in new pkg `src/fanops/studio/` (`views`/`actions`/`app` + templates + vendored htmx 2.0.3). Reads lock-free; the 2 writes (reschedule/edit-caption + the snooze helper) go through ONE `Ledger.transaction` each with in-lock `queued`+not-imminent guards (TOCTOU-safe vs cron). One core change: `FANOPS_PUBLISH_LEAD_MINUTES` (config.py, default 0 → byte-identical; non-int/neg→0) adds a CONSTANT `lead_minutes` offset in `crosspost.surface_time`. Optional extra `[studio]=flask>=3.0`; the CLI import is LAZY (core stays flask-free, guarded by a test). Check: `grep -n "def create_app" src/fanops/studio/app.py && grep -n "publish_lead_minutes" src/fanops/config.py`.
- **v3-UCB bandit (on main):** `variant_learning.ucb_rank` is a deterministic UCB1 allocator (`score = mean_lift + c·sqrt(ln N / n)`, no RNG/hash/wall-clock); `FANOPS_VARIANT_UCB` (default OFF) selects it for the OWN-surface caption bias instead of v2 gated-greedy `best_hooks`. **Amplify floor UNCHANGED** — `variant_amplify` keeps `best_hooks` as its C1 floor and NEVER calls `ucb_rank` (mutation-proven). Check: `grep -n "def ucb_rank" src/fanops/variant_learning.py`.
- **Working tree:** clean (only an untracked, unrelated `-filters` artifact). Check: `git status -sb` (expect `## main...origin/main`, in sync).
- **HEAD (of `main`):** `11db76d` (handoff rev 27 — the last studio commit) → this rev-28 handoff commit lands on top. Check: `git log --oneline -1`.
- **Remote:** `origin` → `github.com/Fleezyflo/fanops` (**PRIVATE**). PRs #1–#9, #11–#17 MERGED; #10 CLOSED (dup of #9); **NO open PRs.** Check: `gh pr list --repo Fleezyflo/fanops --state open` (expect empty).
- **Responder auth (IMPORTANT):** the autonomous LLM responder uses the operator's **EXISTING `claude` subscription** via plain `claude -p` (NOT `--bare`) — it needs `claude login` on the host, **NOT `ANTHROPIC_API_KEY`**. Check: `grep -n "strict-mcp-config" src/fanops/llm.py`.
- **Unit/full tests:** on `feat/m4-setup-runbook` (main@`33103f2` + M4): **871 unit passed, 16 deselected** (`-m "not integration"`, re-derived live this rev). The growth over rev-28's 502 is the review-first Studio + Postiz go-live + M1–M3 + M4 tests. Re-derive the live number; do not quote a stale count. Check: `source .venv/bin/activate && python -m pytest -q` (516/1) and `... -m "not integration"` (502). A fresh venv needs `pip install -e ".[dev,studio]"` (python3.12) — **`,studio` is REQUIRED for the flask route tests to RUN** (they `importorskip("flask")` and skip cleanly otherwise). RECURRING FALSE ALARM: a subagent running bare `pytest` (no venv) reports "pytest-mock not installed"; ALWAYS use `.venv/bin/python -m pytest`.
- **ffmpeg (toolchain):** the system `ffmpeg` is **`ffmpeg-full`** (8.1.1, `brew link --overwrite --force`'d) — text filters (`drawtext`/`subtitles`/`ass`, libass/freetype) for subtitles + creative-variation hooks; the Studio's inline `<video>` relies on the H.264/AAC/+faststart codec invariant (integration-test-pinned). The default formula is STRIPPED; if relinked, subtitles/hooks FAIL OPEN. Check: `ffmpeg -hide_banner -h filter=drawtext | head -1`. CI's Linux runner has full ffmpeg via apt.
- **Lint:** `ruff check src/` is GREEN. Check: `source .venv/bin/activate && ruff check src/` (expect "All checks passed!"). A fresh venv needs `pip install -e ".[dev]"` first.
- **Integration tests:** on `main`: **14 passed, 1 skipped** (live Blotato smoke skips without creds). Adds `test_studio_real.py` (real ffmpeg render → Studio serves the bytes → ffprobe confirms H.264+AAC, the codec invariant) alongside `test_discover_real.py`, `test_variant_*_real.py`. Check: `source .venv/bin/activate && python -m pytest tests/integration -q`.
- **Module/test parity:** on `feat/m4-setup-runbook`: **54 src / 72 tests** (re-derived live this rev). Check: `ls src/fanops/*.py src/fanops/post/*.py src/fanops/studio/*.py | wc -l && ls tests/*.py tests/integration/*.py | wc -l`.
- **Real tooling present:** ffmpeg 8.1.1 (ffmpeg-full), ffprobe, whisper CLI, `say` (macOS TTS) on PATH; whisper `tiny.pt` cached. Check: `for b in ffmpeg ffprobe whisper say; do command -v $b; done`.
- **Posting backend default:** `dryrun` (writes payload JSON to `05_scheduled/`, posts nothing). The **live headline path is now Postiz** (`FANOPS_POSTER=postiz` + `POSTIZ_URL` + `POSTIZ_API_KEY`) — it both PUBLISHES and feeds the learning loop (M2 `PostizMetricsClient`). Blotato (`rest|mcp` + `BLOTATO_API_KEY`) is RETIRED but its code path remains. `is_live_backend` is backend-aware: postiz→`POSTIZ_API_KEY`, rest/mcp→`BLOTATO_API_KEY`. An unknown `FANOPS_POSTER`→dryrun (never a false LIVE banner). Operators set all three through the Studio Go-Live tab, not by hand. Check: `grep -n "def is_live_backend" src/fanops/config.py`.
- **CI:** `.github/workflows/ci.yml` — `unit` job (installs `.[dev,studio]`) + `real-tooling E2E` job (`.[dev,transcribe,studio]`), both run on push + PR into `main`. M1–M3 PRs (#24/#25/#26) all landed CI-green both jobs. Re-check the latest: `gh run list --repo Fleezyflo/fanops --branch main --limit 1`. Durable lesson: studio route/integration tests import flask, so CI installs the `studio` extra AND those test files `pytest.importorskip("flask")` — local-green ≠ CI-green when a dep lives only in `.venv`; always `gh pr checks` after the first push. (Cosmetic backlog: bump deprecated Node-20 actions.)
- **Toolchain-absent behavior:** every `subprocess.run` of a media binary is guarded — pre-Source/one-shot sites raise typed `errors.ToolchainMissingError` → exit 2; pipeline sites record a retriable per-unit `error`. Check: `grep -n "ToolchainMissingError" src/fanops/errors.py`.
- **Concurrency (Phase B + follow-up):** `advance()` AND every standalone write command (`track`/`reconcile`/`adjust`/`ingest`/`pull`) AND the Studio's `reschedule_post`/`edit_caption`/`snooze_clip` run the whole load→mutate→save inside ONE `Ledger.transaction(cfg)` holding the `fcntl.flock` (acquired BEFORE load, single atomic save on clean exit). A 2nd live run is excluded with a typed `LockBusyError`; slow I/O stays OUTSIDE the lock. Check: `grep -n "def transaction\|_save_unlocked" src/fanops/ledger.py && grep -ln "Ledger.transaction" src/fanops/cli.py src/fanops/studio/actions.py`.

## 1. What this is

An autonomous fan-account engine for Moh Flow (bilingual EN/AR rapper). It ingests his videos, decides which moments are worth posting (transcript + audio/scene signals → an agent decision with a recorded reason), cuts platform-ready clips with agent-written EN/AR captions, cross-posts to every fan account × platform via Blotato (staggered for opsec, with a subtle non-synchronized artist @mention), then pulls real performance back to make more of what works. **FanOps Studio** (`fanops studio`, on main) adds a local web cockpit for the operator's editorial job: watch the rendered clips, tweak captions, reschedule/snooze the upcoming queue, read per-variant lift — without touching the autonomous machinery. See `README.md` (front door), `MohFlow-FanOps/00_control/RUNTIME.md` (operating loop + production seams), `MohFlow-FanOps/00_control/RISK.md` (operator risk-acceptance for the multi-account model), `MohFlow-FanOps/00_control/context.md` (functional — injected as agent guidance).

## 2. Architecture

- **Unit chain:** `Source → Moment → Clip → Post`, over one git-versioned JSON ledger (`00_control/ledger.json`, atomic temp+os.replace write under a file lock).
- **Identity:** content-addressed SHA-based ids everywhere (`ids.py`: `make_id`/`child_id`/`surface_key`) — NEVER builtin `hash()` (the #1 v1 bug; cross-process stability is proven by a subprocess idempotency test).
- **Agent gates:** generative steps (`decide_moments`, `write_captions`) cross a file contract in `04_agent_io/requests/` — code writes a `*.request.json` (stamped with a `request_id`), the agent/responder writes a validated `*.response.json` echoing that id, code resumes. Stale responses can never be applied (request_id correlation).
- **Two responders** (`responder.py`): `ManualResponder` (no-op; a human/cron writes responses) vs `LlmResponder` (`FANOPS_RESPONDER=llm`; **autonomous via `claude -p`** — shells the Claude Code CLI through `llm.claude_json` with committed `prompts.py` templates + the gate's pydantic JSON schema, validates against `MomentDecision`/`CaptionSet`, **per-request quarantine**). Needs `claude` on PATH (authenticated); model is injectable for tests.
- **Three posters** (`post/`): `dryrun` (default), `rest` (`BlotatoRestPoster`, retry/backoff/typed-errors), `mcp` (`BlotatoMcpPoster`, injected `tool_caller`).
- **Pipeline** (`pipeline.py` `advance()`): per-unit error quarantine (one bad source/moment/clip → `error` state, never wedges the pass). **CLI** (`cli.py`): `status/ingest/pull/advance/respond/track/adjust/gc/run/digest` + recovery verbs + `discover`/`intake` + **`studio`** (lazy-imported web UI).
- **Studio** (`studio/`, PR #17): `views.py` pure read-models (Flask-free) → `actions.py` lock-safe mutations → `app.py` Flask factory (tab routes + variant-aware `/media/<post_id>` + `/clips/<clip_id>` + POST mutation routes). Templates + vendored HTMX; no JS build step.
- **State machines** (`models.py`): separate `SourceState`/`MomentState`/`ClipState`/`PostState`. `failed`≠`analyzed`; `held` (brand-risk) and `retired` are first-class.

## 3. Gotchas / non-obvious invariants

- **`--base-time` is the SCHEDULE ANCHOR, not the publish cutoff.** crosspost staggers posts to times AFTER base-time; `publish_due(now=None)` publishes whatever is due as of REAL wall-clock now. To publish in the same pass (backfill), pass a `--base-time` in the past. **Studio's editable window** therefore exists only when `FANOPS_PUBLISH_LEAD_MINUTES>0` AND cron runs with a now/future base-time; otherwise the window is empty by design and Review/Schedule show read-only history.
- **Conservative retirement:** `adjust` amplifies top `--winner-pct` (0.3) but retires only the bottom `--retire-pct` (0.2) AND below `--lift-floor` (20.0). Retiring a clip also retires its moment.
- **Three surfaced-but-auto-unrecovered stuck states** (all in the digest + counts, no silent loss): `submitting` (mid-publish crash); **`needs_reconcile`** (ambiguous 5xx/timeout — NOT re-POSTed, may be live); a `published` post Blotato never returned metrics for. `needs_reconcile` only arises under `rest`/`mcp`, never `dryrun`.
- **`dryrun` posts are trackable** (audit C4): `submission_id = f"dryrun_<post_id>"`, so the `publish → track → analyzed → adjust` loop is exercisable offline.
- **Studio naive-time trap:** `timeutil.iso_z` treats a NAIVE datetime as LOCAL time; `reschedule_post` therefore coerces naive→UTC (`replace(tzinfo=utc)`) BEFORE `iso_z`, never relying on `iso_z` to normalize (a naive scheduled_time would otherwise mark the post `failed` in `publish_due`).
- **Whisper model:** default `turbo`; falls back to the best cached `~/.cache/whisper/*.pt` when the requested model can't be downloaded. In THIS env only `tiny` is cached.

## 4. Health checks

Run the §State Check commands. The integration E2E (`tests/integration/test_e2e_real.py`) is the golden path: real `say` TTS → real whisper transcript → real ffmpeg 1080×1920 vertical clip → 2 dryrun posts published; it pins `tiny` in-test and skips cleanly without a cached checkpoint. The Studio integration test (`test_studio_real.py`) is the second golden path: real ffmpeg render → Studio serves the bytes → ffprobe confirms H.264+AAC.

## 5. Now (rewritten each session)

**As of 2026-06-14 (rev 29 — end-to-end finalization: the MVP (M1+M2+M3) is MERGED to `main`; the full upload→publish→LEARN loop runs on Postiz. M4 setup/runbook docs + a doctor Postiz-learning check are in flight on `feat/m4-setup-runbook`).**

**Most recent shipped (on `main` @ `33103f2`):** the **end-to-end finalization MVP** — a brand-new video can now go upload → reviewed → scheduled → published live on Postiz → real per-variant lift, entirely in the Studio, on the operator's real backend. The three milestones:
- **M1 browser ingestion** (PR #24 `963c48b`): the Run tab **Upload video** form streams raw video into `01_inbox/` (traversal-safe, 2 GiB cap, atomic `.uploadpart`→`os.replace`) → **Ingest inbox** catalogues it. No Finder, no CLI.
- **M2 Postiz learning loop** (PR #25 `9c522cf`): `PostizMetricsClient` maps Postiz `analytics/post` → `lift_score`; `pull_metrics`/`cmd_track` are backend-polymorphic; **`is_live_backend` is now backend-aware** (postiz→`POSTIZ_API_KEY`). The loop (track→classify→amplify) runs on Postiz; the Lift tab populates from real metrics.
- **M3 Postiz-aware cutover** (PR #26 `33103f2`): `cutover.py` dispatches postiz↔blotato; the Studio **Go-Live → "5 · Validate learning"** posts one operator-selected throwaway probe, reconciles real labels, writes `cutover.json metrics_confirmed` → `learning_validated` unfreezes `variant_amplify` on Postiz. Never auto-fires; the key is never echoed.

**M4 (this rev, in flight on `feat/m4-setup-runbook`):** mostly docs + one small TDD'd code change.
- NEW `docs/POSTIZ_SETUP.md` (self-hosted Docker as the PRIMARY standup path; the API-key + base-URL step; key is write-only), NEW `docs/RUNBOOK.md` (the cold upload→publish→learn walkthrough, every step naming the real control), `docs/GOLIVE.md` rewritten (Blotato Path A dropped; **Path A = Postiz publishes-AND-learns**, Path B = manual; the old `account_id` hand-edit instruction excised — mapping is the Studio "3 · Map each channel" tab now).
- `doctor.py`: a new **"Postiz learning ready"** composite check (booleans only: key set + every active channel mapped + cutover validated; cascading hint naming the first gap) + de-Blotato'd the accounts-mapping hint and the learning note. TDD'd in `tests/test_doctor.py` (+7). It renders in the Go-Live "4 · Readiness" panel for free (`golive_status` already forwards `report["checks"]`). Suite **871 unit, ruff clean**.

**What works right now:**
- **On `main` (`33103f2`):** everything through rev 28 + the review-first Studio (one-click Prepare, editable cockpit, Publish-now) + Postiz go-live surface + per-platform account onboarding + the M1–M3 MVP. 871 unit, ruff clean.
- **PRIME DIRECTIVE holds:** the C1 amplify/delete path stays blind to variant signal + the UCB bandit; `learning_validated` is the HARD freeze; no speculative actuator fires until a real Postiz row is reconciled. The Studio never marks a post `failed` and never auto-fires a real post.

**Live state caveats:**
- **The live end-to-end proof is operator-gated:** M4's docs + doctor check are buildable offline, but actually publishing a real Postiz post + seeing the Lift tab populate needs the operator's own running Postiz instance (self-hosted Docker — see `POSTIZ_SETUP.md`). Not yet exercised against a real instance.
- **Blotato is RETIRED** but its `rest`/`mcp` poster + metrics code remains (byte-identical, regression-guarded). Don't revive it; don't re-introduce Blotato-first framing.
- **ffmpeg is `ffmpeg-full` (local fix)** — the Studio's inline `<video>` relies on the H.264/AAC/+faststart codec invariant (integration-test-pinned). CI's Linux runner has full ffmpeg.
- **`POSTIZ_API_KEY` is write-only everywhere** — never logged, echoed, rendered, or returned in an ActionResult; only a `key_set`/`ok` boolean surfaces. The doctor report exposes booleans + hints only (grep-tested no key leak).

**Open items, in priority order:**
1. **Finish M4 + ship it:** ecc:python-review the doctor change → PR → CI green → merge. Then the operator runs the live go-live proof against their real Postiz.
2. **M5 (trimmable):** held-clip *release* in the Studio (reuse `cli.py` unhold transition); `pytest-cov` into `[dev]` + a report-only CI step (no `--cov-fail-under`); deferred fast-follows (hashtag editor, drag-reorder). Low-value backlog: trending-audio, daypart scheduling, CI Node-20 bump.

**Pick up here:**
- **`main` @ `33103f2` is the MVP; M4 is on `feat/m4-setup-runbook`** (this rev's docs + doctor check). Verify with the §State Check commands (re-derive the suite count live — 871 unit at this rev — never quote a stale number). `gh pr list --state open` to see if M4's PR is up yet.
- **To run the Studio locally:** `.venv/bin/fanops studio` (or `pip install -e '.[studio]'` in a fresh venv first) → `http://127.0.0.1:8787`. Browser-QA a go-live flip on a TEMP root, NEVER the repo `.env` (a flip dual-writes `.env` + `os.environ`).
- **Never stage `MohFlow-FanOps/00_control/accounts.json`** — it's left deliberately dirty across this work; stage milestone files explicitly each commit.
- **Standing process gotchas (durable multi-agent wisdom):** (a) a FRESH worktree needs its OWN editable venv (`pip install -e '.[dev,studio]'`, **python3.12**). (b) Verifying via the real CLI / scratch render: **`cd` to a scratch root first** (`Config()` resolves from cwd; the data root is `MohFlow-FanOps/00_control/…` — derive paths from `cfg`, never hand-guess). (c) **ALWAYS independently re-run the committed artifact + read the code before trusting a green.** (d) **a feature behind an optional extra must skip-guard its tests AND add the extra to CI** — check `gh pr checks` after the first push. (e) GateGuard fires on every first-file-touch — comply each time (importers/API/schema/verbatim-instruction). (f) an adversarial/skeptic `holds=false` is a lead to investigate, not an auto-verdict.
- Standing invariants: **Do not re-raise audit-C2 (byte-identical media).** **The C1 amplify/delete path stays blind to variant signal AND the UCB bandit.** **`learning_validated` is the hard freeze — nothing speculative fires until cutover reconciles a real row.** **A fresh/local venv must `pip install -e '.[dev,studio]'`** (python3.12).
