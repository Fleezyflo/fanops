# HANDOFF — MOH FLOW FAN OPS

This file is the cross-session source of truth. It is **rewritten each session** (the §Now block is point-in-time, not an append-only log). Frozen provenance lives in `docs/archive/handoff-history.md` (created on first archive). The build plan is `docs/superpowers/plans/2026-05-31-fanops-real-build-v2.md`. Deviations from that plan made during the build are recorded in the auto-memory file `fanops-build-deviations.md`.

## §State (re-verify the Check commands before quoting)

- **Branch:** `main` is the canonical line; Phase B is MERGED into it (PR #1). Phase C work is on branch `phase-c-validation` in **PR #2 (OPEN, mergeState CLEAN, NOT yet merged — the human owns the merge)**. Check: `git branch --show-current` (main tree) / `gh pr view 2 --repo Fleezyflo/fanops --json state`.
- **Working tree:** clean (`## main...origin/main`). Check: `git status -sb`.
- **HEAD (of `main`):** `fa44817` (Merge PR #1: Phase B). Phase C HEAD is `58addbf` on `phase-c-validation` (worktree `/Users/molhamhomsi/fanops-phase-c`). Check: `git log --oneline -1` (main tree).
- **Remote:** `origin` → `github.com/Fleezyflo/fanops` (**PRIVATE**). PR #1 (`/pull/1`) **MERGED** 2026-06-01T17:38. PR #2 (`https://github.com/Fleezyflo/fanops/pull/2`, Phase C) **OPEN** into `main`. Check: `git remote -v && gh pr view 2 --repo Fleezyflo/fanops --json state,mergeable`.
- **Unit tests:** Phase C (PR #2 / `phase-c-validation`): **254 passed, 5 deselected**. `main` (post-Phase-B, pre-Phase-C): 243 passed, 5 deselected. Check: `source .venv/bin/activate && python -m pytest -q -m "not integration"`. (Phase C added +11 over 243: 2 C1 NaN/Inf, 1 C2 wrong-language hold, 1 C3 unknown-surface hold, 6 C2-hardening region/case parametrized + 1 C2 mismatch-regression — the prompt-echo assertion strengthens an existing test.) **Toolchain-free**: the full unit suite passes with `PATH` stripped of ffmpeg/ffprobe/whisper (the CI `unit`-job condition).
- **Integration tests:** 4 passed, 1 skipped (live Blotato smoke skips without creds). Check: `source .venv/bin/activate && python -m pytest -q -m integration`. Full suite (both markers): Phase C `258 passed, 1 skipped` (default invocation runs the 4 normally-deselected tests); `main` `247 passed, 1 skipped`. Lock/deadlock tests run under `pytest-timeout` (`--timeout=20`); a hang is a hard FAIL. (`pytest-timeout` installed in the worktree venv as the enforcement instrument — NOT a shipped dep.)
- **Module/test parity:** 34 src modules (Phase A added `llm.py` + `prompts.py`), 37 test files (Phase A added `test_llm.py` + `test_prompts.py`). The integration *marker* selects 5 tests: the 3 in `tests/integration/` (`test_e2e_real.py` ×2 — the transcript-drives-moment golden path + `test_blotato_smoke.py` creds-skip + `test_publish_track_loop.py`) PLUS `tests/test_adjust.py::test_amplify_then_ingest_then_render_produces_new_clip` (marked `integration` because it shells real ffmpeg — CI-1) — i.e. `-m integration --collect-only` = 5. Check: `ls src/fanops/*.py src/fanops/post/*.py | wc -l && ls tests/*.py tests/integration/*.py | wc -l` (34 / 37) and `python -m pytest -q -m integration --collect-only | grep -c "::"` (5).
- **Real tooling present:** ffmpeg 8.0.1, ffprobe, whisper CLI, `say` (macOS TTS) all on PATH; whisper `tiny.pt` cached (model downloads blocked by proxy in this env). The E2E pins `tiny` itself, so no env var is required; to override the model for a real `fanops` run, set `FANOPS_WHISPER_MODEL`. Check: `for b in ffmpeg ffprobe whisper say; do command -v $b; done`.
- **Posting backend default:** `dryrun` (writes payload JSON to `05_scheduled/`, posts nothing). Switch via `FANOPS_POSTER=rest|mcp` + `BLOTATO_API_KEY`. Check: `grep -n "FANOPS_POSTER" src/fanops/config.py`.
- **CI:** `.github/workflows/ci.yml` — `unit` job (no toolchain) + `e2e` job (installs toolchain, `FANOPS_REQUIRE_E2E=1`, audit H10). Runs on push to `main` AND on `pull_request` into `main`. **PR #2 (Phase C) CI:** run `26775233413` = **`completed success`** (both jobs: `unit` ✓ 20s, `real-tooling E2E` ✓ — watched to green with `--exit-status`, first try, no CI-1 trap this time). PR #1 (Phase B) CI also green before merge. Check: `gh run list --repo Fleezyflo/fanops --branch phase-c-validation --limit 3`. (Non-fatal annotation: "Node.js 20 actions are deprecated" — cosmetic.)
- **Toolchain-absent behavior:** every `subprocess.run` of a media binary (ffmpeg/ffprobe/whisper/yt-dlp) is guarded — `check=False` does NOT catch a pre-launch `FileNotFoundError`, so absent binaries used to crash. Now: pre-Source/one-shot sites (ffprobe at ingest, yt-dlp at `pull`) raise typed `errors.ToolchainMissingError` → `cli.main` exit 2 + "install …", no traceback; pipeline-quarantined sites (ffmpeg render, whisper transcribe, ffmpeg signals) record a retriable per-unit `error`. Check: `grep -rn "subprocess.run" src/fanops/ | grep -v "_run_ffprobe\|_run_ffmpeg\|try:"` (expect only the lines INSIDE the guards) and `grep -n "ToolchainMissingError" src/fanops/errors.py`.
- **Concurrency (Phase B):** `advance()` runs the WHOLE load→mutate→save inside ONE `Ledger.transaction(cfg)` holding the `fcntl.flock` — acquired BEFORE load, single atomic save on clean exit (audit B4). A 2nd live run is excluded with a typed `LockBusyError`. `publish_due(in_transaction=True)` uses the unlocked save (no self-deadlock); crosspost+publish are wrapped (log+continue) so a non-auth raise can't roll back the pass, while a fatal `BlotatoAuthError` still escapes (F52). The slow `claude -p` responder runs OUTSIDE the lock (`request_id` correlation + capture-and-recheck against a mid-call re-seed, audit A3). Check: `grep -n "def transaction\|_save_unlocked" src/fanops/ledger.py && grep -n "rid_before\|rid_after" src/fanops/responder.py`.

## 1. What this is

An autonomous fan-account engine for Moh Flow (bilingual EN/AR rapper). It ingests his videos, decides which moments are worth posting (transcript + audio/scene signals → an agent decision with a recorded reason), cuts platform-ready clips with agent-written EN/AR captions, cross-posts to every fan account × platform via Blotato (staggered for opsec, with a subtle non-synchronized artist @mention), then pulls real performance back to make more of what works. See `README.md` (front door), `MohFlow-FanOps/00_control/RUNTIME.md` (operating loop + production seams), `MohFlow-FanOps/00_control/RISK.md` (recorded operator risk-acceptance for the multi-account model), `MohFlow-FanOps/00_control/context.md` (functional — injected as agent guidance).

## 2. Architecture

- **Unit chain:** `Source → Moment → Clip → Post`, over one git-versioned JSON ledger (`00_control/ledger.json`, atomic temp+os.replace write under a file lock).
- **Identity:** content-addressed SHA-based ids everywhere (`ids.py`: `make_id`/`child_id`/`surface_key`) — NEVER builtin `hash()` (the #1 v1 bug; cross-process stability is proven by a subprocess idempotency test).
- **Agent gates:** generative steps (`decide_moments`, `write_captions`) cross a file contract in `04_agent_io/requests/` — code writes a `*.request.json` (stamped with a `request_id`), the agent/responder writes a validated `*.response.json` echoing that id, code resumes. Stale responses can never be applied (request_id correlation).
- **Two responders** (`responder.py`): `ManualResponder` (no-op; a human/cron writes responses) vs `LlmResponder` (`FANOPS_RESPONDER=llm`; **autonomous via `claude -p`** — default model shells the Claude Code CLI through `llm.claude_json` with committed `prompts.py` templates + the gate's pydantic JSON schema, validates against `MomentDecision`/`CaptionSet`, **per-request quarantine** so one bad/failed gate logs + stays pending without halting the rest). Needs `claude` on PATH (authenticated); model is injectable for tests.
- **Three posters** (`post/`): `dryrun` (default), `rest` (`BlotatoRestPoster`, retry/backoff/typed-errors), `mcp` (`BlotatoMcpPoster`, injected `tool_caller`).
- **Pipeline** (`pipeline.py` `advance()`): per-unit error quarantine (one bad source/moment/clip → `error` state, never wedges the pass). **CLI** (`cli.py`): `status/ingest/pull/advance/respond/track/adjust/gc/run/digest`.
- **State machines** (`models.py`): separate `SourceState`/`MomentState`/`ClipState`/`PostState`. `failed`≠`analyzed`; `held` (brand-risk) and `retired` are first-class.

## 3. Gotchas / non-obvious invariants

- **`--base-time` is the SCHEDULE ANCHOR, not the publish cutoff.** crosspost staggers posts to times AFTER base-time; `publish_due(now=None)` publishes whatever is due as of REAL wall-clock now. To publish in the same pass (backfill), pass a `--base-time` in the past. (This was a fixed plan contradiction.)
- **Conservative retirement:** `adjust` amplifies top `--winner-pct` (0.3) but retires only the bottom `--retire-pct` (0.2) AND below `--lift-floor` (20.0) — objectively-fine clips above the floor are never retired. Retiring a clip also retires its moment (so it can't be re-rendered).
- **Three surfaced-but-auto-unrecovered stuck states** (all in the digest + counts, no silent loss): a post stranded in `submitting` by a mid-publish crash; a post in **`needs_reconcile`** (REST hit an ambiguous 5xx/timeout after sending the body — it may be live, so it is NOT re-POSTed; Blotato has no idempotency key); a `published` post Blotato never returned metrics for. The first two need a poll step (`GET /v2/posts/:id` → promote/reset) — backlog item (a). `needs_reconcile` only ever arises under the `rest`/`mcp` backends, never `dryrun`.
- **`dryrun` posts are trackable** (audit C4): the dryrun poster stamps `submission_id = f"dryrun_<post_id>"`, so the `publish → track → analyzed → adjust` loop is fully exercisable offline by feeding `track` a metrics row keyed on that id. (CLI `track` still skips in dryrun — no live metrics source — but the seam is proven by `tests/integration/test_publish_track_loop.py`.)
- **Whisper model:** default `turbo`; falls back to the best cached `~/.cache/whisper/*.pt` when the requested model can't be downloaded (offline/proxied). In THIS env only `tiny` is cached.

## 4. Health checks

Run the §State Check commands. The integration E2E (`tests/integration/test_e2e_real.py`) is the golden path: real `say` TTS → real whisper transcript (asserts real whisper segment structure + ≥4 words + a vocoder-robust "anymore" anchor — CI-2) → real ffmpeg 1080×1920 vertical clip → 2 dryrun posts published. It pins the `tiny` whisper model in-test (no env var needed) and skips cleanly on a host with no cached checkpoint, so a fresh checkout / CI runner sees a clear skip rather than a cryptic `assert 0 == 1`.

## 5. Now (rewritten each session)

**As of 2026-06-01 (rev 11).**

**Most recent shipped (this session — Phase C of the live+autonomous plan: autonomous-LLM validation hardening H4/H5/H6. On branch `phase-c-validation`, worktree `/Users/molhamhomsi/fanops-phase-c`, PR #2 OPEN into `main`, NOT yet merged — the human owns the merge. Phase B's PR #1 was MERGED to `main` as `fa44817` earlier today):**
- `a59c361` — **C1 (audit H4): reject non-finite (NaN/Inf) moment timestamps.** Defense-in-depth: a `@field_validator("start","end")` on `MomentPick` (a NaN never constructs) AND a `math.isfinite` guard at the top of `validate_pick` (caught via `model_construct`). Was: `MomentPick(NaN)` constructed + `validate_pick` returned valid (NaN comparisons are all False) → `nan-nan` id + `ffmpeg -ss nan`.
- `b99a536` — **C2 (audit H5): hold clips whose caption language ≠ source language.** `CaptionItem` gains optional `language`; `ingest_captions` holds on a declared mismatch.
- `a8fe172` — **C3 (audit H6): hold with a SPECIFIC reason naming an unknown caption surface key.** A typo'd surface (`@accounts/instagram`) is held naming it, BEFORE the generic missing-caption logic.
- `d2aed22` — **C2 HARDENING** (after the Adversarial phase found 2 real defects in the literal-plan C2 and the Workflow correctly BLOCKED Integrate rather than green-wash): (1) the naive exact-string language compare wrongly HELD same-language region/case variants (`en-US`/`EN`/`en-GB`/`"en "` vs `en`) — fixed by normalizing to the base IETF subtag (`_lang_base`); (2) a wrong-language caption that nulls `item.language` slipped — the `None` exemption stays (blanket-holding would halt the autonomous pipeline) but `caption_prompt` now REQUIRES the model to self-declare `language`, closing it at our source; the external-null residual is documented in code.
- `58addbf` — **docs sync** (README + RUNTIME.md): the captions schema now carries optional `language`; the C2↔RUNTIME.md `language`-field line reconciled (commit `fa345f3` had said "CaptionItem carries no per-item language field"; C2 added it — now reconciled in the opposite direction); README held-clip list extended.

Enforcement: dedicated worktree (own venv editable-installing the worktree's OWN src — verified, no false-green), TDD watched RED→GREEN every fix, every new test mutation-proven. Driven through the **Workflow** engine: 6 named phases (Frame→Implement→Verify→Adversarial→Integrate→Close), structured-output schemas, an independent verifier + ≥2 independent skeptics per fix (never self-grading, majority vote). Run 1 `wf_152543cf-98f` (13 agents) blocked on C2; I root-caused + hardened; re-confirm run `wf_7d7baade-7c5` (3 agents) confirmed hardened C2 2/2, mutation-proven both surfaces, no new bypass. Tests: unit `254 passed, 5 deselected`; integration `4 passed, 1 skipped`. Real-CLI smoke (scratch cwd): wrong-language→HELD, typo-surface→HELD-naming-it, legit `en-US`+clean `en`→NOT-held (advance to queued post). CI run `26775233413` = `completed success`.

**What works right now:**
- **The autonomous LLM can't write a NaN/Inf timestamp (H4 closed):** rejected at both the `MomentPick` field validator and `validate_pick` (defense-in-depth); a valid finite pick still passes.
- **Wrong-language captions are held (H5 closed):** `ingest_captions` holds any clip whose declared caption `language` ≠ the source `language`, compared on the normalized base IETF subtag (so `en-US`/`EN` == `en` — no false-positive on region/casing variants). The committed `caption_prompt` makes the model self-declare its language, so our autonomous path always carries a tag.
- **Typo'd caption surfaces are held with a precise reason (H6 closed):** a caption targeting a surface key not in the requested set is held with a reason naming the bad surface, before (and distinct from) the generic missing-caption hold.
- **The three guards compose:** a clip with BOTH a wrong-language and an unknown-surface item is held deterministically (H6 runs first, wins, names the surface); a NaN pick + a valid pick in one decision → NaN rejected, valid pick reconciles.
- Everything Phase B and earlier still holds (B4 transaction lock / no-self-deadlock / M1 / M2 / A3 TOCTOU; autonomous `claude -p` responder + per-request quarantine; full dryrun pipeline; the earlier H/C hardening; toolchain-absent guards). A shared per-clip media URL across accounts is intended (C2 the audit-finding, distinct from this Phase's C2) — do NOT "fix" it.

**Live state caveats:**
- **Phase C is on a PR, not merged.** `main` is `fa44817` (Phase B); the Phase C code is on `phase-c-validation` (HEAD `58addbf`) in PR #2 (mergeState CLEAN). It only reaches `main` when the human merges. **CI run `26775233413` = `completed success`** (both jobs: `unit` ✓ 20s, `real-tooling E2E` ✓ — watched with `--exit-status`).
- **C2 None-language residual (documented, not a Phase C defect):** a hostile/buggy EXTERNAL caption response that nulls `language` while writing wrong-language text still reaches `captioned` — the ingest guard exempts a None to avoid halting the pipeline on legitimately-undeclared captions. Closed at our OWN autonomous source (the prompt self-declares); a true close needs language *detection* (a heavier dep) — backlog if external responders are ever trusted.
- **`claude -p` still needs `ANTHROPIC_API_KEY`** in the `fanops` environment (`--bare` never reads OAuth/keychain — Phase A post-merge note in `fanops-build-deviations.md`). Phase C is fully credential-free and toolchain-free for its unit tests, so this didn't surface this session, but it remains the deploy caveat.
- Still nothing run against REAL Blotato (`dryrun` default; `accounts.json` = `@TBD`); the INTEGRATION CHECKPOINTs (Blotato presign/metrics/reconcile shapes, MCP tool name) remain UNVERIFIED against a live call. **No "Block LIVE" defect and no open audit-HIGH remain** (the three Phase C HIGHs are now closed).

**Open items, in priority order:**
1. **Merge PR #2** (`https://github.com/Fleezyflo/fanops/pull/2`, Phase C) into `main` once audited — then the §State Branch/HEAD lines revert to `main`/the merge commit, and `git worktree remove /Users/molhamhomsi/fanops-phase-c` + delete the branch. (Human-owned.)
2. **Phase D — Live Blotato seam (next phase; mostly codeable now, ONE `[GATED]` step).** D1 client-side idempotency token persisted before the POST (fixes H1 root: stranded posts get a `submission_id` so reconcile can poll them); D MCP-poster auth handling (raise `BlotatoAuthError` so the type-based halt fires); **D5 is `[GATED]`** — verifying the actual Blotato 2xx field name needs sandbox creds (`BLOTATO_API_KEY`+`BLOTATO_SMOKE_ACCOUNT_ID`) the operator does NOT yet have. The plan has literal TDD steps for D1 (`crosspost.py`/`run.py`/`reconcile.py`); stop + surface at D5.
3. **LIVE prerequisite, OPERATOR-GATED (needs creds):** run `tests/integration/test_blotato_smoke.py` against the Blotato sandbox — resolves D5 + the whole "unverified contract" risk.
4. **`claude -p` live verification (operator-gated):** in an `ANTHROPIC_API_KEY`-set environment, drive one real gate end-to-end and confirm a `moments__*.response.json` with real picks (plan A4 scratch recipe).
5. **Phases E/F** of the live+autonomous plan: E (learning-loop-in-cron + dead-man's-switch + responder backoff), F (operator-recovery verbs). **Bounded-retry note → E:** `fanops run` retries a persistently-failing gate up to `for _ in range(10)`/invocation with no backoff — a "stop retrying a deterministically-rejected gate" policy is a natural E refinement. **Also E/F:** a startup preflight warning when `claude` is present but `ANTHROPIC_API_KEY` is unset (the silent-zero-output trap).
6. **New feature (audit-C2 adjudication):** per-account creative variation for A/B content learning. Own spec → plan → build cycle.
7. **Pre-existing backlog** (`RUNTIME.md §Backlog`): REST backoff jitter; externalize brand-risk lists + lift weights; per-platform max-duration clamp; `fanops unhold`; `_parse`/`BASE_URL` triplication consolidation; per-source ranking; media size cap; **C2 caption language *detection*** (the None-residual true-close). (Cosmetic: bump CI action majors off Node-20.)

**Pick up here:**
- **First: merge PR #2** (item 1) if auditing it now — the worktree at `/Users/molhamhomsi/fanops-phase-c` stays alive for any PR-feedback iteration. After merge, `git worktree remove` it + delete the branch. (The two untracked `.phase-c-*.js` workflow scripts in that worktree are my scratch orchestration, not project code — discard with the worktree.)
- **Then, recommended next: Phase D** of `docs/superpowers/plans/2026-06-01-fanops-live-autonomous.md` (live Blotato seam). Do D1–D4 (codeable now, same Workflow-enforced loop), then **STOP at D5 and surface the `[GATED]` Blotato-sandbox-creds need** — do not guess the 2xx field name.
- **`claude -p` live answer is operator-gated:** needs `ANTHROPIC_API_KEY` exported in the deploy env (`--bare` ignores OAuth/keychain). Wiring complete + real-CLI-degradation-verified (Phase A/B).
- **LIVE Blotato path is operator-gated:** sandbox smoke test (item 3, needs creds) + the 3 human-only steps (create accounts → connect in Blotato → paste account_ids + set active; `RUNTIME.md` "three human-only steps").
- Standing: branch `phase-c-validation` pushed to private `origin`, in sync; PR #2 open. **Do not re-raise audit-C2 (byte-identical media) from a future audit** — adjudicated, not missed. When verifying through the real CLI, **`cd` to the project root (or scratch root) first** (`Config()` resolves the ledger from cwd; no `FANOPS_ROOT`) — and a non-held clip correctly ADVANCES past `captioned` to a `queued` post (don't assert it stops at `captioned`). When a real-CLI check claims to test an absent/unauthed tool, make sure it's *actually* absent/unauthed (the venv bin carries `whisper`/`yt-dlp`; `claude` is present-but-unauthed here). A fresh worktree needs its OWN editable venv (`pip install -e ".[dev]"`) — symlinking the parent `.venv` tests the WRONG src tree. macOS has no `timeout` binary — use `pytest-timeout`'s `--timeout=N` for lock/deadlock tests. Audit workflow at `.claude/workflows/fanops-deep-audit.js` (read-only its agents before re-running).
