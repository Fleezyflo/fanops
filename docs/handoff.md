# HANDOFF — MOH FLOW FAN OPS

This file is the cross-session source of truth. It is **rewritten each session** (the §Now block is point-in-time, not an append-only log). Frozen provenance lives in `docs/archive/handoff-history.md` (created on first archive). The build plan is `docs/superpowers/plans/2026-05-31-fanops-real-build-v2.md`. Deviations from that plan made during the build are recorded in the auto-memory file `fanops-build-deviations.md`.

## §State (re-verify the Check commands before quoting)

- **Branch / canonical line:** working on **`feat-studio-ui`** @ **`9d59506`** (**PR #17 OPEN + CI-GREEN** — FanOps Studio, 16 commits ahead). The CANONICAL MERGED line is still **`main`** @ **`16813d4`** (+ rev-26 handoff `9431c57`) — **Studio is NOT yet merged.** On `main`: Phases A–F + G finale + tail + subtitles (PR #8) + creative variation v1 (PR #9) + content discovery (PR #12) + v2 A/B loop (PR #14) + all three v2 follow-ups (amplify `143deea`, transfer #15 `323ba86`, UCB #16 `16813d4`). Check: `git branch --show-current` (expect `feat-studio-ui` until #17 merges) / `gh pr list --repo Fleezyflo/fanops --state open` (expect #17).
- **v3-UCB bandit (on main):** `variant_learning.ucb_rank` is a deterministic UCB1 allocator (`score = mean_lift + c·sqrt(ln N / n)`, no RNG/hash/wall-clock); `FANOPS_VARIANT_UCB` (default OFF) selects it for the OWN-surface caption bias instead of v2 gated-greedy `best_hooks`, `FANOPS_VARIANT_UCB_C` (default √2; 0=greedy, neg/bad→default) tunes exploration. INDEPENDENT of `FANOPS_VARIANT_LEARNING`. **Amplify floor UNCHANGED** — `variant_amplify` keeps `best_hooks` as its C1 floor and NEVER calls `ucb_rank` (extended `_FORBIDDEN_IN_AMPLIFY` + caller-lock + UCB-on==off invariant, all mutation-proven). Check: `grep -n "def ucb_rank" src/fanops/variant_learning.py`.
- **FanOps Studio (PR #17, branch only — NOT yet on main):** a local Flask+Jinja+HTMX content cockpit (`fanops studio` → `127.0.0.1:8787`; Review/Schedule/Lift) in new pkg `src/fanops/studio/` (`views`/`actions`/`app` + templates + vendored htmx 2.0.3). Reads are lock-free; the 2 writes (reschedule/edit-caption + the snooze helper) go through ONE `Ledger.transaction` each with in-lock `queued`+not-imminent guards (TOCTOU-safe vs cron). One core change: `FANOPS_PUBLISH_LEAD_MINUTES` (config.py, default 0 → byte-identical; non-int/neg→0) adds a CONSTANT `lead_minutes` offset in `crosspost.surface_time`. Optional extra `[studio]=flask>=3.0`; the CLI import is LAZY (core stays flask-free, guarded by a test). Check: `grep -n "def create_app" src/fanops/studio/app.py && grep -n "publish_lead_minutes" src/fanops/config.py`.
- **Working tree:** clean (only an untracked, unrelated `-filters` artifact). Check: `git status -sb` (expect `## feat-studio-ui...origin/feat-studio-ui`, in sync).
- **HEAD (of `feat-studio-ui`):** `9d59506` (the studio CI fix) → this rev-27 handoff commit lands on top. `main`'s HEAD is unchanged at `9431c57`. Check: `git log --oneline -1`.
- **Remote:** `origin` → `github.com/Fleezyflo/fanops` (**PRIVATE**). PRs #1–#9, #11–#16 MERGED; #10 CLOSED (dup of #9); **#17 OPEN (FanOps Studio) + CI-GREEN, awaiting operator merge.** Check: `gh pr list --repo Fleezyflo/fanops --state open` / `gh pr checks 17 --repo Fleezyflo/fanops`.
- **Responder auth (IMPORTANT):** the autonomous LLM responder uses the operator's **EXISTING `claude` subscription** via plain `claude -p` (NOT `--bare`) — it needs `claude login` on the host, **NOT `ANTHROPIC_API_KEY`**. Check: `grep -n "strict-mcp-config" src/fanops/llm.py`.
- **Unit/full tests:** on `feat-studio-ui`@`9d59506`: **502 unit passed, 15 deselected** (`-m "not integration"`) / **516 passed, 1 skipped** (full, incl. integration) — verified this session. The +~46 over main's 470 are the studio tests (config/crosspost lead, views, actions, app, integration). The 1 skip = creds-gated Blotato smoke. Check: `source .venv/bin/activate && python -m pytest -q` (516/1) and `... -m "not integration"` (502). A fresh venv needs `pip install -e ".[dev,studio]"` (python3.12) — **`,studio` is REQUIRED for the flask route tests to RUN** (they `importorskip("flask")` and skip cleanly otherwise). RECURRING FALSE ALARM: a subagent running bare `pytest` (no venv) reports "pytest-mock not installed"; ALWAYS use `.venv/bin/python -m pytest`.
- **ffmpeg (toolchain):** the system `ffmpeg` is **`ffmpeg-full`** (8.1.1, `brew link --overwrite --force`'d) — it has the text filters (`drawtext`/`subtitles`/`ass`, libass/freetype) the burned-in subtitles + creative-variation hooks need; the Studio's inline `<video>` relies on the H.264/AAC/+faststart codec invariant (integration-test-pinned). The default formula is STRIPPED; if relinked, subtitles/hooks FAIL OPEN (plain clip + warning). Check: `ffmpeg -hide_banner -h filter=drawtext | head -1` (must NOT say "Unknown filter"). CI's Linux runner has full ffmpeg via apt.
- **Lint:** `ruff check src/` is GREEN (`[tool.ruff.lint] select=["E","F"]`, 4 style E-rules ignored). Check: `source .venv/bin/activate && ruff check src/` (expect "All checks passed!"). A fresh venv needs `pip install -e ".[dev]"` first.
- **Integration tests:** on `feat-studio-ui`: **14 passed, 1 skipped** (live Blotato smoke skips without creds). Adds `test_studio_real.py` (real ffmpeg render → Studio serves the bytes → ffprobe confirms H.264+AAC, the codec invariant) alongside `test_discover_real.py`, `test_variant_*_real.py`. Lock/deadlock tests run under `pytest-timeout`. Check: `source .venv/bin/activate && python -m pytest tests/integration -q`.
- **Module/test parity:** on `feat-studio-ui`: **45 src / 54 tests** (studio added `studio/{__init__,views,actions,app}.py` = +4 src; `test_studio_{views,actions,app}.py` + `integration/test_studio_real.py` = +4 tests). Check: `ls src/fanops/*.py src/fanops/post/*.py src/fanops/studio/*.py | wc -l && ls tests/*.py tests/integration/*.py | wc -l`.
- **Real tooling present:** ffmpeg 8.1.1 (the **ffmpeg-full** build), ffprobe, whisper CLI, `say` (macOS TTS) on PATH; whisper `tiny.pt` cached. Check: `for b in ffmpeg ffprobe whisper say; do command -v $b; done`.
- **Posting backend default:** `dryrun` (writes payload JSON to `05_scheduled/`, posts nothing). Switch via `FANOPS_POSTER=rest|mcp` + `BLOTATO_API_KEY`. Check: `grep -n "FANOPS_POSTER" src/fanops/config.py`.
- **CI:** `.github/workflows/ci.yml` — `unit` job (now installs `.[dev,studio]`) + `real-tooling E2E` job (now `.[dev,transcribe,studio]`), both run on push + PR into `main`. **PR #17 CI: BOTH GREEN** — `unit` 25s + `real-tooling E2E` 3m4s (run `27048840767`, verified this session). NOTE (rev-27 fix): the FIRST PR push went RED because the studio route/integration tests import flask but CI installed only `.[dev]`; fixed by adding `,studio` to both jobs + `pytest.importorskip("flask")` in the two flask-dependent test files. Check: `gh pr checks 17 --repo Fleezyflo/fanops`. (Cosmetic backlog: bump deprecated Node-20 actions.)
- **Toolchain-absent behavior:** every `subprocess.run` of a media binary is guarded — pre-Source/one-shot sites (ffprobe at ingest, yt-dlp at `pull`) raise typed `errors.ToolchainMissingError` → `cli.main` exit 2; pipeline-quarantined sites (ffmpeg render, whisper, ffmpeg signals) record a retriable per-unit `error`. Check: `grep -n "ToolchainMissingError" src/fanops/errors.py`.
- **Concurrency (Phase B + follow-up):** `advance()` AND every standalone write command (`track`, `reconcile`, `adjust`, `ingest`, `pull`) — AND the Studio's `reschedule_post`/`edit_caption`/`snooze_clip` — run the whole load→mutate→save inside ONE `Ledger.transaction(cfg)` holding the `fcntl.flock` (acquired BEFORE load, single atomic save on clean exit, audit B4). A 2nd live run is excluded with a typed `LockBusyError`. Slow I/O stays OUTSIDE the lock. Check: `grep -n "def transaction\|_save_unlocked" src/fanops/ledger.py && grep -ln "Ledger.transaction" src/fanops/cli.py src/fanops/studio/actions.py`.

## 1. What this is

An autonomous fan-account engine for Moh Flow (bilingual EN/AR rapper). It ingests his videos, decides which moments are worth posting (transcript + audio/scene signals → an agent decision with a recorded reason), cuts platform-ready clips with agent-written EN/AR captions, cross-posts to every fan account × platform via Blotato (staggered for opsec, with a subtle non-synchronized artist @mention), then pulls real performance back to make more of what works. **FanOps Studio** (PR #17) adds a local web cockpit for the operator's editorial job: watch the rendered clips, tweak captions, reschedule/snooze the upcoming queue, read per-variant lift — without touching the autonomous machinery. See `README.md` (front door), `MohFlow-FanOps/00_control/RUNTIME.md` (operating loop + production seams), `MohFlow-FanOps/00_control/RISK.md` (operator risk-acceptance for the multi-account model), `MohFlow-FanOps/00_control/context.md` (functional — injected as agent guidance).

## 2. Architecture

- **Unit chain:** `Source → Moment → Clip → Post`, over one git-versioned JSON ledger (`00_control/ledger.json`, atomic temp+os.replace write under a file lock).
- **Identity:** content-addressed SHA-based ids everywhere (`ids.py`: `make_id`/`child_id`/`surface_key`) — NEVER builtin `hash()` (the #1 v1 bug; cross-process stability is proven by a subprocess idempotency test).
- **Agent gates:** generative steps (`decide_moments`, `write_captions`) cross a file contract in `04_agent_io/requests/` — code writes a `*.request.json` (stamped with a `request_id`), the agent/responder writes a validated `*.response.json` echoing that id, code resumes. Stale responses can never be applied (request_id correlation).
- **Two responders** (`responder.py`): `ManualResponder` (no-op; a human/cron writes responses) vs `LlmResponder` (`FANOPS_RESPONDER=llm`; **autonomous via `claude -p`** — default model shells the Claude Code CLI through `llm.claude_json` with committed `prompts.py` templates + the gate's pydantic JSON schema, validates against `MomentDecision`/`CaptionSet`, **per-request quarantine** so one bad/failed gate logs + stays pending without halting the rest). Needs `claude` on PATH (authenticated); model is injectable for tests.
- **Three posters** (`post/`): `dryrun` (default), `rest` (`BlotatoRestPoster`, retry/backoff/typed-errors), `mcp` (`BlotatoMcpPoster`, injected `tool_caller`).
- **Pipeline** (`pipeline.py` `advance()`): per-unit error quarantine (one bad source/moment/clip → `error` state, never wedges the pass). **CLI** (`cli.py`): `status/ingest/pull/advance/respond/track/adjust/gc/run/digest` + recovery verbs + `discover`/`intake` + **`studio`** (lazy-imported web UI).
- **Studio** (`studio/`, PR #17): `views.py` pure read-models (Flask-free) → `actions.py` lock-safe mutations → `app.py` Flask factory (tab routes + variant-aware `/media/<post_id>` + `/clips/<clip_id>` + POST mutation routes). Templates + vendored HTMX; no JS build step.
- **State machines** (`models.py`): separate `SourceState`/`MomentState`/`ClipState`/`PostState`. `failed`≠`analyzed`; `held` (brand-risk) and `retired` are first-class.

## 3. Gotchas / non-obvious invariants

- **`--base-time` is the SCHEDULE ANCHOR, not the publish cutoff.** crosspost staggers posts to times AFTER base-time; `publish_due(now=None)` publishes whatever is due as of REAL wall-clock now. To publish in the same pass (backfill), pass a `--base-time` in the past. **Studio's editable window** therefore exists only when `FANOPS_PUBLISH_LEAD_MINUTES>0` AND cron runs with a now/future base-time; otherwise the window is empty by design and Review/Schedule show read-only history.
- **Conservative retirement:** `adjust` amplifies top `--winner-pct` (0.3) but retires only the bottom `--retire-pct` (0.2) AND below `--lift-floor` (20.0). Retiring a clip also retires its moment.
- **Three surfaced-but-auto-unrecovered stuck states** (all in the digest + counts, no silent loss): `submitting` (mid-publish crash); **`needs_reconcile`** (ambiguous 5xx/timeout — NOT re-POSTed, may be live); a `published` post Blotato never returned metrics for. `needs_reconcile` only arises under `rest`/`mcp`, never `dryrun`.
- **`dryrun` posts are trackable** (audit C4): `submission_id = f"dryrun_<post_id>"`, so the `publish → track → analyzed → adjust` loop is exercisable offline.
- **Studio naive-time trap:** `timeutil.iso_z` treats a NAIVE datetime as LOCAL time; `reschedule_post` therefore coerces naive→UTC (`replace(tzinfo=utc)`) BEFORE `iso_z`, never relying on `iso_z` to normalize (a naive scheduled_time would otherwise mark the post `failed` in `publish_due`).
- **Whisper model:** default `turbo`; falls back to the best cached `~/.cache/whisper/*.pt` when the requested model can't be downloaded. In THIS env only `tiny` is cached.

## 4. Health checks

Run the §State Check commands. The integration E2E (`tests/integration/test_e2e_real.py`) is the golden path: real `say` TTS → real whisper transcript → real ffmpeg 1080×1920 vertical clip → 2 dryrun posts published; it pins `tiny` in-test and skips cleanly without a cached checkpoint. The Studio integration test (`test_studio_real.py`) is the second golden path: real ffmpeg render → Studio serves the bytes → ffprobe confirms H.264+AAC.

## 5. Now (rewritten each session)

**As of 2026-06-06 (rev 27 — FanOps Studio (local content-cockpit web UI) BUILT; PR #17 OPEN + fully CI-GREEN, awaiting operator merge to `main`).**

**Most recent shipped:** **FanOps Studio — a local single-operator web cockpit over the ledger — built end-to-end on `feat-studio-ui`; PR #17 OPEN and CI-GREEN (both jobs: `unit` 25s ✓, `real-tooling E2E` 3m4s ✓, run `27048840767`).** `pip install -e '.[studio]' && fanops studio` serves `http://127.0.0.1:8787` with three tabs — **Review · Schedule · Lift** (verified live this session: `/`→302, all tabs→200). 16 commits ahead of `main`: spec (2) + plan (1) + studio 1–13 + a CI fix. Suite **502 unit / 516 full, 1 skipped** (the creds-gated Blotato smoke); ruff clean. Built spec→plan→execute via multi-agent workflows: an Understand pass verified every spec claim against live code (caught 3 real corrections pre-code: the negative-guard, `surface_time` threading, the `iso_z`-naive-as-local trap), a 5-lens adversarial plan review (all green), then 13 strict-TDD tasks each with an INDEPENDENT verifier + adversarial mutation proofs on the critical invariants. Spec+plan: `docs/superpowers/{specs/2026-06-06-fanops-studio-design.md, plans/2026-06-06-fanops-studio.md}`.

**What the Studio is (and the ONE core-code change):** A curator's cockpit, NOT an ops console — it edits the *upcoming* queue and never becomes a publish gate (auto-publish stays). It mutates the ledger ONLY through `Ledger.transaction` (in-lock existence + `queued` + not-imminent guards). The only pipeline change is **`FANOPS_PUBLISH_LEAD_MINUTES`** (config.py, default 0 → byte-identical to today; non-int/negative→0): a CONSTANT offset added in `crosspost.surface_time` (new keyword `lead_minutes`, the 7 existing call sites unchanged) that creates the editable window without breaking determinism/monotonicity. New package `src/fanops/studio/`: `views.py` (read-models, Flask-free), `actions.py` (lock-safe reschedule/edit-caption/snooze; naive→UTC-Z coercion), `app.py` (Flask factory; variant-aware `/media/<post_id>` → `media_urls[0]` else base clip, 404 not 500), Jinja templates + vendored HTMX 2.0.3. `fanops studio [--host --port]` CLI with **LAZY** flask import (core no-[studio] install stays flask-free — guarded by a test). `[studio]` optional extra (`flask>=3.0`).

**The CI lesson (rev 27, durable):** the studio route + integration tests import flask, but CI's `unit` job installed only `.[dev]` → the FIRST PR-push CI run went RED (`unit` fail), while local was green (flask lives in `.venv`). Fixed both ways: `pytest.importorskip("flask")` in the two flask-dependent test files (skip cleanly on a core install, mirroring the integration toolchain-skip), AND CI now installs `.[dev,studio]` / `.[dev,transcribe,studio]` so they actually RUN. **LESSON: a feature behind an optional extra must (a) skip-guard its extra-dependent tests AND (b) get the extra into CI — local-green ≠ CI-green when the dep lives only in `.venv`. ALWAYS `gh pr checks` after the first push, before declaring done.**

**What works right now:**
- **On `main` (unchanged, `16813d4` + rev-26 handoff `9431c57`):** the entire live-autonomous plan + tail + subtitles + creative variation v1/v2 + all three v2 follow-ups (amplify `143deea`, transfer #15, UCB #16). Studio is NOT yet on main.
- **On PR #17 branch `feat-studio-ui` (`9d59506`):** FanOps Studio. 502 unit / 516 full, 1 skipped. CI both jobs GREEN.
- **PRIME DIRECTIVE holds:** the Studio never marks a post `failed`, never fires publish, never touches the C1 amplify/delete path. Reads are lock-free; the two writes are guarded inside one transaction each.

**Live state caveats:**
- **Studio is built + CI-green but NOT merged** — PR #17 awaits the operator's merge decision (you chose Push+PR this session). Nothing on `main` changed.
- **The editable window exists only by convention** (spec §4): default `FANOPS_PUBLISH_LEAD_MINUTES=0` + the repo's past default `--base-time` → posts publish same-pass → empty editable window (Review/Schedule then show read-only history only). Need `lead>0` AND a now/future base-time for a real window.
- **Going LIVE is still PURELY OPERATOR-GATED (Blotato)** — unchanged; the Studio doesn't alter this. Pre-live, the Lift tab is empty by design (no analyzed posts under the dryrun backend) and shows reason-bearing empty states.
- **ffmpeg is `ffmpeg-full` (local fix)** — the Studio's inline `<video>` relies on the H.264/AAC/+faststart codec invariant (integration-test-pinned). CI's Linux runner has full ffmpeg.

**Open items, in priority order:**
1. **Merge PR #17 to `main`** when ready (it's green). After merge, re-point §State Branch/HEAD/PRs lines back to `main`.
2. **Operator-credential-gated (Blotato) — THE highest-value path** (unchanged): a Blotato key → live-contract smoke (read-only) → cutover (G3 runbook in `RUNTIME.md`). Unlocks real metrics, which is what makes the Studio's Lift tab AND all creative-variation machinery (v1/v2/v3) actually populate. NEVER autonomously fire `blotato_create_post`.
3. **Studio fast follow-ons (deferred in v1, see spec §13/§17):** held-clip *release* + caption editor (v1 is review-only because release-without-edit re-holds via the brand-risk regex); fold hashtags into the Blotato `text` (currently a dead field, shown read-only); drag-and-drop reorder; the Phase-2 `/dev` portal (status/recovery surface). Low-value backlog unchanged (trending-audio, daypart scheduling, CI Node-20 bump).

**Pick up here:**
- **PR #17 (`feat-studio-ui` @ `9d59506`) is OPEN and fully GREEN.** Verify: `gh pr checks 17 --repo Fleezyflo/fanops` (both pass) and `gh pr list --repo Fleezyflo/fanops --state open` (expect #17). The branch is 16 commits ahead of `main`; `main` is untouched at `16813d4`/`9431c57`.
- **To merge:** `gh pr merge 17 --repo Fleezyflo/fanops --squash` (or per operator preference) → `git checkout main && git pull` → re-run §State checks → rewrite §State Branch/PRs/HEAD to `main`.
- **To run the Studio locally:** `.venv/bin/fanops studio` (or `pip install -e '.[studio]'` in a fresh venv first) → `http://127.0.0.1:8787`. For a non-empty editable window: `FANOPS_PUBLISH_LEAD_MINUTES=120` and run `advance`/`run` with a now/future `--base-time`.
- **When the operator gets a Blotato key:** re-probe the MCP (`blotato_get_user`, read-only); if real data returns, run the live-contract verification (fix any field mismatch TDD). NEVER autonomously fire `blotato_create_post`.
- **Standing process gotchas (durable multi-agent wisdom):** (a) a FRESH worktree needs its OWN editable venv (`pip install -e '.[dev]'`, **python3.12 not 3.14**); a feature built in the MAIN tree reuses the existing `.venv`. (b) Verifying via the real CLI / scratch render: **`cd` to a scratch root first** (`Config()` resolves from cwd). (c) **ALWAYS independently re-run the committed artifact + read the code before trusting a green** (this build used a separate verifier per task + mutation proofs). (d) **a feature behind an optional extra must skip-guard its tests AND add the extra to CI** (rev-27) — and **check `gh pr checks` after the first push.** (e) `git diff <branch> main` before deleting any merged branch. (f) **VERIFY THE TOOLCHAIN before building a feature that depends on it.** (g) an adversarial/skeptic `holds=false` is a lead to investigate against real code, not an auto-verdict. (h) never do branch surgery while another actor mutates the repo.
- Standing invariants: **Do not re-raise audit-C2 (byte-identical media).** **The C1 amplify/delete path stays blind to variant signal AND the UCB bandit.** **Do NOT autonomously fire `blotato_create_post`.** **A fresh/local venv must `pip install -e '.[dev]'`** (python3.12; add `,studio` to run the Studio UI tests).
