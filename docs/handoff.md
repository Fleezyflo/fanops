# HANDOFF — MOH FLOW FAN OPS

This file is the cross-session source of truth. It is **rewritten each session** (the §Now block is point-in-time, not an append-only log). Frozen provenance lives in `docs/archive/handoff-history.md` (created on first archive). The build plan is `docs/superpowers/plans/2026-05-31-fanops-real-build-v2.md`. Deviations from that plan made during the build are recorded in the auto-memory file `fanops-build-deviations.md`.

## §State (re-verify the Check commands before quoting)

- **Branch:** `phase-b-concurrency` (Phase B work; PR #1 open into `main`, NOT yet merged — the human owns the merge). `main` itself is still at `f0af461`. Check: `git branch --show-current`.
- **Working tree:** clean. Check: `git status -sb`.
- **HEAD (of `phase-b-concurrency`):** `7eca2b9` (the CI test fix; Phase B commits below). Check: `git log --oneline -1`.
- **Remote:** `origin` → `github.com/Fleezyflo/fanops` (**PRIVATE**). Branch `phase-b-concurrency` pushed + tracking; PR #1 (`https://github.com/Fleezyflo/fanops/pull/1`) open into `main`. Check: `git remote -v && gh pr view 1 --repo Fleezyflo/fanops --json state,mergeable`.
- **Unit tests:** 243 passed, 5 deselected. Check: `source .venv/bin/activate && python -m pytest -q -m "not integration"`. (Phase B added +11 over Phase A's 232: 3 B1+B2 lock/spy/deadlock, 1 M2-crosspost, 2 publish schedule-parse, 2 publish stage-guard, 2 B3/M1, 1 A3 TOCTOU.) **Toolchain-free**: the full unit suite passes with `PATH` stripped of ffmpeg/ffprobe/whisper (the CI `unit`-job condition) — verified this session.
- **Integration tests:** 4 passed, 1 skipped (live Blotato smoke skips without creds). Check: `source .venv/bin/activate && python -m pytest -q -m integration`. Full suite (both markers): `247 passed, 1 skipped`. Lock/deadlock tests run under `pytest-timeout` (`--timeout=20`); a hang is a hard FAIL, not a session hang. (`pytest-timeout` is installed in the worktree venv as the enforcement instrument — NOT a shipped dep.)
- **Module/test parity:** 34 src modules (Phase A added `llm.py` + `prompts.py`), 37 test files (Phase A added `test_llm.py` + `test_prompts.py`). The integration *marker* selects 5 tests: the 3 in `tests/integration/` (`test_e2e_real.py` ×2 — the transcript-drives-moment golden path + `test_blotato_smoke.py` creds-skip + `test_publish_track_loop.py`) PLUS `tests/test_adjust.py::test_amplify_then_ingest_then_render_produces_new_clip` (marked `integration` because it shells real ffmpeg — CI-1) — i.e. `-m integration --collect-only` = 5. Check: `ls src/fanops/*.py src/fanops/post/*.py | wc -l && ls tests/*.py tests/integration/*.py | wc -l` (34 / 37) and `python -m pytest -q -m integration --collect-only | grep -c "::"` (5).
- **Real tooling present:** ffmpeg 8.0.1, ffprobe, whisper CLI, `say` (macOS TTS) all on PATH; whisper `tiny.pt` cached (model downloads blocked by proxy in this env). The E2E pins `tiny` itself, so no env var is required; to override the model for a real `fanops` run, set `FANOPS_WHISPER_MODEL`. Check: `for b in ffmpeg ffprobe whisper say; do command -v $b; done`.
- **Posting backend default:** `dryrun` (writes payload JSON to `05_scheduled/`, posts nothing). Switch via `FANOPS_POSTER=rest|mcp` + `BLOTATO_API_KEY`. Check: `grep -n "FANOPS_POSTER" src/fanops/config.py`.
- **CI:** `.github/workflows/ci.yml` — `unit` job (no toolchain) + `e2e` job (installs toolchain, `FANOPS_REQUIRE_E2E=1`, audit H10). Runs on push to `main` AND on `pull_request` into `main`. **PR #1 CI:** the FIRST run on the branch FAILED the `unit` job (the 2 M2 pipeline tests shelled real ffprobe via `ingest_drops` — the CI-1 trap; fixed in `7eca2b9` by mocking `ingest_drops`); the post-fix run `26770252579` = **`completed success`** (both jobs: `unit` ✓ 25s, `real-tooling E2E` ✓). Check: `gh run list --repo Fleezyflo/fanops --branch phase-b-concurrency --limit 3`. (Non-fatal annotation: "Node.js 20 actions are deprecated" — cosmetic.)
- **Toolchain-absent behavior:** every `subprocess.run` of a media binary (ffmpeg/ffprobe/whisper/yt-dlp) is guarded — `check=False` does NOT catch a pre-launch `FileNotFoundError`, so absent binaries used to crash. Now: pre-Source/one-shot sites (ffprobe at ingest, yt-dlp at `pull`) raise typed `errors.ToolchainMissingError` → `cli.main` exit 2 + "install …", no traceback; pipeline-quarantined sites (ffmpeg render, whisper transcribe, ffmpeg signals) record a retriable per-unit `error`. Check: `grep -rn "subprocess.run" src/fanops/ | grep -v "_run_ffprobe\|_run_ffmpeg\|try:"` (expect only the lines INSIDE the guards) and `grep -n "ToolchainMissingError" src/fanops/errors.py`.
- **Concurrency (Phase B):** `advance()` runs the WHOLE load→mutate→save inside ONE `Ledger.transaction(cfg)` holding the `fcntl.flock` — acquired BEFORE load, single atomic save on clean exit (audit B4). A 2nd live run is excluded with a typed `LockBusyError`. `publish_due(in_transaction=True)` uses the unlocked save (no self-deadlock); crosspost+publish are wrapped (log+continue) so a non-auth raise can't roll back the pass, while a fatal `BlotatoAuthError` still escapes (F52). The slow `claude -p` responder runs OUTSIDE the lock (`request_id` correlation + capture-and-recheck against a mid-call re-seed, audit A3). Check: `grep -n "def transaction\|_save_unlocked" src/fanops/ledger.py && grep -n "rid_before\|rid_after" src/fanops/responder.py`.

## 1. What this is

An autonomous fan-account engine for Moh Flow (bilingual EN/AR rapper). It ingests his videos, decides which moments are worth posting (transcript + audio/scene signals → an agent decision with a recorded reason), cuts platform-ready clips with agent-written EN/AR captions, cross-posts to every fan account × platform via Blotato (staggered for opsec, with a subtle non-synchronized artist @mention), then pulls real performance back to make more of what works. See `README.md` (front door), `MohFlow-FanOps/00_control/RUNTIME.md` (operating loop + production seams), `MohFlow-FanOps/00_control/RISK.md` (recorded operator risk-acceptance for the multi-account model), `MohFlow-FanOps/00_control/context.md` (functional — injected as agent guidance).

## 2. Architecture

- **Unit chain:** `Source → Moment → Clip → Post`, over one git-versioned JSON ledger (`00_control/ledger.json`, atomic temp+os.replace write under a file lock).
- **Identity:** content-addressed SHA-based ids everywhere (`ids.py`: `make_id`/`child_id`/`surface_key`) — NEVER builtin `hash()` (the #1 v1 bug; cross-process stability is proven by a subprocess idempotency test).
- **Agent gates:** generative steps (`decide_moments`, `write_captions`) cross a file contract in `04_agent_io/requests/` — code writes a `*.request.json` (stamped with a `request_id`), the agent/responder writes a validated `*.response.json` echoing that id, code resumes. Stale responses can never be applied (request_id correlation).
- **Two responders** (`responder.py`): `ManualResponder` (no-op; a human/cron writes responses) vs `LlmResponder` (`FANOPS_RESPONDER=llm`; **autonomous via `claude -p`** — default model shells the Claude Code CLI through `llm.claude_json` with committed `prompts.py` templates + the gate's pydantic JSON schema, validates against `MomentDecision`/`CaptionSet`, **per-request quarantine** so one bad/failed gate logs + stays pending without halting the rest). Needs `claude` on PATH (authenticated); model is injectable for tests.
- **Three posters** (`post/`): `dryrun` (default), `rest` (`BlotatoRestPoster`, retry/backoff/typed-errors), `mcp` (`BlotatoMcpPoster`, injected `tool_caller`).
- **Pipeline** (`pipeline.py` `advance()`): per-unit error quarantine (one bad source/moment/clip → `error` state, never wedges the pass). **CLI** (`cli.py`): `status/ingest/pull/advance/respond/track/adjust/gc/run/digest`.
- **State machines** (`models.py`): separate `SourceState`/`MomentState`/`ClipState`/`PostState`. `failed`≠`analyzed`; `held` (brand-risk) and `retired` are first-class.

## 3. Gotchas / non-obvious invariants

- **`--base-time` is the SCHEDULE ANCHOR, not the publish cutoff.** crosspost staggers posts to times AFTER base-time; `publish_due(now=None)` publishes whatever is due as of REAL wall-clock now. To publish in the same pass (backfill), pass a `--base-time` in the past. (This was a fixed plan contradiction.)
- **Conservative retirement:** `adjust` amplifies top `--winner-pct` (0.3) but retires only the bottom `--retire-pct` (0.2) AND below `--lift-floor` (20.0) — objectively-fine clips above the floor are never retired. Retiring a clip also retires its moment (so it can't be re-rendered).
- **Three surfaced-but-auto-unrecovered stuck states** (all in the digest + counts, no silent loss): a post stranded in `submitting` by a mid-publish crash; a post in **`needs_reconcile`** (REST hit an ambiguous 5xx/timeout after sending the body — it may be live, so it is NOT re-POSTed; Blotato has no idempotency key); a `published` post Blotato never returned metrics for. The first two need a poll step (`GET /v2/posts/:id` → promote/reset) — backlog item (a). `needs_reconcile` only ever arises under the `rest`/`mcp` backends, never `dryrun`.
- **`dryrun` posts are trackable** (audit C4): the dryrun poster stamps `submission_id = f"dryrun_<post_id>"`, so the `publish → track → analyzed → adjust` loop is fully exercisable offline by feeding `track` a metrics row keyed on that id. (CLI `track` still skips in dryrun — no live metrics source — but the seam is proven by `tests/integration/test_publish_track_loop.py`.)
- **Whisper model:** default `turbo`; falls back to the best cached `~/.cache/whisper/*.pt` when the requested model can't be downloaded (offline/proxied). In THIS env only `tiny` is cached.

## 4. Health checks

Run the §State Check commands. The integration E2E (`tests/integration/test_e2e_real.py`) is the golden path: real `say` TTS → real whisper transcript (asserts real whisper segment structure + ≥4 words + a vocoder-robust "anymore" anchor — CI-2) → real ffmpeg 1080×1920 vertical clip → 2 dryrun posts published. It pins the `tiny` whisper model in-test (no env var needed) and skips cleanly on a host with no cached checkpoint, so a fresh checkout / CI runner sees a clear skip rather than a cryptic `assert 0 == 1`.

## 5. Now (rewritten each session)

**As of 2026-06-01 (rev 10).**

**Most recent shipped (this session — Phase B of the live+autonomous plan: concurrency correctness. On branch `phase-b-concurrency`, PR #1 open into `main`, NOT yet merged — the human owns the merge):**
- `f11543c` — **B1+B2 (audit B4 + M2), one atomic commit: the ledger transaction lock.** `Ledger.transaction(cfg, *, timeout)` holds the `fcntl.flock` across the WHOLE load→mutate→save (acquired BEFORE load, single atomic save on clean exit via new `_save_unlocked`); `save()` delegates to it. `advance()`'s whole body moved inside the transaction; trailing `save()` gone; digest reads `Ledger.load(cfg)` OUTSIDE the lock. `publish_due(in_transaction=True)` uses the unlocked save (no self-deadlock). M2 (NOT subsumed — proven): crosspost+publish wrapped (log+continue) so a non-auth raise can't roll back the pass; a fatal `BlotatoAuthError` still escapes (F52); the publish schedule-parse moved INSIDE the per-post try so a malformed on-disk `scheduled_time` is a per-post failure, not an escape (adversarial-review finding, fixed not waved off).
- `a3ff280` — **B3 (audit M1): `reconcile_moments` retire guard.** Skips the upsert when the prior moment is `MomentState.retired` (a fresh `decided` copy was resurrecting retired lineages). Non-retired moments still upsert.
- `ec42cd5` — **A3 answer-stale TOCTOU CLOSED** (the Phase-A "Recorded-for-later" item). `answer_pending` now captures `rid_before` BEFORE the slow `claude -p` call, re-verifies `rid_after` AFTER, drops the answer on mismatch, stamps the captured rid. No lock across the model call. Decision record: `docs/superpowers/specs/2026-06-01-phaseB-toctou-decision.md` (`6cfbf12`).
- `19fefac` — **docs sync** (README + RUNTIME.md): the lock now spans the whole pass; `LockBusyError` semantics; the responder runs outside the lock.
- `7eca2b9` — **CI fix**: the 2 M2 pipeline tests shelled real ffprobe via `ingest_drops` (passed on macOS, failed the Linux no-toolchain `unit` job — the CI-1 trap); now mock `ingest_drops`, so the full unit suite passes with `PATH` stripped.

Enforcement: dedicated worktree, TDD with watched RED→GREEN, every new test mutation-proven. Verification driven through the **Workflow** engine (3 workflows: independent verifier + ≥2 adversarial skeptics each, prompted to REFUTE the concurrency claims). One skeptic finding REFUTED → triaged via `receiving-code-review` → fixed. Tests: unit `243 passed, 5 deselected` (incl. toolchain-stripped), full `247 passed, 1 skipped`. Real-CLI proof: `fanops advance` + `fanops run` exit 0, no deadlock, ledger written once-per-pass atomically; 2-process race clean.

**What works right now:**
- **Overlapping cron runs are safe (audit B4 closed):** the WHOLE `advance()` pass is one `Ledger.transaction` holding the flock load→mutate→save; a 2nd live run is excluded with a typed `LockBusyError`. The lost-update window (a published post vanishing / a `submitting` reverting into a double-post) is gone. Proven by a real 2-process repro (`counts_seen_on_entry {'B':0,'A':1}`).
- **No self-deadlock + M2 hardened:** `publish_due(in_transaction=True)` uses the unlocked save; crosspost+publish are wrapped so a non-auth raise can't roll back the pass; a fatal `BlotatoAuthError` still halts (F52). A malformed on-disk `scheduled_time` is a per-post failure, not a pass-killer.
- **M1 closed:** a `retired` moment is never resurrected by `reconcile_moments`; non-retired re-decision still works.
- **A3 TOCTOU closed:** the responder can't apply a stale-payload answer as fresh when an overlapping run re-seeds a gate mid-`claude -p` (capture-and-recheck rid; window CLOSED, not narrowed — adversarially verified, no surviving interleaving found).
- Everything Phase A and earlier still holds (autonomous `claude -p` responder + per-request quarantine; full dryrun pipeline; H6/H7/H8/H4/H1-3/H9/C4 hardening; toolchain-absent guards). A shared per-clip media URL across accounts is intended (C2) — do NOT "fix" it.

**Live state caveats:**
- **Phase B is on a PR, not merged.** `main` is still `f0af461`; the Phase B code is on `phase-b-concurrency` (HEAD `7eca2b9`) in PR #1. It only reaches `main` when the human merges. **CI run `26770252579` = `completed success`** (both jobs: `unit` ✓ 25s, `real-tooling E2E` ✓ — watched to green with `--exit-status`).
- **`claude -p` still needs `ANTHROPIC_API_KEY`** in the `fanops` environment (`--bare` never reads OAuth/keychain — see the Phase A post-merge note in `fanops-build-deviations.md`). The real-CLI `fanops run` this session hit `"Not logged in"` (the SAME env-auth condition, NOT a Phase B regression) and the responder quarantined it cleanly — confirming the H2/H7 contracts survive Phase B.
- Still nothing run against REAL Blotato (`dryrun` default; `accounts.json` = `@TBD`); the INTEGRATION CHECKPOINTs (Blotato presign/metrics/reconcile shapes, MCP tool name) remain UNVERIFIED against a live call. **No "Block LIVE" defect and no open audit-HIGH remain.**

**Open items, in priority order:**
1. **Merge PR #1** (`https://github.com/Fleezyflo/fanops/pull/1`) into `main` once you've audited it — then the §State Branch/HEAD lines revert to `main`/the merge commit. (Human-owned.)
2. **Phase C — Autonomous-LLM validation hardening (next phase, no creds; plan Phase C).** H4 reject NaN/Inf timestamps in moment picks; H5 hold clips whose caption language ≠ source language; H6 validate caption surface keys against the requested set. The plan has literal TDD steps + tests for C1/C2/C3.
3. **LIVE prerequisite, OPERATOR-GATED (needs creds):** run `tests/integration/test_blotato_smoke.py` against the Blotato sandbox (`BLOTATO_API_KEY`+`BLOTATO_SMOKE_ACCOUNT_ID`).
4. **`claude -p` live verification (operator-gated):** in an `ANTHROPIC_API_KEY`-set environment, drive one real gate end-to-end and confirm a `moments__*.response.json` with real picks (plan A4 scratch recipe).
5. **Phases D/E/F** of the live+autonomous plan: D (live Blotato seam — `[GATED]` smoke verification), E (learning-loop-in-cron + dead-man's-switch + responder backoff), F (operator-recovery verbs). **Bounded-retry note → Phase E:** `fanops run` retries a persistently-failing gate up to `for _ in range(10)`/invocation with no backoff — bounded per-run, but a backoff / "stop retrying a deterministically-rejected gate" policy is a natural E refinement. **Also for E/F:** a startup preflight warning when `claude` is present but `ANTHROPIC_API_KEY` is unset (the silent-zero-output trap).
6. **New feature (C2 adjudication):** per-account creative variation for A/B content learning. Own spec → plan → build cycle.
7. **Pre-existing backlog** (`RUNTIME.md §Backlog`): REST backoff jitter; externalize brand-risk lists + lift weights; per-platform max-duration clamp; `fanops unhold`; `_parse`/`BASE_URL` triplication consolidation; per-source ranking; media size cap. (Cosmetic: bump CI action majors off Node-20.)

**Pick up here:**
- **First: merge PR #1** (item 1) if auditing it now — the worktree at `.worktrees/phase-b-concurrency` stays alive for any PR-feedback iteration. After merge, `git worktree remove` it and delete the branch.
- **Then, recommended next: Phase C** of `docs/superpowers/plans/2026-06-01-fanops-live-autonomous.md` (autonomous-LLM validation hardening — NaN/caption-language/caption-surface). The plan has literal TDD steps (C1 `models.py`/`moments.py`, C2 `caption.py` language hold, C3 `caption.py` surface validation). Same Workflow-enforced loop as Phase B.
- **`claude -p` live answer is operator-gated:** needs `ANTHROPIC_API_KEY` exported in the deploy env (`--bare` ignores OAuth/keychain). Wiring is complete + real-CLI-degradation-verified.
- **LIVE Blotato path is operator-gated:** sandbox smoke test (item 3, needs creds) + the 3 human-only steps (create accounts → connect in Blotato → paste account_ids + set active; `RUNTIME.md` "three human-only steps").
- Standing: branch `phase-b-concurrency` pushed to private `origin`, in sync; PR #1 open. **Do not re-raise C2 from a future audit** — adjudicated, not missed. When verifying through the real CLI, **`cd` to the project root (or scratch root) first** (`Config()` resolves the ledger from cwd; no `FANOPS_ROOT`) — and when a real-CLI check claims to test an absent/unauthed tool, make sure the tool is *actually* absent/unauthed (the venv bin carries `whisper`/`yt-dlp`; `claude` is present-but-unauthed here). macOS has no `timeout` binary — use `pytest-timeout`'s `--timeout=N` (installed in the worktree venv) for lock/deadlock tests. Audit workflow at `.claude/workflows/fanops-deep-audit.js` (read-only its agents before re-running).
