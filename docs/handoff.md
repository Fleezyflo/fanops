# HANDOFF — MOH FLOW FAN OPS

This file is the cross-session source of truth. It is **rewritten each session** (the §Now block is point-in-time, not an append-only log). Frozen provenance lives in `docs/archive/handoff-history.md` (created on first archive). The build plan is `docs/superpowers/plans/2026-05-31-fanops-real-build-v2.md`. Deviations from that plan made during the build are recorded in the auto-memory file `fanops-build-deviations.md`.

## §State (re-verify the Check commands before quoting)

- **Branch:** `main` (canonical line). Phases A, B, C, D, E, **and the Phase B follow-up are ALL MERGED into `main`** (PRs #1, #2, #3, #4, #5). **NO open PRs remain.** Only `main` exists (all phase worktrees/branches cleaned up). Check: `git branch --show-current` / `gh pr list --repo Fleezyflo/fanops --state open` (expect EMPTY).
- **Working tree:** clean (`## main...origin/main`). Check: `git status -sb`.
- **HEAD (of `main`):** `18fae9f` (this rev-15 Close handoff sits on top; the prior rev-14 handoff is its parent). Notable recent commits below it: `868a3a0` (Phase E docs-sync: README/RUNTIME for the learning loop + dead-man's-switch + `--bare`/`ANTHROPIC_API_KEY` connection), the Merge PR #3 commit `7e50412` (Phase B follow-up: transaction-wrap all standalone CLI write commands), the Phase E merge `c21f07a`, and the E2/E3/E4-harden mutation-test commits (test-only). ALL phase code (A–E + B-followup) is ON `main`. Check: `git log --oneline -1`.
- **Remote:** `origin` → `github.com/Fleezyflo/fanops` (**PRIVATE**). PR #1 (Phase B) MERGED; PR #2 (Phase C) MERGED; PR #4 (Phase D) MERGED (`dabbce5`); PR #5 (Phase E) MERGED (`c21f07a`); **PR #3 (Phase B follow-up) MERGED 2026-06-02 (`7e50412`), CI green against current base (updated-branch run `26812620349`)**. NO open PRs. Check: `git remote -v && gh pr list --repo Fleezyflo/fanops --state open`.
- **Unit/full tests:** on `main` (post-PR-#3-merge): **291 passed, 1 skipped** (full; +8 over Phase E's 283 — the B-followup `test_cli_transactional.py` + updates). Check: `source .venv/bin/activate && python -m pytest -q`. The 1 skip = creds-gated Blotato smoke. NOTE: PR #3 added **`pytest-timeout`** to dev-deps + a global `timeout=60` in `pyproject` (a hang now FAILS FAST); a fresh/local venv must `pip install -e ".[dev]"` or pytest warns "Unknown config option: timeout" (plugin absent) — done in this env.
- **Integration tests:** 4 passed, 1 skipped (live Blotato smoke skips without creds) — same on `main` and the branch. Lock/deadlock tests run under `pytest-timeout` (global `timeout=60` in `pyproject` + a stricter `--timeout=20` for the lock tests); a hang is a hard FAIL.
- **Module/test parity:** 34 src modules (Phase A added `llm.py` + `prompts.py`), 37 test files (Phase A added `test_llm.py` + `test_prompts.py`). The integration *marker* selects 5 tests: the 3 in `tests/integration/` (`test_e2e_real.py` ×2 — the transcript-drives-moment golden path + `test_blotato_smoke.py` creds-skip + `test_publish_track_loop.py`) PLUS `tests/test_adjust.py::test_amplify_then_ingest_then_render_produces_new_clip` (marked `integration` because it shells real ffmpeg — CI-1) — i.e. `-m integration --collect-only` = 5. Check: `ls src/fanops/*.py src/fanops/post/*.py | wc -l && ls tests/*.py tests/integration/*.py | wc -l` (34 / 37) and `python -m pytest -q -m integration --collect-only | grep -c "::"` (5).
- **Real tooling present:** ffmpeg 8.0.1, ffprobe, whisper CLI, `say` (macOS TTS) all on PATH; whisper `tiny.pt` cached (model downloads blocked by proxy in this env). The E2E pins `tiny` itself, so no env var is required; to override the model for a real `fanops` run, set `FANOPS_WHISPER_MODEL`. Check: `for b in ffmpeg ffprobe whisper say; do command -v $b; done`.
- **Posting backend default:** `dryrun` (writes payload JSON to `05_scheduled/`, posts nothing). Switch via `FANOPS_POSTER=rest|mcp` + `BLOTATO_API_KEY`. Check: `grep -n "FANOPS_POSTER" src/fanops/config.py`.
- **CI:** `.github/workflows/ci.yml` — `unit` job (no toolchain) + `e2e` job (installs toolchain, `FANOPS_REQUIRE_E2E=1`, audit H10). Runs on push to `main` AND on `pull_request` into `main`. **Latest `main` push (which carried the Phase E docs-sync `868a3a0`) CI GREEN: run `26812986398` at `18fae9f` = `completed success`, both jobs incl. real-tooling E2E (2m31s, FANOPS_REQUIRE_E2E so it ran not skipped), watched to completion + re-queried.** (Earlier green: Phase E PR #5 `26811594318`; merge/cherry-pick `26811831877`.) Check: `gh run list --repo Fleezyflo/fanops --branch main --limit 3`. (Non-fatal annotation: "Node.js 20 actions are deprecated" — cosmetic.)
- **Toolchain-absent behavior:** every `subprocess.run` of a media binary (ffmpeg/ffprobe/whisper/yt-dlp) is guarded — `check=False` does NOT catch a pre-launch `FileNotFoundError`, so absent binaries used to crash. Now: pre-Source/one-shot sites (ffprobe at ingest, yt-dlp at `pull`) raise typed `errors.ToolchainMissingError` → `cli.main` exit 2 + "install …", no traceback; pipeline-quarantined sites (ffmpeg render, whisper transcribe, ffmpeg signals) record a retriable per-unit `error`. Check: `grep -rn "subprocess.run" src/fanops/ | grep -v "_run_ffprobe\|_run_ffmpeg\|try:"` (expect only the lines INSIDE the guards) and `grep -n "ToolchainMissingError" src/fanops/errors.py`.
- **Concurrency (Phase B + follow-up):** `advance()` AND **every standalone write command** (`track`, `reconcile`, `adjust`, `ingest`, `pull`) run the WHOLE load→mutate→save inside ONE `Ledger.transaction(cfg)` holding the `fcntl.flock` — acquired BEFORE load, single atomic save on clean exit (audit B4, now everywhere). A 2nd live run is excluded with a typed `LockBusyError`. Slow I/O stays OUTSIDE the lock: `publish_due(in_transaction=True)` uses the unlocked save; `track`'s metrics fetch + `reconcile`'s per-post polls + `pull`'s yt-dlp download all run BEFORE the transaction (only the in-memory apply is locked) — so a 30s network call never serializes behind the ledger lock. crosspost+publish are wrapped (log+continue) so a non-auth raise can't roll back the pass, while a fatal `BlotatoAuthError` still escapes (F52). The slow `claude -p` responder runs OUTSIDE the lock (`request_id` correlation + capture-and-recheck against a mid-call re-seed, audit A3). Check: `grep -n "def transaction\|_save_unlocked" src/fanops/ledger.py && grep -ln "Ledger.transaction" src/fanops/cli.py`.

## 1. What this is

An autonomous fan-account engine for Moh Flow (bilingual EN/AR rapper). It ingests his videos, decides which moments are worth posting (transcript + audio/scene signals → an agent decision with a recorded reason), cuts platform-ready clips with agent-written EN/AR captions, cross-posts to every fan account × platform via Blotato (staggered for opsec, with a subtle non-synchronized artist @mention), then pulls real performance back to make more of what works. See `README.md` (front door), `MohFlow-FanOps/00_control/RUNTIME.md` (operating loop + production seams), `MohFlow-FanOps/00_control/RISK.md` (recorded operator risk-acceptance for the multi-account model), `MohFlow-FanOps/00_control/context.md` (functional — injected as agent guidance).

## 2. Architecture

- **Unit chain:** `Source → Moment → Clip → Post`, over one git-versioned JSON ledger (`00_control/ledger.json`, atomic temp+os.replace write under a file lock).
- **Identity:** content-addressed SHA-based ids everywhere (`ids.py`: `make_id`/`child_id`/`surface_key`) — NEVER builtin `hash()` (the #1 v1 bug; cross-process stability is proven by a subprocess idempotency test).
- **Agent gates:** generative steps (`decide_moments`, `write_captions`) cross a file contract in `04_agent_io/requests/` — code writes a `*.request.json` (stamped with a `request_id`), the agent/responder writes a validated `*.response.json` echoing that id, code resumes. Stale responses can never be applied (request_id correlation).
- **Two responders** (`responder.py`): `ManualResponder` (no-op; a human/cron writes responses) vs `LlmResponder` (`FANOPS_RESPONDER=llm`; **autonomous via `claude -p`** — default model shells the Claude Code CLI through `llm.claude_json` with committed `prompts.py` templates + the gate's pydantic JSON schema, validates against `MomentDecision`/`CaptionSet`, **per-request quarantine** so one bad/failed gate logs + stays pending without halting the rest). Needs `claude` on PATH (authenticated); model is injectable for tests.
- **Three posters** (`post/`): `dryrun` (default), `rest` (`BlotatoRestPoster`, retry/backoff/typed-errors), `mcp` (`BlotatoMcpPoster`, injected `tool_caller`).
- **Pipeline** (`pipeline.py` `advance()`): per-unit error quarantine (one bad source/moment/clip → `error` state, never wedges the pass). **CLI** (`cli.py`): `status/ingest/pull/advance/respond/track/adjust/gc/run/digest`.
- **State machines** (`models.py`): separate `SourceState`/`MomentState`/`ClipState`/`PostState`. `failed`≠`analyzed`; `held` (brand-risk) and `retired` are first-class.

## 3. Gotchas / non-obvious invariants

- **`--base-time` is the SCHEDULE ANCHOR, not the publish cutoff.** crosspost staggers posts to times AFTER base-time; `publish_due(now=None)` publishes whatever is due as of REAL wall-clock now. To publish in the same pass (backfill), pass a `--base-time` in the past. (This was a fixed plan contradiction.)
- **Conservative retirement:** `adjust` amplifies top `--winner-pct` (0.3) but retires only the bottom `--retire-pct` (0.2) AND below `--lift-floor` (20.0) — objectively-fine clips above the floor are never retired. Retiring a clip also retires its moment (so it can't be re-rendered).
- **Three surfaced-but-auto-unrecovered stuck states** (all in the digest + counts, no silent loss): a post stranded in `submitting` by a mid-publish crash; a post in **`needs_reconcile`** (REST hit an ambiguous 5xx/timeout after sending the body — it may be live, so it is NOT re-POSTed; Blotato has no idempotency key); a `published` post Blotato never returned metrics for. The first two need a poll step (`GET /v2/posts/:id` → promote/reset) — backlog item (a). `needs_reconcile` only ever arises under the `rest`/`mcp` backends, never `dryrun`.
- **`dryrun` posts are trackable** (audit C4): the dryrun poster stamps `submission_id = f"dryrun_<post_id>"`, so the `publish → track → analyzed → adjust` loop is fully exercisable offline by feeding `track` a metrics row keyed on that id. (CLI `track` still skips in dryrun — no live metrics source — but the seam is proven by `tests/integration/test_publish_track_loop.py`.)
- **Whisper model:** default `turbo`; falls back to the best cached `~/.cache/whisper/*.pt` when the requested model can't be downloaded (offline/proxied). In THIS env only `tiny` is cached.

## 4. Health checks

Run the §State Check commands. The integration E2E (`tests/integration/test_e2e_real.py`) is the golden path: real `say` TTS → real whisper transcript (asserts real whisper segment structure + ≥4 words + a vocoder-robust "anymore" anchor — CI-2) → real ffmpeg 1080×1920 vertical clip → 2 dryrun posts published. It pins the `tiny` whisper model in-test (no env var needed) and skips cleanly on a host with no cached checkpoint, so a fresh checkout / CI runner sees a clear skip rather than a cryptic `assert 0 == 1`.

## 5. Now (rewritten each session)

**As of 2026-06-02 (rev 15 — Phase E Close: real-CLI dead-man's-switch smoke + docs-sync + CI watch).**

**Most recent shipped (this Close session):** the Phase E **docs-sync** `868a3a0` (README + RUNTIME for: the learning loop closing in `run`, the per-source `max_amplify_per_source` budget, the dead-man's-switch heartbeat fields + how an external cron/PagerDuty monitor alerts on them, the "Pending agent gates" digest section, and the `--bare`/`ANTHROPIC_API_KEY` → silently-unauthed-responder connection) landed on `origin/main` (now under the rev-14/15 handoff commits at `18fae9f`); **CI green** (run `26812986398`, both jobs incl. the real-tooling E2E, watched to completion). The Phase E build itself was already merged before this session via **PR #5 (`c21f07a`)** with the E2/E3/E4 test-harden commits (`ef139d8`/`0933729`/`f6128b7`); the Phase B follow-up via **PR #3 (`7e50412`)**. The Phase E deviations + the hollow-test→harden episode + the deferred responder-backoff decision are recorded in the auto-memory `fanops-build-deviations.md` (§Phase E).

**Dead-man's-switch VERIFIED via the real CLI this session (not just the suite):** two back-to-back `python -m fanops.cli run --base-time 2026-06-02T18:00:00Z` on a scratch root (`/tmp/fanops_close`, cwd-resolved so it's not a repo false-green) → both **exit 0**; heartbeat printed BOTH times with a **CHANGING** ts (`2026-06-02T10:03:17.833573+00:00` then `2026-06-02T10:03:24.582671+00:00`) — the dead-cron-vs-alive-idle signal works for real; the learning pass was **skipped in dryrun** (0 `learn` lines in `run.log`, guard held); and both heartbeats were **appended** to `07_reports/run.log` (2 lines). This is the B5 guarantee exercised end-to-end through the binary, not a mock.

**Original rev-14 framing (still true): TWO merges this build cycle — Phase B follow-up (PR #3, merge `7e50412`) then Phase E (PR #5, merge `c21f07a`).**

**PR #3 (Phase B follow-up) — MERGED `7e50412`, CI green against the current base (the stale PR branch was updated from `main` → fresh CI run `26812620349` = success, THEN merged — not a blind merge of a 23-commit-behind branch).** Transaction-wraps every standalone CLI write command (`track`/`reconcile`/`adjust`/`ingest`/`pull`) so audit B4's lost-update fix now covers them too, with network/yt-dlp I/O kept OUTSIDE the lock; adds `pytest-timeout` (a self-deadlock now fails fast in CI). `test_cli_transactional.py` proves each command transacts + network runs unlocked (mutation-proven). Suite 283→291/1.

**Phase E (PR #5, merge `c21f07a`; the E2-harden mutation tests on top as `ef139d8`/`0933729`/`f6128b7`). Closes the audit's last 2 block-live findings — #2 and #6):**
- `1ee7e20` — **E1 (audit A2): the learning loop now closes in `run`.** After the respond+advance loop converges, `run` runs ONE guarded `track→adjust` pass (`pull_metrics`→`classify_outcomes`→`amplify`/`retire`), only when `cfg.poster_backend != "dryrun"` AND `cfg.blotato_api_key` (mirrors the reconcile guard), inside a `Ledger.transaction` (Phase B), try/except-log-continue (a metrics hiccup can't crash `run`). `amplify(..., max_amplify_per_source=3)` caps per-source via `src.meta["amplify_count"]` so an autonomous LLM can't grow one source's clips without bound.
- `7d2c434` + `ef139d8` — **E2 (audit B5): the dead-man's-switch.** `fanops.__version__="0.3.0"`; `advance` summary carries `published_in_run` (this-run delta, NOT cumulative) + `last_published_age_hours`; `run`/`advance` emit a CHANGING `heartbeat` JSON line (stdout + run.log) with version + ts + the deltas, so an external monitor can alert on "0 published in N runs" / "last post too old" / "cron itself dead" (previously healthy-idle and wedged emitted byte-identical output). The ts-changes-run-to-run property is mutation-proven (`ef139d8`).
- `696053f` — **E3 (audit H3): pending agent gates in the digest.** An explicit "Pending agent gates" section lists `pending(cfg, kind=...)` per kind so the operator sees WHICH gate the responder hasn't cleared instead of silent staleness.
- `5256db2` — **E4: per-post reconcile logging.** `reconcile_posts` logs one line per post at every branch (promoted/failed/left/skipped-no-id) to surface 429/timeout storms + the irreducible id-less human-reconcile residue.

Enforcement: dedicated worktree `/Users/molhamhomsi/fanops-phase-e` (now removed; merged). Same Workflow-style loop (Frame→Implement→Verify→Adversarial→Integrate→Close), independent verifier + ≥2 skeptics per task, mutation proof per new test. Suite **272→282 passed, 1 skipped** (now on `main`). PR #5 CI `26811594318` + merge/cherry-pick `26811831877` both = `completed success` (both jobs incl. real-tooling E2E, watched + re-queried).

**What works right now (on `main`):**
- **The learning loop is autonomous (A2 closed):** `run` pulls metrics + amplifies winners / retires losers ONCE per invocation on a live backend (skipped in dryrun, never crashes `run`); amplification is capped per source.
- **A silently-dead pipeline is detectable (B5 closed):** every `run`/`advance` prints a heartbeat (changing ts + `fanops_version` + `published_in_run` + `last_published_age_hours`) to stdout and run.log — a monitor diffing consecutive lines distinguishes alive-idle (ts advances, delta may be 0) from cron-dead (no new line).
- **The digest surfaces pending agent gates** (which gate the responder hasn't answered) and **reconcile logs every per-post branch.**
- **Every crossposted Post is reconcilable from birth (H1); a successful live post is never mis-marked `failed` (B2); the MCP poster honors the auth-halt contract (B3); 429 backoff is jittered + bounded; reconcile survives a per-post poll error** — all Phase D, on `main`.
- Everything Phase A/B/C still holds (autonomous `claude -p` + per-request quarantine; B4 transaction lock; H4/H5/H6 validation). **PRIME DIRECTIVE holds:** no path marks a maybe-live post `failed`. A shared per-clip media URL across accounts is intended — do NOT "fix" it.

**Live state caveats:**
- **All 6 original block-live findings are now CODE-CLOSED** (A1 responder, B4 race, B2 submission-id, B3 MCP auth, A2 learning loop, B5 dead-man's-switch) + the HIGH validation set (H1/H4/H5/H6) + observability (H3). What remains to "live" is OPERATOR-GATED, not code: valid `ANTHROPIC_API_KEY` in the deploy env, valid Blotato auth + connected real accounts, and the 3 human-only account steps. **Phase F (operator-recovery verbs) is the last credential-free phase.**
- **`claude -p` needs `ANTHROPIC_API_KEY`** in the `fanops` env (`--bare` ignores OAuth/keychain) — unchanged deploy caveat; E2's heartbeat (published_in_run=0 forever) + E3's pending-gates digest are now how a silently-unauthed responder becomes operator-visible.
- **LIVE Blotato is operator-gated + unauthenticated this session:** the MCP is connected but a data-returning call returns "Invalid API key or auth session". D5 was verified READ-ONLY against the tool schemas (contract MATCHES; `postUrl`/`publicUrl` split reconciled — no live bug). A live test post is DEFERRED (needs valid auth + an operator-named throwaway account; `blotato_create_post` posts to a REAL account, no dry-run — NEVER fired autonomously). The metrics ENGAGEMENT-FIELDS checkpoint honestly remains (only a real authed published post confirms which fields analytics exposes). See `[[blotato-mcp-not-reachable-headless]]`.

**Open items, in priority order:**
1. **Phase F — operator-recovery CLI verbs (THE NEXT + LAST credential-free phase).** `fanops unhold`/`retry-metrics`/`retry-source` so a non-expert never hand-edits `ledger.json`. Plan: `docs/superpowers/plans/2026-06-01-fanops-live-autonomous.md` Phase F (lines ~1748+). **Fold in:** the startup-preflight-warning idea (warn when `claude` present but `ANTHROPIC_API_KEY` unset). **The responder-backoff item was DEFERRED-WITH-REASONING in Phase E** (NOT silently dropped — it is a separate retry-policy task needing a product call on dead-letter/quarantine/halt; the operator-visibility win is already shipped via the heartbeat + pending-gates digest). Re-open it as its own task only after production heartbeat/digest data shows the real failure shape — see `fanops-build-deviations.md` §Phase E.
2. **LIVE Blotato verification, OPERATOR-GATED:** a data-returning MCP call + a live test post need valid Blotato auth + an operator-named throwaway account. Then the metrics-fields checkpoint can close.
3. **`claude -p` live answer (operator-gated):** in an `ANTHROPIC_API_KEY`-set env, drive one real gate end-to-end (plan A4 recipe).
4. **New feature (audit-C2 adjudication):** per-account creative variation (own spec→plan→build cycle).
5. **Pre-existing backlog** (`RUNTIME.md §Backlog`): externalize brand-risk lists + lift weights; per-platform max-duration clamp; `_parse`/`BASE_URL` triplication; per-source ranking; media size cap; C2 caption language *detection*. (Done across phases: 429-jitter, `fanops unhold` is Phase F.) (Cosmetic: bump CI actions off Node-20.)

**Pick up here:**
- **Start Phase F** of `docs/superpowers/plans/2026-06-01-fanops-live-autonomous.md` (operator-recovery verbs — the LAST credential-free phase) on a FRESH worktree off `main` (`18fae9f`), same Workflow-enforced loop (Frame→Implement→Verify→Adversarial→Integrate→Close). Read the Phase F task list first (the plan's `# Phase F` section, lines ~1748+) for the exact verbs + TDD steps. All credential-free. **The responder-backoff item is NOT open for Phase F as a given** — it was deliberately DEFERRED-WITH-REASONING in Phase E (a retry-policy change is a different category from E's observability tasks; the operator win — making a stuck/unauthed responder VISIBLE via the heartbeat + pending-gates digest — is already delivered; designing dead-letter/quarantine/halt is its own task once production data shows the real failure shape). See `fanops-build-deviations.md` §Phase E for the full reasoning before re-opening it.
- **All phase worktrees + branches are cleaned up** (phase-b-concurrency, phase-c, phase-d-blotato, phase-e, phase-b-followup all merged + deleted local + `origin`; worktrees removed). **NO open PRs.** Only `main` + its primary worktree remain. Workflow scripts for prior phases live on `main` at `.claude/workflows/fanops-phase-{d,d-confirm,e}.js`.
- **Standing process gotchas (carried forward — they still bite the next phase):** (a) a FRESH worktree needs its OWN editable venv (`pip install -e ".[dev]"`, **python3.12 not the default 3.14** which is out of the `<3.14` range) — symlinking the parent `.venv` tests the WRONG src tree. (b) When verifying through the real CLI, **`cd` to the project/scratch root first** (`Config()` resolves the ledger from cwd; no `FANOPS_ROOT`). (c) **The Workflow JS parser rejects `arr.map((x,i)=>()=>agent(...))` with `${x}` interpolation in the prompt** — use explicit `parallel([()=>agent(plainStringFn(a)), ...])` with string-concatenation prompts. (d) **Skeptic/audit agents can leave the worktree modified mid-run** — always `git status` + independently verify/mutation-prove before trusting or committing their work. (e) **A subagent chip can land a commit on the branch AFTER you push/PR** (the E2-harden `e372536` did) — before deleting a merged branch, `git diff <branch> main` and cherry-pick any branch-only commits; never blind-delete.
- Standing: `main` at `18fae9f`, in sync with `origin/main`; NO open PRs. **Do not re-raise audit-C2 (byte-identical media)** — adjudicated. **Do NOT autonomously fire `blotato_create_post`** — it posts to a real account. **A fresh/local venv must `pip install -e ".[dev]"`** (PR #3 added `pytest-timeout` + a `timeout=60` pyproject option — without the plugin pytest warns "Unknown config option: timeout").
