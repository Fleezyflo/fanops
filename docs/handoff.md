# HANDOFF — MOH FLOW FAN OPS

This file is the cross-session source of truth. It is **rewritten each session** (the §Now block is point-in-time, not an append-only log). Frozen provenance lives in `docs/archive/handoff-history.md` (created on first archive). The build plan is `docs/superpowers/plans/2026-05-31-fanops-real-build-v2.md`. Deviations from that plan made during the build are recorded in the auto-memory file `fanops-build-deviations.md`.

## §State (re-verify the Check commands before quoting)

- **Branch:** `main` (canonical line) @ **`16813d4`**. Phases A–F + the G finale + credential-free TAIL + subtitles (PR #8) + creative variation v1 (PR #9) + content discovery (PR #12) + v2 A/B learning loop (PR #14) + **ALL THREE v2 follow-ups: variant-gated amplification (`143deea`), cross-account transfer (PR #15 `323ba86`), and the v3 UCB bandit (PR #16 `16813d4`)** are all on `main`. **NO open PRs.** All three creative-variation flags (`FANOPS_VARIANT_AMPLIFY`/`_TRANSFER`/`_UCB`) default OFF, fail-safe, C1-isolated. Check: `git branch --show-current` / `gh pr list --repo Fleezyflo/fanops --state open` (expect empty).
- **v3-UCB bandit (now on main):** `variant_learning.ucb_rank` is a deterministic UCB1 allocator (`score = mean_lift + c·sqrt(ln N / n)`, no RNG/hash/wall-clock — Thompson rejected for needing an RNG); `FANOPS_VARIANT_UCB` (default OFF) selects it for the OWN-surface caption bias instead of v2 gated-greedy `best_hooks`, `FANOPS_VARIANT_UCB_C` (default √2; 0=greedy, neg/bad→default) tunes exploration. INDEPENDENT of `FANOPS_VARIANT_LEARNING`. **Amplify floor UNCHANGED** — `variant_amplify` keeps `best_hooks` as its C1 floor and NEVER calls `ucb_rank`; enforced by extended `_FORBIDDEN_IN_AMPLIFY` + a positive `ucb_rank` caller-lock (caption.py/digest.py only) + an `amplify_candidates` UCB-on==UCB-off invariant, all mutation-proven, + on-disk determinism (`tests/integration/test_variant_ucb_real.py`). Coexists with transfer (orthogonal: UCB=own-surface scorer; transfer=`learned_hooks_transferred` cold-start key; `_gate_state` precedence UCB→learning→borrowing→gathering). Check: `grep -n "def ucb_rank" src/fanops/variant_learning.py`.
- **Working tree:** clean, in sync with origin. Check: `git status -sb` (expect `## main...origin/main` with nothing below).
- **HEAD (of `main`):** `5f275fd` (Merge pull request #14 — Creative Variation v2) → this rev-24 handoff commit lands on top. Check: `git log --oneline -1`.
- **Remote:** `origin` → `github.com/Fleezyflo/fanops` (**PRIVATE**). PRs #1–#9, #11, #12, #13, #14 MERGED; #10 CLOSED (duplicate of #9); no open PRs. Check: `git remote -v && gh pr list --repo Fleezyflo/fanops --state open`.
- **Responder auth (rev-21, IMPORTANT):** the autonomous LLM responder uses the operator's **EXISTING `claude` subscription** via plain `claude -p` (NOT `--bare`) — it needs `claude login` on the host, **NOT `ANTHROPIC_API_KEY`**. Proven: the real autonomous pipeline ran end-to-end on the subscription (`FANOPS_RESPONDER=llm`, dryrun) — `sources→moments→clips→posts→published=1`, no longer parks at `awaiting_moments`. Check: `grep -n "strict-mcp-config" src/fanops/llm.py` (the new flag; `--bare` is gone).
- **Unit/full tests:** on `main`@`16813d4` (all three follow-ups merged): **470 passed, 1 skipped** (verified this session post-merge). Lineage: 421 at the v3 fork point (`143deea`, amplify) → +~25 (transfer PR #15) → +24 (v3 UCB PR #16) = 470. Check: `source .venv/bin/activate && python -m pytest -q`. The 1 skip = creds-gated Blotato smoke. NOTE: dev-deps include **`pytest-timeout`** AND **`ruff>=0.5`** (+ a global `timeout=60` in `pyproject`); a fresh/local venv must `pip install -e ".[dev]"` (python3.12). KNOWN FLAKE: the offline-`whisper` E2E (`test_e2e_real.py`) can "fail" via the 60s pytest-timeout under heavy parallel load, but SKIPS cleanly under a normal `pytest -q` run and runs green in CI. RECURRING FALSE ALARM: a subagent running bare `pytest` (no venv) reports "pytest-mock not installed / N errors" — ALWAYS verify with `source .venv/bin/activate && python -m pytest -q` first (387/1).
- **ffmpeg (toolchain):** the system `ffmpeg` is now **`ffmpeg-full`** (8.1.1, `brew link --overwrite --force`'d) — it has the text filters (`drawtext`/`subtitles`/`ass`, libass/freetype) the burned-in-subtitles feature needs. The default `ffmpeg` formula on this box is a STRIPPED build WITHOUT them; if it ever gets relinked, subtitles FAIL OPEN (clip renders without text + logs a warning, never blocks). Check: `ffmpeg -hide_banner -h filter=drawtext | head -1` (must NOT say "Unknown filter"). Local-machine fix only; CI's Linux runner has full ffmpeg via apt.
- **Lint:** `ruff check src/` is GREEN (`[tool.ruff.lint] select=["E","F"]`, 4 style E-rules ignored, F bug-class fully retained). Check: `source .venv/bin/activate && ruff check src/` (expect "All checks passed!"). A fresh venv needs `pip install -e ".[dev]"` first (ruff is a dev-dep).
- **Integration tests:** 7 passed, 1 skipped (live Blotato smoke skips without creds). Includes `test_discover_real.py` (discovery) and `test_variant_learning_real.py` (the v2 loop-closing proof: a winning hook reaches the real on-disk caption request). Lock/deadlock tests run under `pytest-timeout` (global `timeout=60` in `pyproject` + a stricter `--timeout=20` for the lock tests); a hang is a hard FAIL. Check: `source .venv/bin/activate && python -m pytest tests/integration -q`.
- **Module/test parity:** on `main`@`16813d4`: **41 src / 50 tests** (amplify added `variant_amplify.py`+2 tests; transfer added `variant_transfer.py`+2 tests; v3-UCB added NO new src module — it extends `variant_learning.py` — + `test_variant_ucb_real.py`). Check: `ls src/fanops/*.py src/fanops/post/*.py | wc -l && ls tests/*.py tests/integration/*.py | wc -l`.
- **Real tooling present:** ffmpeg 8.1.1 (now the **ffmpeg-full** build — text filters/libass), ffprobe, whisper CLI, `say` (macOS TTS) all on PATH; whisper `tiny.pt` cached. The E2E pins `tiny` itself; override with `FANOPS_WHISPER_MODEL`. Check: `for b in ffmpeg ffprobe whisper say; do command -v $b; done`.
- **Posting backend default:** `dryrun` (writes payload JSON to `05_scheduled/`, posts nothing). Switch via `FANOPS_POSTER=rest|mcp` + `BLOTATO_API_KEY`. Check: `grep -n "FANOPS_POSTER" src/fanops/config.py`.
- **CI:** `.github/workflows/ci.yml` — `unit` job (no toolchain) + `real-tooling E2E` job (installs toolchain, `FANOPS_REQUIRE_E2E=1`, audit H10). Runs on push to `main` AND on `pull_request` into `main`. **PR #16 (v3-UCB) CI: `unit` GREEN (24s, run `27028755112`); `real-tooling E2E` was RUNNING at handoff time — re-check with `gh pr checks 16 --repo Fleezyflo/fanops`.** `main` was last green on the amplify merge `143deea` and the v2 merge `5f275fd` (run `26964159930`). Check: `gh run list --repo Fleezyflo/fanops --branch main --limit 2`. (Non-fatal annotation: "Node.js 20 actions are deprecated" — cosmetic backlog: bump `actions/checkout@v4`/`setup-python@v5`/`cache@v4`.)
- **Toolchain-absent behavior:** every `subprocess.run` of a media binary (ffmpeg/ffprobe/whisper/yt-dlp) is guarded — `check=False` does NOT catch a pre-launch `FileNotFoundError`, so absent binaries used to crash. Now: pre-Source/one-shot sites (ffprobe at ingest, yt-dlp at `pull`) raise typed `errors.ToolchainMissingError` → `cli.main` exit 2 + "install …", no traceback; pipeline-quarantined sites (ffmpeg render, whisper transcribe, ffmpeg signals) record a retriable per-unit `error`. Check: `grep -rn "subprocess.run" src/fanops/ | grep -v "_run_ffprobe\|_run_ffmpeg\|try:"` (expect only the lines INSIDE the guards) and `grep -n "ToolchainMissingError" src/fanops/errors.py`.
- **Concurrency (Phase B + follow-up):** `advance()` AND **every standalone write command** (`track`, `reconcile`, `adjust`, `ingest`, `pull`) run the WHOLE load→mutate→save inside ONE `Ledger.transaction(cfg)` holding the `fcntl.flock` — acquired BEFORE load, single atomic save on clean exit (audit B4, now everywhere). A 2nd live run is excluded with a typed `LockBusyError`. Slow I/O stays OUTSIDE the lock: `publish_due(in_transaction=True)` uses the unlocked save; `track`'s metrics fetch + `reconcile`'s per-post polls + `pull`'s yt-dlp download all run BEFORE the transaction (only the in-memory apply is locked) — so a 30s network call never serializes behind the ledger lock. crosspost+publish are wrapped (log+continue) so a non-auth raise can't roll back the pass, while a fatal `BlotatoAuthError` still escapes (F52). The slow `claude -p` responder runs OUTSIDE the lock (`request_id` correlation + capture-and-recheck against a mid-call re-seed, audit A3). Check: `grep -n "def transaction\|_save_unlocked" src/fanops/ledger.py && grep -ln "Ledger.transaction" src/fanops/cli.py`.

## 1. What this is

An autonomous fan-account engine for Moh Flow (bilingual EN/AR rapper). It ingests his videos, decides which moments are worth posting (transcript + audio/scene signals → an agent decision with a recorded reason), cuts platform-ready clips with agent-written EN/AR captions, cross-posts to every fan account × platform via Blotato (staggered for opsec, with a subtle non-synchronized artist @mention), then pulls real performance back to make more of what works. See `README.md` (front door), `MohFlow-FanOps/00_control/RUNTIME.md` (operating loop + production seams), `MohFlow-FanOps/00_control/RISK.md` (recorded operator risk-acceptance for the multi-account model), `MohFlow-FanOps/00_control/context.md` (functional — injected as agent guidance).

## 2. Architecture

- **Unit chain:** `Source → Moment → Clip → Post`, over one git-versioned JSON ledger (`00_control/ledger.json`, atomic temp+os.replace write under a file lock).
- **Identity:** content-addressed SHA-based ids everywhere (`ids.py`: `make_id`/`child_id`/`surface_key`) — NEVER builtin `hash()` (the #1 v1 bug; cross-process stability is proven by a subprocess idempotency test).
- **Agent gates:** generative steps (`decide_moments`, `write_captions`) cross a file contract in `04_agent_io/requests/` — code writes a `*.request.json` (stamped with a `request_id`), the agent/responder writes a validated `*.response.json` echoing that id, code resumes. Stale responses can never be applied (request_id correlation).
- **Two responders** (`responder.py`): `ManualResponder` (no-op; a human/cron writes responses) vs `LlmResponder` (`FANOPS_RESPONDER=llm`; **autonomous via `claude -p`** — default model shells the Claude Code CLI through `llm.claude_json` with committed `prompts.py` templates + the gate's pydantic JSON schema, validates against `MomentDecision`/`CaptionSet`, **per-request quarantine** so one bad/failed gate logs + stays pending without halting the rest). Needs `claude` on PATH (authenticated); model is injectable for tests.
- **Three posters** (`post/`): `dryrun` (default), `rest` (`BlotatoRestPoster`, retry/backoff/typed-errors), `mcp` (`BlotatoMcpPoster`, injected `tool_caller`).
- **Pipeline** (`pipeline.py` `advance()`): per-unit error quarantine (one bad source/moment/clip → `error` state, never wedges the pass). **CLI** (`cli.py`): `status/ingest/pull/advance/respond/track/adjust/gc/run/digest`.
- **State machines** (`models.py`): separate `SourceState`/`MomentState`/`ClipState`/`PostState`. `failed`≠`analyzed`; `held` (brand-risk) and `retired` are first-class.

## 3. Gotchas / non-obvious invariants

- **`--base-time` is the SCHEDULE ANCHOR, not the publish cutoff.** crosspost staggers posts to times AFTER base-time; `publish_due(now=None)` publishes whatever is due as of REAL wall-clock now. To publish in the same pass (backfill), pass a `--base-time` in the past. (This was a fixed plan contradiction.)
- **Conservative retirement:** `adjust` amplifies top `--winner-pct` (0.3) but retires only the bottom `--retire-pct` (0.2) AND below `--lift-floor` (20.0) — objectively-fine clips above the floor are never retired. Retiring a clip also retires its moment (so it can't be re-rendered).
- **Three surfaced-but-auto-unrecovered stuck states** (all in the digest + counts, no silent loss): a post stranded in `submitting` by a mid-publish crash; a post in **`needs_reconcile`** (REST hit an ambiguous 5xx/timeout after sending the body — it may be live, so it is NOT re-POSTed; Blotato has no idempotency key); a `published` post Blotato never returned metrics for. The first two need a poll step (`GET /v2/posts/:id` → promote/reset) — backlog item (a). `needs_reconcile` only ever arises under the `rest`/`mcp` backends, never `dryrun`.
- **`dryrun` posts are trackable** (audit C4): the dryrun poster stamps `submission_id = f"dryrun_<post_id>"`, so the `publish → track → analyzed → adjust` loop is fully exercisable offline by feeding `track` a metrics row keyed on that id. (CLI `track` still skips in dryrun — no live metrics source — but the seam is proven by `tests/integration/test_publish_track_loop.py`.)
- **Whisper model:** default `turbo`; falls back to the best cached `~/.cache/whisper/*.pt` when the requested model can't be downloaded (offline/proxied). In THIS env only `tiny` is cached.

## 4. Health checks

Run the §State Check commands. The integration E2E (`tests/integration/test_e2e_real.py`) is the golden path: real `say` TTS → real whisper transcript (asserts real whisper segment structure + ≥4 words + a vocoder-robust "anymore" anchor — CI-2) → real ffmpeg 1080×1920 vertical clip → 2 dryrun posts published. It pins the `tiny` whisper model in-test (no env var needed) and skips cleanly on a host with no cached checkpoint, so a fresh checkout / CI runner sees a clear skip rather than a cryptic `assert 0 == 1`.

## 5. Now (rewritten each session)

**As of 2026-06-05 (rev 26 — Creative Variation v3 / the deterministic UCB bandit MERGED to `main` (PR #16 `16813d4`); ALL THREE v2 follow-ups now on main, NO open PRs).**

**Most recent shipped:** **Creative Variation v3 — the deterministic UCB bandit — MERGED to `main` as `16813d4` (PR #16).** Required a non-trivial merge: transfer PR #15 landed on main while #16 was open, so the two collided in the shared caption-bias seam (config/caption/digest + tests + RUNTIME.md); resolved by KEEPING BOTH (orthogonal — UCB swaps the OWN-surface scorer, transfer adds a separate `learned_hooks_transferred` key; `_gate_state` precedence hand-merged to UCB→learning→borrowing→gathering). A dropped `@property` decorator at a conflict-hunk boundary caused 9 test failures — caught by running the suite BEFORE committing the merge, fixed in one line. **Merged `main` suite: 470 passed, 1 skipped; ruff clean; unit CI ✓** (verified locally + on main). The LOWEST-priority of the three v2 follow-ups. Replaces v2's gated-greedy `best_hooks` ONLY on the OWN-surface caption bias with **`variant_learning.ucb_rank`** — a deterministic UCB1 bandit `score = mean_lift + c·sqrt(ln N / n)`. **The central design challenge (no-RNG under the content-addressed invariant) is solved structurally:** UCB1 needs no randomness — the score is pure arithmetic over ledger counts/means, so a re-run is byte-identical (Thompson sampling rejected for requiring an RNG). Kills both v2 weaknesses: early-winner LOCK-IN (the confidence bonus lets an under-sampled challenger overtake a thin-lead leader) and SILENCE below the gate (no `min_gap` floor — never silent once any variant data exists). `FANOPS_VARIANT_UCB` (default OFF) selects it; `FANOPS_VARIANT_UCB_C` (default √2; 0=pure greedy; neg/bad→default) tunes exploration. Shared `_collect_lifts` (v2 `best_hooks` and v3 read the ledger identically; `best_hooks` behavior preserved). Digest reports the UCB pick. Suite 421→**445, 1 skipped** (+24 across config/scorer/caption/digest/isolation/integration). Spec+plan: `docs/superpowers/{specs,plans}/2026-06-04-creative-variation-v3-ucb-bandit*.md`.

**SAFETY — the one NEW invariant beyond v2's (load-bearing):** `variant_amplify` (the amplify auto-propagate follow-up, merged `143deea`) uses `best_hooks` as its **C1 floor** to authorize the delete-cascade-adjacent amplify path. v3 must NEVER let the *exploratory* bandit become an amplify authorization. Guaranteed structurally (`ucb_rank` is swapped in ONLY inside `caption._learned_hooks`) AND tested three ways, all **mutation-proven** / independently re-verified: (1) `_FORBIDDEN_IN_AMPLIFY` extended with `ucb_rank`/`variant_ucb` (the AST data-flow test); (2) a positive `ucb_rank` caller-lock allowing ONLY `caption.py`/`digest.py` (NOT `variant_amplify.py`) — injecting a real `ucb_rank(...)` call into `amplify_candidates` turns it RED; (3) an `amplify_candidates(led,cfg)` UCB-on==UCB-off byte-identity test (a real WIN candidate, not two empties). Determinism proven END-TO-END on disk (`tests/integration/test_variant_ucb_real.py`: the UCB pick "NEW" reaches the real caption-request file, AND a re-run from an identically-seeded root is byte-identical — `request_id` confirmed a deterministic SHA-1 content+chain hash, no clock/random).

**HOW IT WAS BUILT + THE EPISODE LESSON:** spec→plan→strict-TDD (5 tasks), each implemented by a fresh subagent then reviewed in two stages (spec-compliance, then code-quality) by separate read-only reviewers, with the controller independently re-running every safety claim (the determinism SHA-chain read, the mutation RED/GREEN, the amplify-floor off==on). **The build PAUSED mid-setup** when the repo turned out to be under a CONCURRENT actor: the amplify session was building in the SAME working tree (branch identity flipped twice in minutes; HEAD found mid-cherry-pick with a `config.py` conflict). I down-tooled rather than do branch surgery on another actor's in-flight work; once amplify merged (`143deea`) and the repo went quiescent, I branched cleanly off `main`. **The brief/old-memory's "transfer already merged" premise was FALSE** (transfer is PR #15 OPEN, absent on this base) — I corrected the spec/plan wording as the first commit.
- **RULE 1 (durable):** never do branch surgery while another actor mutates the repo — wait for quiescence (no in-progress merge/cherry-pick, stable branch identity, clean tree).
- **RULE 2 (durable):** the brief's spec→plan→review pipeline is a gate to PAUSE at, NOT a prompt to interrogate the operator at every seam. I over-asked early (re-confirming things the brief already settled) and got "Wasn't the brief clear enough?" ×3 + "don't ask me, sort it out". Reserve operator questions for decisions genuinely theirs (branch-base under a concurrent-actor tangle; outward-facing PR-vs-merge) — not algorithm defaults I'm equipped to set (e.g. c=√2).

**Prior shipped (all on `main`):** variant-gated amplification / the amplify auto-propagate follow-up (`143deea`, NOT via a PR I own — landed by the concurrent amplify session); v2 A/B learning loop (PR #14 `5f275fd`); content discovery + intake (PR #12); responder-on-subscription (PR #11); creative variation v1 (PR #9). Detail for v1/v2 episode lessons in `fanops-build-deviations.md` + `fanops-variation-v2-learning-loop.md`; the amplify follow-up in `fanops-variation-v3-amplify.md`.

**What works right now:**
- **On `main`:** the entire live-autonomous plan (all 19 audit gaps) + credential-free tail + subtitles (`FANOPS_BURN_SUBS`) + creative variation v1 (`FANOPS_CREATIVE_VARIATION`) + content-intake (`fanops discover`→approve→`fanops intake`) + the v2 A/B learning loop (`FANOPS_VARIANT_LEARNING`) + **variant-gated amplification (`FANOPS_VARIANT_AMPLIFY`, default OFF, fail-SAFE, the first feature to touch the C1 path — gated FAR harder than v2: best_hooks floor + 8 posts + 25 gap + ≥3-window streak + budget + kill switch)**. Suite on main `143deea`: **421/1** (verified this session — the v3-branch fork point).
- **On PR #16 branch (NOT yet on main):** the v3 UCB bandit (`FANOPS_VARIANT_UCB`). 445/1 on the branch.
- **PRIME DIRECTIVE holds:** no path marks a maybe-live post `failed`. The C1 amplify/delete path stays blind to variant signal — and now also blind to the UCB bandit (mechanized, mutation-proven, both on the branch and via amplify's own tests on main).

**Live state caveats:**
- **Going LIVE is PURELY OPERATOR-GATED** (G3 runbook in `RUNTIME.md`): real fan accounts + Blotato connect + numeric `account_id`s; `BLOTATO_API_KEY`; **`claude login` on the host** (NO `ANTHROPIC_API_KEY`); the `[GATED]` Blotato smoke; cron `fanops run` + dead-man's-switch. The BRAIN is unblocked (proven on the subscription); the ONLY remaining gate is BLOTATO (posting).
- **Blotato subscription NOT yet acquired (operator):** the MCP returns "Invalid API key or auth session" on any data call; the live-contract smoke is BLOCKED until a key + re-auth. `blotato_create_post` posts to a REAL account, NEVER fired autonomously.
- **v3 UCB (and v2/amplify) are OPT-IN + need DATA:** all default OFF; even ON they emit nothing until a surface has analyzed posts with real lift — which only accrues AFTER going live and pulling metrics, and requires `FANOPS_CREATIVE_VARIATION=1` to have variants. Pre-live they are correctly inert.
- **ffmpeg is `ffmpeg-full` (local fix):** subtitles + creative-variation hooks need libass text filters; a stripped relink → FAIL OPEN (plain clip + warning). CI's Linux runner has full ffmpeg via apt.

**Open items, in priority order:**
1. **Operator-credential-gated (Blotato) — THE ONLY HIGH-VALUE PATH LEFT.** Get a Blotato subscription/key → run the live-contract smoke (read-only) + fix any field mismatch TDD → the live cutover (G3 runbook in `RUNTIME.md`). This is what unlocks real metrics — and thus what makes any of the creative-variation machinery (v1 attribution, v2 learning, v3 transfer/amplify/UCB) actually *learn* from real data (all are correctly inert pre-live). The BRAIN is unblocked (responder proven on the `claude` subscription); the ONLY remaining gate is BLOTATO (posting). NEVER autonomously fire `blotato_create_post`.
2. **The v2 follow-up trio is DONE** (amplify `143deea` + transfer #15 + UCB #16 all on main). No feature work outstanding. "v3" is an overloaded label across all three (amplify/transfer/UCB all self-labelled v3) — see `fanops-variation-v3-amplify.md` + `fanops-v3-followups-state.md`.
3. **Low-value backlog** (operator-deprioritized): trending-audio, daypart/timezone scheduling, per-surface best-window learning, per-source ranking. By-design "not built": multi-artist tenancy, richer secrets manager. Cosmetic: bump CI off deprecated Node-20 actions. Possible future v3-UCB refinements (documented out-of-scope in its spec): recency-weighted/non-stationary counts, weighted multi-hook hints, per-platform `c`.

**Pick up here:**
- **`main` @ `16813d4` is clean, in sync, NO open PRs.** Verify with the §State Check commands (expect 470/1, ruff clean, 41 src / 50 tests). **The v2 follow-up TRIO is COMPLETE** (amplify `143deea` + transfer #15 `323ba86` + UCB #16 `16813d4`, all merged). The only high-value path left is the operator-credential-gated Blotato live smoke + cutover.
- **Confirm main CI went green on `16813d4`:** `gh run list --repo Fleezyflo/fanops --branch main --limit 1` (the `unit` job was ✓ at handoff; `real-tooling E2E` was still running — the merged tree is locally-verified 470/1, so expect green; the offline-whisper E2E is a KNOWN FLAKE under load but green in CI).
- **When the operator gets a Blotato key:** re-probe the MCP (`blotato_get_user`, read-only) to confirm auth; if real data returns, run the live-contract verification (diff against `_extract_submission_id`/media/analytics, fix any mismatch TDD). NEVER autonomously fire `blotato_create_post`.
- **Standing process gotchas (durable multi-agent wisdom):** (a) a FRESH worktree needs its OWN editable venv (`pip install -e ".[dev]"`, **python3.12 not 3.14**); this session built on a feature branch in the MAIN tree (existing `.venv`) to skip that bootstrap. (b) Verifying through the real CLI / scratch render: **`cd` to a scratch root first** (`Config()` resolves from cwd). (c) **ALWAYS independently re-run the committed artifact + read the code before trusting a green** — this session a reviewer re-did the mutation proof and the determinism root-cause rather than trust the implementer's report. (d) **ONE executor per task.** (e) `git diff <branch> main` before deleting any merged branch. (f) **VERIFY THE TOOLCHAIN before building a feature that depends on it.** (g) **For "did the pixels render", VIEW the image — OCR is flaky.** (h) an adversarial/skeptic `holds=false` is a lead to investigate against real code, not an automatic verdict. (i) **NEW (rev 25): never do branch surgery while another actor mutates the repo — wait for quiescence.** (j) **NEW (rev 25): verify branch-state against the RIGHT ref (`git log main`, not a feature branch's log) before trusting "X is merged" claims** — the brief's "transfer merged" was false; I caught it only by checking `main` directly.
- Standing invariants: **Do not re-raise audit-C2 (byte-identical media)** — the valuable reframing (creative variation v1/v2/v3 + amplify) is BUILT. **The C1 amplify/delete path must stay blind to variant signal AND to the UCB bandit** (mechanized + mutation-proven; amplify's `best_hooks` floor is the ONLY variant signal it reads, and v3 keeps it that way). **Do NOT autonomously fire `blotato_create_post`.** **A fresh/local venv must `pip install -e ".[dev]"`** (python3.12).
