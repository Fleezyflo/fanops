# HANDOFF — MOH FLOW FAN OPS

This file is the cross-session source of truth. It is **rewritten each session** (the §Now block is point-in-time, not an append-only log). Frozen provenance lives in `docs/archive/handoff-history.md` (created on first archive). The build plan is `docs/superpowers/plans/2026-05-31-fanops-real-build-v2.md`. Deviations from that plan made during the build are recorded in the auto-memory file `fanops-build-deviations.md`.

## §State (re-verify the Check commands before quoting)

- **Branch:** `main` (canonical line). Phases A–F + the G finale + the credential-free TAIL + burned-in subtitles (PR #8) + **per-account creative variation (PR #9) are ALL MERGED into `main` (PRs #1–#9; #10 was a duplicate, CLOSED).** **NO open PRs.** The plan is COMPLETE, the credential-free backlog is DONE, and BOTH deferred-from-plan features shipped (subtitles + creative variation). All feature branches + worktrees deleted. Check: `git branch --show-current` / `gh pr list --repo Fleezyflo/fanops --state open` (expect empty).
- **Working tree:** clean, in sync with origin. Check: `git status -sb` (expect `## main...origin/main` with nothing below).
- **HEAD (of `main`):** `8c69b5b` (Merge pull request #12 from Fleezyflo/feat-content-discovery) → this rev-22 handoff commit lands on top. Check: `git log --oneline -1`.
- **Remote:** `origin` → `github.com/Fleezyflo/fanops` (**PRIVATE**). PRs #1–#9 + #11 MERGED; #10 CLOSED (duplicate of #9); no open PRs. Check: `git remote -v && gh pr list --repo Fleezyflo/fanops --state open`.
- **Responder auth (rev-21, IMPORTANT):** the autonomous LLM responder uses the operator's **EXISTING `claude` subscription** via plain `claude -p` (NOT `--bare`) — it needs `claude login` on the host, **NOT `ANTHROPIC_API_KEY`**. Proven: the real autonomous pipeline ran end-to-end on the subscription (`FANOPS_RESPONDER=llm`, dryrun) — `sources→moments→clips→posts→published=1`, no longer parks at `awaiting_moments`. Check: `grep -n "strict-mcp-config" src/fanops/llm.py` (the new flag; `--bare` is gone).
- **Unit/full tests:** on `main`: **351 passed, 1 skipped** (creative variation added +13 over subtitles' 338: overlay.burn_hook_only / Moment.hook+derive_hook / variant model fields / crosspost variant wiring / digest lift-by-variant / config toggle). Check: `source .venv/bin/activate && python -m pytest -q`. The 1 skip = creds-gated Blotato smoke. NOTE: dev-deps include **`pytest-timeout`** AND **`ruff>=0.5`** (+ a global `timeout=60` in `pyproject`); a fresh/local venv must `pip install -e ".[dev]"` (python3.12). KNOWN FLAKE: the offline-`whisper` E2E (`test_e2e_real.py`) can "fail" via the 60s pytest-timeout under heavy parallel load, but SKIPS cleanly under a normal `pytest -q` run and runs green in CI. RECURRING FALSE ALARM: a subagent running bare `pytest` (no venv) reports "pytest-mock not installed / N errors" — ALWAYS verify with `source .venv/bin/activate && python -m pytest -q` first (351/1).
- **ffmpeg (toolchain):** the system `ffmpeg` is now **`ffmpeg-full`** (8.1.1, `brew link --overwrite --force`'d) — it has the text filters (`drawtext`/`subtitles`/`ass`, libass/freetype) the burned-in-subtitles feature needs. The default `ffmpeg` formula on this box is a STRIPPED build WITHOUT them; if it ever gets relinked, subtitles FAIL OPEN (clip renders without text + logs a warning, never blocks). Check: `ffmpeg -hide_banner -h filter=drawtext | head -1` (must NOT say "Unknown filter"). Local-machine fix only; CI's Linux runner has full ffmpeg via apt.
- **Lint:** `ruff check src/` is GREEN (`[tool.ruff.lint] select=["E","F"]`, 4 style E-rules ignored, F bug-class fully retained). Check: `source .venv/bin/activate && ruff check src/` (expect "All checks passed!"). A fresh venv needs `pip install -e ".[dev]"` first (ruff is a dev-dep).
- **Integration tests:** 4 passed, 1 skipped (live Blotato smoke skips without creds). Lock/deadlock tests run under `pytest-timeout` (global `timeout=60` in `pyproject` + a stricter `--timeout=20` for the lock tests); a hang is a hard FAIL.
- **Module/test parity:** 38 src modules (discovery added `discover.py`), 43 test files (discovery added `test_discover.py` + `tests/integration/test_discover_real.py`). Check: `ls src/fanops/*.py src/fanops/post/*.py | wc -l && ls tests/*.py tests/integration/*.py | wc -l` (38 / 43).
- **Real tooling present:** ffmpeg 8.1.1 (now the **ffmpeg-full** build — text filters/libass), ffprobe, whisper CLI, `say` (macOS TTS) all on PATH; whisper `tiny.pt` cached. The E2E pins `tiny` itself; override with `FANOPS_WHISPER_MODEL`. Check: `for b in ffmpeg ffprobe whisper say; do command -v $b; done`.
- **Posting backend default:** `dryrun` (writes payload JSON to `05_scheduled/`, posts nothing). Switch via `FANOPS_POSTER=rest|mcp` + `BLOTATO_API_KEY`. Check: `grep -n "FANOPS_POSTER" src/fanops/config.py`.
- **CI:** `.github/workflows/ci.yml` — `unit` job (no toolchain) + `e2e` job (installs toolchain, `FANOPS_REQUIRE_E2E=1`, audit H10). Runs on push to `main` AND on `pull_request` into `main`. **`main` CI GREEN on the content-discovery merge commit `8c69b5b` (run watched `WATCH_EXIT=0`, both jobs); prior greens at `98f7d7a`, `01292cd`.** Check: `gh run list --repo Fleezyflo/fanops --branch main --limit 2`. (Non-fatal annotation: "Node.js 20 actions are deprecated" — cosmetic; backlog to bump `actions/checkout@v4`/`setup-python@v5`/`cache@v4`.)
- **Toolchain-absent behavior:** every `subprocess.run` of a media binary (ffmpeg/ffprobe/whisper/yt-dlp) is guarded — `check=False` does NOT catch a pre-launch `FileNotFoundError`, so absent binaries used to crash. Now: pre-Source/one-shot sites (ffprobe at ingest, yt-dlp at `pull`) raise typed `errors.ToolchainMissingError` → `cli.main` exit 2 + "install …", no traceback; pipeline-quarantined sites (ffmpeg render, whisper transcribe, ffmpeg signals) record a retriable per-unit `error`. Check: `grep -rn "subprocess.run" src/fanops/ | grep -v "_run_ffprobe\|_run_ffmpeg\|try:"` (expect only the lines INSIDE the guards) and `grep -n "ToolchainMissingError" src/fanops/errors.py`.
- **Concurrency (Phase B + follow-up):** `advance()` AND **every standalone write command** (`track`, `reconcile`, `adjust`, `ingest`, `pull`) run the WHOLE load→mutate→save inside ONE `Ledger.transaction(cfg)` holding the `fcntl.flock` — acquired BEFORE load, single atomic save on clean exit (audit B4, now everywhere). A 2nd live run is excluded with a typed `LockBusyError`. Slow I/O stays OUTSIDE the lock: `publish_due(in_transaction=True)` uses the unlocked save; `track`'s metrics fetch + `reconcile`'s per-post polls + `pull`'s yt-dlp download all run BEFORE the transaction (only the in-memory apply is locked) — so a 30s network call never serializes behind the ledger lock. crosspost+publish are wrapped (log+continue) so a non-auth raise can't roll back the pass, while a fatal `BlotatoAuthError` still escapes (F52). The slow `claude -p` responder runs OUTSIDE the lock (`request_id` correlation + capture-and-recheck against a mid-call re-seed, audit A3). Check: `grep -n "def transaction\|_save_unlocked" src/fanops/ledger.py && grep -ln "Ledger.transaction" src/fanops/cli.py`.

## 1. What this is

An autonomous fan-account engine for Moh Flow (bilingual EN/AR rapper). It ingests his videos, decides which moments are worth posting (transcript + audio/scene signals → an agent decision with a recorded reason), cuts platform-ready clips with agent-written EN/AR captions, cross-posts to every fan account × platform via Blotato (staggered for opsec, with a subtle non-synchronized artist @mention), then pulls real performance back to make more of what works. See `README.md` (front door), `MohFlow-FanOps/00_control/RUNTIME.md` (operating loop + production seams), `MohFlow-FanOps/00_control/RISK.md` (recorded operator risk-acceptance for the multi-account model), `MohFlow-FanOps/00_control/context.md` (functional — injected as agent guidance).

## 2. Architecture

- **Unit chain:** `Source → Moment → Clip → Post`, over one git-versioned JSON ledger (`00_control/ledger.json`, atomic temp+os.replace write under a file lock).
- **Identity:** content-addressed SHA-based ids everywhere (`ids.py`: `make_id`/`child_id`/`surface_key`) — NEVER builtin `hash()` (the #1 v1 bug; cross-process stability is proven by a subprocess idempotency test).
- **Agent gates:** generative steps (`decide_moments`, `write_captions`) cross a file contract in `04_agent_io/requests/` — code writes a `*.request.json` (stamped with a `request_id`), the agent/responder writes a validated `*.response.json` echoing that id, code resumes. Stale responses can never be applied (request_id correlation).
- **Two responders** (`responder.py`): `ManualResponder` (no-op; a human/cron writes responses) vs `LlmResponder` (`FANOPS_RESPONDER=llm`; **autonomous via `claude -p`** — default model shells the Claude Code CLI through `llm.claude_json` with committed `prompts.py` templates + the gate's pydantic JSON schema, validates against `MomentDecision`/`CaptionSet`, **per-request quarantine** so one bad/failed gate logs + stays pending without halting the rest). Needs `claude` on PATH (authenticated); model is injectable for tests.
- **Three posters** (`post/`): `dryrun` (default), `rest` (`BlotatoRestPoster`, retry/backoff/typed-errors), `mcp` (`BlotatoMcpPoster`, injected `tool_caller`).
- **Pipeline** (`pipeline.py` `advance()`): per-unit error quarantine (one bad source/moment/clip → `error` state, never wedges the pass). **CLI** (`cli.py`): `status/ingest/pull/advance/respond/track/adjust/gc/run/digest`.
- **State machines** (`models.py`): separate `SourceState`/`MomentState`/`ClipState`/`PostState`. `failed`≠`analyzed`; `held` (brand-risk) and `retired` are first-class.

## 3. Gotchas / non-obvious invariants

- **`--base-time` is the SCHEDULE ANCHOR, not the publish cutoff.** crosspost staggers posts to times AFTER base-time; `publish_due(now=None)` publishes whatever is due as of REAL wall-clock now. To publish in the same pass (backfill), pass a `--base-time` in the past. (This was a fixed plan contradiction.)
- **Conservative retirement:** `adjust` amplifies top `--winner-pct` (0.3) but retires only the bottom `--retire-pct` (0.2) AND below `--lift-floor` (20.0) — objectively-fine clips above the floor are never retired. Retiring a clip also retires its moment (so it can't be re-rendered).
- **Three surfaced-but-auto-unrecovered stuck states** (all in the digest + counts, no silent loss): a post stranded in `submitting` by a mid-publish crash; a post in **`needs_reconcile`** (REST hit an ambiguous 5xx/timeout after sending the body — it may be live, so it is NOT re-POSTed; Blotato has no idempotency key); a `published` post Blotato never returned metrics for. The first two need a poll step (`GET /v2/posts/:id` → promote/reset) — backlog item (a). `needs_reconcile` only ever arises under the `rest`/`mcp` backends, never `dryrun`.
- **`dryrun` posts are trackable** (audit C4): the dryrun poster stamps `submission_id = f"dryrun_<post_id>"`, so the `publish → track → analyzed → adjust` loop is fully exercisable offline by feeding `track` a metrics row keyed on that id. (CLI `track` still skips in dryrun — no live metrics source — but the seam is proven by `tests/integration/test_publish_track_loop.py`.)
- **Whisper model:** default `turbo`; falls back to the best cached `~/.cache/whisper/*.pt` when the requested model can't be downloaded (offline/proxied). In THIS env only `tiny` is cached.

## 4. Health checks

Run the §State Check commands. The integration E2E (`tests/integration/test_e2e_real.py`) is the golden path: real `say` TTS → real whisper transcript (asserts real whisper segment structure + ≥4 words + a vocoder-robust "anymore" anchor — CI-2) → real ffmpeg 1080×1920 vertical clip → 2 dryrun posts published. It pins the `tiny` whisper model in-test (no env var needed) and skips cleanly on a host with no cached checkpoint, so a fresh checkout / CI runner sees a clear skip rather than a cryptic `assert 0 == 1`.

## 5. Now (rewritten each session)

**As of 2026-06-04 (rev 23 — content discovery shipped; repo hygiene + the PR-#10 "why didn't it merge" clarified).**

**This turn (housekeeping, no code change):** deleted the orphan remote branch `feat-per-account-creative-variation` (it was the spawned-chip's branch for PR #9 — fully merged, so the leftover was just clutter; confirmed 0 unmerged commits before deleting). Removed my `/tmp` scratch demo dirs. **CLARIFIED for the record: PR #10 shows CLOSED-not-merged ON PURPOSE — it was a duplicate.** The creative-variation feature was built TWICE in parallel (the spawned chip → PR #9, which merged first; my inline workflow → PR #10), so #10 became a conflicting duplicate and I closed it. Nothing failed; the feature is on `main` via #9 (deviations PROCESS LESSON 4). The `passport scan.mp4` the operator asked about was a 14-byte synthetic test fixture I made in `/tmp` to prove the PII-name filter — NEVER a real file on the machine; all demos ran on synthetic temp media, no real content touched.

**Most recent shipped:** **Content discovery + folder-review intake — MERGED to `main` as `8c69b5b` (PR #12).** The operator's brief: scan a folder for content (cheap), choose yes/no the EASIEST way (a Finder folder), only "yes" enters the pipeline. Built: **`fanops discover <folder>`** scans for media (PII-named excluded via `is_excluded`), writes a **thumbnail + cheap metadata** (ffprobe + ONE ffmpeg frame — NO transcription/LLM = least cost; enforced by a workflow cost-guardrail gate) per candidate into **`00_review/`** + a `manifest.json`; the operator browses `00_review/` in Finder and **moves keeper thumbnails into `00_review/approved/`**; **`fanops intake`** copies the approved ORIGINALS into `01_inbox/` for the existing pipeline. Dedups vs ledger (`already_seen`); fail-soft/open; idempotent + missing-safe intake. Suite 351→**363, 1 skipped**; ruff clean; CI green. **PROVEN end-to-end on real content** (orchestrator verified, not just the workflow): a bank of 2 videos + a PII-named file + a non-media file → discover found exactly 2 (PII/non-media excluded), wrote VIEWABLE thumbnails (confirmed by viewing one); approved ONLY one → intake copied ONLY that original into `01_inbox/`, the non-approved one stayed out; 2nd intake idempotent. New module `discover.py`; spec+plan at `docs/superpowers/{specs,plans}/2026-06-04-content-discovery-review*.md`. See deviations §Content discovery.

**Prior shipped (rev-21):** **Responder-auth fix — MERGED as `98f7d7a` (PR #11).** Dropped `--bare` so the autonomous responder uses the operator's EXISTING `claude` subscription/login (NOT `ANTHROPIC_API_KEY`); preflight gates on `claude`-on-PATH; proven the full pipeline runs end-to-end on the subscription (the fix that turned the "nothing burger" on). See deviations §Responder auth.

**Prior shipped (rev-20):** **Per-account creative variation (backlog (j)) — MERGED as `01292cd` (PR #9).** With `FANOPS_CREATIVE_VARIATION=1` (default OFF, opt-in) each active account gets a genuinely different caption + burned-in on-screen hook for the same clip — the caption agent returns a per-surface `hook`, crosspost burns it onto the SHARED base clip via a cheap per-account overlay pass (`overlay.burn_hook_only`) and stamps a DETERMINISTIC `Post.variant_key` (= `surface_key`, never random/hash()) + `variant_hook`; the digest's "Lift by variant" section attributes which creative wins (OBSERVE-ONLY — touches NONE of amplify/classify_outcomes/_delete_moment_cascade, the C1-risk path). Fail-open everywhere. Suite **338→351, 1 skipped**; ruff clean; CI green (run `26945799108`). **Real two-account render PROVEN VISUALLY** — I rendered both accounts' clips and confirmed distinct burned hooks ("THEY SLEPT ON ME" vs "NOT ANYMORE") by viewing the frames directly. Spec + plan: `docs/superpowers/{specs,plans}/2026-06-04-per-account-creative-variation*.md`.

**TWO IMPORTANT EPISODE LESSONS (full detail in `fanops-build-deviations.md` §Per-account creative variation):**
- **DUPLICATE-WORK COLLISION (PROCESS LESSON 4):** this feature got built TWICE in parallel — once by a spawned-task chip (branch `feat-per-account-creative-variation` → PR #9, which MERGED first) and once by an orchestrator Workflow I ran (branch `feat-creative-variation` → PR #10). Both were functionally identical (same plan); the chip won the race, my PR #10 went CONFLICTING, and I correctly CLOSED #10 as a duplicate rather than force it. **Rule: a spawned chip and a same-session Workflow are TWO executors — pick ONE; treat an already-spawned chip as in-flight work.**
- **OCR-vs-VISUAL debugging catch:** during my render proof, tesseract OCR read NOTHING off the burned frames (every extraction strategy), nearly making me conclude libass was broken + rewrite the hook to `drawtext`. systematic-debugging + **viewing the PNG directly** (Read on the image) showed the text rendered PERFECTLY — the OCR was a flaky probe, not a render defect. **Rule: for "did the pixels render", VISUAL INSPECTION is ground truth; OCR is a flaky secondary probe.**

**What works right now (all on `main`):**
- **The entire live-autonomous plan (all 19 audit gaps) + the credential-free backlog tail + BOTH deferred features (burned-in subtitles PR #8, creative variation PR #9) + content-discovery/folder-review intake (PR #12) are CODE-COMPLETE.** Block-live findings closed; HIGH validation; observability; 4 recovery verbs; cutover-safety preflight; tunable HOLD gate + lift weights; media cap; duration clamp; configurable artist name; lint-clean dedup'd codebase; **on-screen subtitles + hook** burned into every clip (`FANOPS_BURN_SUBS`); **per-account A/B creative variation** (`FANOPS_CREATIVE_VARIATION`) with lift-by-variant attribution; and a **content-intake gate** — `fanops discover <folder>` (cheap thumbnails+metadata into `00_review/`) → operator moves keepers into `00_review/approved/` in Finder → `fanops intake` copies only the approved originals into `01_inbox/` (rejects never pipelined).
- **PRIME DIRECTIVE holds:** no path marks a maybe-live post `failed`. A shared per-clip media URL across accounts is intended (the variation feature ADDS per-account variant files only when explicitly enabled).

**Live state caveats:**
- **What remains to go LIVE is PURELY OPERATOR-GATED** (the G3 runbook in `RUNTIME.md`; the preflight hard-blocks the silent-misconfig traps): (1–3) create real fan accounts, connect in Blotato, paste numeric `account_id`s + `status: active`; (4) `BLOTATO_API_KEY` in `.env`; (5) **`claude login` on the host** (the responder uses the EXISTING subscription — NO `ANTHROPIC_API_KEY` needed, rev-21); (6) the `[GATED]` D5 Blotato smoke; (7) cron `fanops run` + the dead-man's-switch monitor. **The BRAIN is now unblocked (proven end-to-end on the subscription); the ONLY remaining gate is BLOTATO (posting).**
- **Blotato subscription NOT yet acquired (operator, 2026-06-04):** the connected MCP returns "Invalid API key or auth session" on any data call. The live-contract smoke (verifies `postSubmissionId`/media/analytics shapes against a REAL API vs mocks) is BLOCKED until a key + MCP re-auth. A live test post stays DEFERRED; `blotato_create_post` posts to a REAL account, NEVER fired autonomously.
- **ffmpeg is `ffmpeg-full` (local fix):** burned-in subtitles AND creative-variation hooks need libass text filters, which the default stripped `ffmpeg` lacked. If `ffmpeg` ever gets relinked to a stripped build, both FAIL OPEN (plain clip + warning). CI's Linux runner has full ffmpeg via apt.

**Open items, in priority order:**
1. **Operator-credential-gated (Blotato):** (a) get a **Blotato subscription/key** → then I run the live-contract smoke (read-only) + fix any field mismatch TDD; (b) the **live cutover** (G3 runbook).
2. **Remaining buildable-now backlog** (credential-free, no toolchain blocker — the operator deemed these LOW-VALUE): **trending-audio selection** (integration-gated — needs a trending-audio API), **daypart/timezone scheduling**, **per-surface best-window learning** (each needs a design pass + has limited value pre-live); (d) per-source ranking [low-stakes]. By-design "not built": multi-artist tenancy, richer secrets manager. 2 Minor test nits (the tail's `match="size"`; `responder_mode=="llm"` exact-string). Cosmetic: bump CI off deprecated Node-20 actions.

**Pick up here:**
- **`main` (`e36c0c3` + this handoff on top) is clean, in sync, no open PR, only the `main` branch exists remotely (orphan branches cleaned). ALL the high-value work the operator prioritized is DONE** — the plan, both deferred features, the responder-on-subscription fix, and the content-discovery intake gate. The remaining backlog (daypart/trending-audio/best-window/per-source-ranking) the operator explicitly deemed unimportant. The only high-value path left is the operator-credential-gated Blotato live smoke + cutover. (The full discover→intake→`run` dryrun-queue demo was OFFERED + awaits the operator's go — not auto-run.)
- **When the operator gets a Blotato key:** re-probe the MCP (`blotato_get_user` read-only) to confirm auth; if it returns real data, run the live-contract verification (diff against `_extract_submission_id`/media/analytics assumptions, fix any mismatch TDD). NEVER autonomously fire `blotato_create_post`.
- **Toolchain note:** subtitles + creative-variation hooks render real text ONLY if `ffmpeg` has libass (`subtitles`/`drawtext`). This machine is set (ffmpeg-full linked). Verify `ffmpeg -hide_banner -h filter=drawtext | head -1` (≠ "Unknown filter"); if a fresh ffmpeg lacks it, `brew install ffmpeg-full && brew link --overwrite --force ffmpeg-full`.
- **Standing process gotchas (durable multi-agent wisdom):** (a) a FRESH worktree needs its OWN editable venv (`pip install -e ".[dev]"`, **python3.12 not 3.14**; pulls `pytest-timeout` + `ruff`). (b) Verifying through the real CLI / scratch render: **`cd` to a scratch root first** (`Config()` resolves from cwd; no `FANOPS_ROOT`). (c) **Skeptics must NOT run `parallel()` against ONE shared worktree** — run them SEQUENTIALLY or give each `isolation: 'worktree'`. Always independently re-run the committed artifact before trusting a workflow's green. (d) **ONE executor per task** — don't run a spawned chip AND a same-session Workflow on the same plan (PROCESS LESSON 4: caused the duplicate PR #10). (e) `git diff <branch> main` before deleting any merged branch. (f) **VERIFY THE TOOLCHAIN before building a feature that depends on it.** (g) **For "did the pixels render", VIEW the image (Read the PNG) — OCR is a flaky probe.**
- Standing invariants: **Do not re-raise audit-C2 (byte-identical media)** — adjudicated; the *valuable* reframing (per-account creative variation) is now BUILT (PR #9). **Do NOT autonomously fire `blotato_create_post`.** **A fresh/local venv must `pip install -e ".[dev]"`** (python3.12).
