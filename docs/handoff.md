# HANDOFF — MOH FLOW FAN OPS

This file is the cross-session source of truth. It is **rewritten each session** (the §Now block is point-in-time, not an append-only log). Frozen provenance lives in `docs/archive/handoff-history.md` (created on first archive). The build plan is `docs/superpowers/plans/2026-05-31-fanops-real-build-v2.md`. Deviations from that plan made during the build are recorded in the auto-memory file `fanops-build-deviations.md`.

## §State (re-verify the Check commands before quoting)

- **Branch:** `main`. Check: `git branch --show-current`.
- **Working tree:** clean. Check: `git status -sb`.
- **HEAD:** `5a60c1b` (+ this handoff commit on top). Check: `git log --oneline -1`.
- **Remote:** `origin` → `github.com/Fleezyflo/fanops` (**PRIVATE**), `main` tracks `origin/main`. Pushed. Check: `git remote -v && gh repo view Fleezyflo/fanops --json isPrivate`.
- **Unit tests:** 219 passed, 5 deselected. Check: `source .venv/bin/activate && python -m pytest -q -m "not integration"`.
- **Integration tests:** 4 passed, 1 skipped (live Blotato smoke skips without creds; the publish→track→analyzed loop test, the real-tooling E2E, AND the amplify→ingest→real-render test all run). Check: `source .venv/bin/activate && python -m pytest -q -m integration`. (The E2E pins its own whisper model in-test — no `FANOPS_WHISPER_MODEL` needed; it skips cleanly if no checkpoint is cached.)
- **Module/test parity:** 32 src modules (incl. `reconcile.py` for H4), 35 test files. The integration *marker* now selects 5 tests: the 3 in `tests/integration/` (`test_e2e_real.py` ×2 — the transcript-drives-moment golden path + `test_blotato_smoke.py` creds-skip + `test_publish_track_loop.py`) PLUS `tests/test_adjust.py::test_amplify_then_ingest_then_render_produces_new_clip` (marked `integration` because it shells real ffmpeg — CI-1) — i.e. `-m integration --collect-only` = 5. Unit-suite additions this session: `test_e2e_transcript_assertion.py` (CI-2 vocoder guard) + the toolchain-absent tests in `test_ingest.py`/`test_signals.py`/`test_pipeline.py`/`test_cli.py`/`test_clip.py`/`test_transcribe.py`. Check: `ls src/fanops/*.py src/fanops/post/*.py | wc -l && ls tests/*.py tests/integration/*.py | wc -l` (32 / 35) and `python -m pytest -q -m integration --collect-only | grep -c "::"` (5).
- **Real tooling present:** ffmpeg 8.0.1, ffprobe, whisper CLI, `say` (macOS TTS) all on PATH; whisper `tiny.pt` cached (model downloads blocked by proxy in this env). The E2E pins `tiny` itself, so no env var is required; to override the model for a real `fanops` run, set `FANOPS_WHISPER_MODEL`. Check: `for b in ffmpeg ffprobe whisper say; do command -v $b; done`.
- **Posting backend default:** `dryrun` (writes payload JSON to `05_scheduled/`, posts nothing). Switch via `FANOPS_POSTER=rest|mcp` + `BLOTATO_API_KEY`. Check: `grep -n "FANOPS_POSTER" src/fanops/config.py`.
- **CI:** `.github/workflows/ci.yml` — unit job + an e2e job that installs the toolchain and runs the integration suite with `FANOPS_REQUIRE_E2E=1` (turns a tooling-absent skip into a failure, audit H10). **GREEN on GitHub** — the first two runs went RED (CI-1 ffmpeg-in-unit, CI-2 espeak-vs-`slept`); after the `dc646d1` fix, run `26744617793` = `completed success` (both jobs). The `679f089` toolchain-sweep run was in flight at handoff time — re-confirm it's green. (Non-fatal annotation on every run: "Node.js 20 actions are deprecated" for checkout/setup-python/cache@v4 — cosmetic, bump to newer action majors when convenient.) Check: `gh run list --repo Fleezyflo/fanops --limit 3`.
- **Toolchain-absent behavior:** every `subprocess.run` of a media binary (ffmpeg/ffprobe/whisper/yt-dlp) is guarded — `check=False` does NOT catch a pre-launch `FileNotFoundError`, so absent binaries used to crash. Now: pre-Source/one-shot sites (ffprobe at ingest, yt-dlp at `pull`) raise typed `errors.ToolchainMissingError` → `cli.main` exit 2 + "install …", no traceback; pipeline-quarantined sites (ffmpeg render, whisper transcribe, ffmpeg signals) record a retriable per-unit `error`. Check: `grep -rn "subprocess.run" src/fanops/ | grep -v "_run_ffprobe\|_run_ffmpeg\|try:"` (expect only the lines INSIDE the guards) and `grep -n "ToolchainMissingError" src/fanops/errors.py`.

## 1. What this is

An autonomous fan-account engine for Moh Flow (bilingual EN/AR rapper). It ingests his videos, decides which moments are worth posting (transcript + audio/scene signals → an agent decision with a recorded reason), cuts platform-ready clips with agent-written EN/AR captions, cross-posts to every fan account × platform via Blotato (staggered for opsec, with a subtle non-synchronized artist @mention), then pulls real performance back to make more of what works. See `README.md` (front door), `MohFlow-FanOps/00_control/RUNTIME.md` (operating loop + production seams), `MohFlow-FanOps/00_control/RISK.md` (recorded operator risk-acceptance for the multi-account model), `MohFlow-FanOps/00_control/context.md` (functional — injected as agent guidance).

## 2. Architecture

- **Unit chain:** `Source → Moment → Clip → Post`, over one git-versioned JSON ledger (`00_control/ledger.json`, atomic temp+os.replace write under a file lock).
- **Identity:** content-addressed SHA-based ids everywhere (`ids.py`: `make_id`/`child_id`/`surface_key`) — NEVER builtin `hash()` (the #1 v1 bug; cross-process stability is proven by a subprocess idempotency test).
- **Agent gates:** generative steps (`decide_moments`, `write_captions`) cross a file contract in `04_agent_io/requests/` — code writes a `*.request.json` (stamped with a `request_id`), the agent/responder writes a validated `*.response.json` echoing that id, code resumes. Stale responses can never be applied (request_id correlation).
- **Two responders** (`responder.py`): `ManualResponder` (no-op; a human/cron writes responses) vs `LlmResponder` (`FANOPS_RESPONDER=llm`; wraps an LLM call, validates against `MomentDecision`/`CaptionSet`).
- **Three posters** (`post/`): `dryrun` (default), `rest` (`BlotatoRestPoster`, retry/backoff/typed-errors), `mcp` (`BlotatoMcpPoster`, injected `tool_caller`).
- **Pipeline** (`pipeline.py` `advance()`): per-unit error quarantine (one bad source/moment/clip → `error` state, never wedges the pass). **CLI** (`cli.py`): `status/ingest/pull/advance/respond/track/adjust/gc/run/digest`.
- **State machines** (`models.py`): separate `SourceState`/`MomentState`/`ClipState`/`PostState`. `failed`≠`analyzed`; `held` (brand-risk) and `retired` are first-class.

## 3. Gotchas / non-obvious invariants

- **`--base-time` is the SCHEDULE ANCHOR, not the publish cutoff.** crosspost staggers posts to times AFTER base-time; `publish_due(now=None)` publishes whatever is due as of REAL wall-clock now. To publish in the same pass (backfill), pass a `--base-time` in the past. (This was a fixed plan contradiction.)
- **Conservative retirement:** `adjust` amplifies top `--winner-pct` (0.3) but retires only the bottom `--retire-pct` (0.2) AND below `--lift-floor` (20.0) — objectively-fine clips above the floor are never retired. Retiring a clip also retires its moment (so it can't be re-rendered).
- **Three surfaced-but-auto-unrecovered stuck states** (all in the digest + counts, no silent loss): a post stranded in `submitting` by a mid-publish crash; a post in **`needs_reconcile`** (REST hit an ambiguous 5xx/timeout after sending the body — it may be live, so it is NOT re-POSTed; Blotato has no idempotency key); a `published` post Blotato never returned metrics for. The first two need a poll step (`GET /v2/posts/:id` → promote/reset) — backlog item (a). `needs_reconcile` only ever arises under the `rest`/`mcp` backends, never `dryrun`.
- **`dryrun` posts are trackable** (audit C4): the dryrun poster stamps `submission_id = f"dryrun_<post_id>"`, so the `publish → track → analyzed → adjust` loop is fully exercisable offline by feeding `track` a metrics row keyed on that id. (CLI `track` still skips in dryrun — no live metrics source — but the seam is proven by `tests/integration/test_publish_track_loop.py`.)
- **Whisper model:** default `turbo`; falls back to the best cached `~/.cache/whisper/*.pt` when the requested model can't be downloaded (offline/proxied). In THIS env only `tiny` is cached.

## 4. Health checks

Run the §State Check commands. The integration E2E (`tests/integration/test_e2e_real.py`) is the golden path: real `say` TTS → real whisper transcript (asserts "slept") → real ffmpeg 1080×1920 vertical clip → 2 dryrun posts published. It pins the `tiny` whisper model in-test (no env var needed) and skips cleanly on a host with no cached checkpoint, so a fresh checkout / CI runner sees a clear skip rather than a cryptic `assert 0 == 1`.

## 5. Now (rewritten each session)

**As of 2026-06-01 (rev 8).**

**Most recent shipped (this session — got CI GREEN on GitHub for the first time + hardened every media-toolchain call site; all pushed to `origin/main`):**
- `dc646d1` — **CI red on `main` fixed (2 test-hygiene defects, both masked by macOS, TDD).** The H10 `ci.yml` ran on GitHub for the first time and went RED on both jobs — neither a product defect. **CI-1:** `tests/test_adjust.py::test_amplify_then_ingest_then_render` shelled real ffmpeg but sat in the no-toolchain `unit` job (`check=False` suppresses a nonzero rc, NOT the `FileNotFoundError` when the binary is absent) → marked `@pytest.mark.integration` (the repo's own marker = "hits real ffmpeg"); it now runs in the e2e job and genuinely renders. **CI-2:** the E2E asserted the literal word `"slept"`, which survives macOS `say` but not Linux espeak (whisper-tiny hears espeak as "Nice lap, Tommy, not anymore.") → replaced with `transcribe.real_transcript_signal()` (real whisper segments + ≥4 words) + a vocoder-robust "anymore" anchor; unit-guarded against BOTH engines' actual transcripts in `test_e2e_transcript_assertion.py`. **Verified: CI run `26744617793` = `completed success`, both jobs.**
- `bdc0c44` — **ffmpeg/whisper ABSENT → graceful per-unit error, not an uncaught crash (TDD).** `render_moment`/`transcribe_source` now catch a pre-launch `FileNotFoundError`/`OSError` → record `ClipState.error`/`SourceState.error` with "toolchain missing", leaving the unit retriable. Mutation-proven (reverted prod → both new tests fail with the exact uncaught error).
- `679f089` — **completed the toolchain-absent sweep (TDD).** A grep showed the gap was systematic — 5 sites. New typed `errors.ToolchainMissingError` for the pre-Source/one-shot sites that CRASH (`ffprobe` at ingest, `yt-dlp` at `pull`) → `cli.main` exit 2 + "install …", no traceback; the quarantined `ffmpeg`-in-signals site records a retriable `SourceState.error`. Real-CLI verified (after catching one false-green: venv-bin `yt-dlp` was on PATH so the binary wasn't actually absent — re-ran with `python` absolute + empty PATH → exit 2).

Tests green (re-derived this session): unit `219 passed, 5 deselected` (205→219, +14); integration `4 passed, 1 skipped` (the +1 vs last session's 3 is the adjust test now in the integration marker).

**What works right now:**
- Full pipeline runs (dryrun): ingest → transcribe → signals → moment gate → clip render → caption gate → crosspost → publish, pausing at each agent gate; per-unit quarantine inside the 3 explicit loops.
- **CI is real and GREEN:** the H10 `ci.yml` has actually executed on GitHub (Actions enabled) and passes both jobs — the unit job is genuinely hermetic again, the e2e job runs real ffmpeg/whisper/espeak with `FANOPS_REQUIRE_E2E=1` so a silent skip would fail.
- **Toolchain robustness is complete:** every ffmpeg/ffprobe/whisper/yt-dlp shell is guarded — an absent binary either halts cleanly (exit 2 + actionable message, ingest/pull) or degrades to a retriable per-unit error (render/transcribe/signals), never a raw traceback. (grep-confirmed zero unguarded `subprocess.run` of a media binary.)
- **The unattended path is hardened end-to-end:** orphaned lock self-heals (H6); a responder exception degrades cleanly (H7); auth failures halt-by-type with a clean message (H8); stranded posts auto-reconcile where the API allows (H4); staggering is monotonic + collision-free (H1/H2); the @mention window holds across re-tags (H3); the learning loop is dryrun-exercisable (C4) with pinned scoring (H9).
- **A shared per-clip media URL across accounts is intended** (C2): NOT a defect; do not "fix" it.
- Audit-confirmed solid (don't "fix"): SHA content-addressed ids, request_id agent correlation, atomic ledger writes, cascade-preserves-live-lineage, clean secret handling, F11.

**Live state caveats:** Still nothing run against REAL Blotato — `dryrun` default; `accounts.json` = `@TBD`. The INTEGRATION CHECKPOINTs (media-upload presign shape, metrics endpoint shape, reconcile `GET /v2/posts/:id` shape, MCP tool name) are UNVERIFIED against a live call (smoke test skips without creds). **No "Block LIVE" defect and no open audit-HIGH remain; CI is green.** The gate to a staging run is the operator-gated smoke test + the 3 human-only account steps. (CI annotation noise: "Node.js 20 actions are deprecated" on checkout/setup-python/cache@v4 — cosmetic.)

**Open items, in priority order:**
1. **Audit MEDIUM (the only open audit items left — these are now the top code work, both correctness, no creds; see `fanops-build-deviations.md` "Notable HIGH/MEDIUM"):** **M2** — `crosspost_clips`/`publish_due` run *outside* the per-unit quarantine in `pipeline.advance()` (lines 78/91), so one raise discards the whole pass's in-memory progress before `save()` (line 93) runs. Data-loss-shaped → higher value. **M1** — `reconcile_moments` un-retires a retired moment (undoes `adjust.retire`); guard the upsert against `MomentState.retired`.
2. **LIVE prerequisite, OPERATOR-GATED (needs creds):** run `tests/integration/test_blotato_smoke.py` against the Blotato sandbox (`BLOTATO_API_KEY`+`BLOTATO_SMOKE_ACCOUNT_ID`) — resolves the remaining unverified contracts at once.
3. **New feature (from C2 adjudication):** per-account creative variation for A/B content learning (hook/caption/edit variants per account/cohort, lift loop attributes the winner). Own spec → plan → build cycle.
4. **Deferred follow-ups (prior sessions):** per-request isolation inside `LlmResponder.answer_pending` (H7 — one bad gate halts the tick); the human-reconcile residue for id-less stranded posts (H4 — irreducible given the API).
5. **Pre-existing backlog** (`RUNTIME.md §Backlog`): REST backoff jitter; externalize brand-risk lists + lift weights; per-platform max-duration clamp; `fanops unhold`; `_parse`/`BASE_URL` triplication consolidation; per-source ranking; media size cap. (Also cosmetic: bump CI action majors off Node-20.)

**Pick up here:**
- **Recommended next code work (no creds needed): M2** — bring `crosspost_clips` + `publish_due` under the per-unit quarantine, OR checkpoint `led.save()` between units, in `pipeline.advance()` (the two calls at `src/fanops/pipeline.py:78` and `:91`, before the single `save()` at `:93`), so a mid-loop raise leaves already-processed units persisted. Same robustness family as H6/H7/this session's toolchain work. TDD: a mid-loop raise must leave already-processed units saved (a `save()`-spy or a forced raise on the Nth unit, asserting the first N-1 survive a reload). Then **M1** (`reconcile_moments` must not un-retire a `MomentState.retired` moment — guard the upsert). Both scoped in `fanops-build-deviations.md` ("Notable HIGH/MEDIUM").
- **Re-confirm the in-flight CI run** (`679f089` toolchain sweep) is green: `gh run list --repo Fleezyflo/fanops --limit 2`. The `dc646d1` run already passed, so the red-main problem is resolved; this is just confirming the latest push.
- **LIVE path is operator-gated, not agent-gated:** sandbox smoke test (item 2, needs creds) + the 3 human-only steps (create accounts → connect in Blotato → paste account_ids + set active; `RUNTIME.md` "three human-only steps").
- Standing: `main` pushed to private `origin` (Fleezyflo/fanops); working tree clean after the handoff commit. **Do not re-raise C2 from a future audit** — adjudicated, not missed. When verifying through the real CLI, **`cd` to the project root first** (`Config()` resolves the ledger from cwd; no `FANOPS_ROOT`) — and when a real-CLI check claims to test an absent tool, make sure the tool is *actually* off PATH (the venv bin carries `whisper`/`yt-dlp`; a leftover PATH entry caused a false-green this session). Audit workflow at `.claude/workflows/fanops-deep-audit.js` (tighten its agents to read-only before re-running).
