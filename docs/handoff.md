# HANDOFF — MOH FLOW FAN OPS

This file is the cross-session source of truth. It is **rewritten each session** (the §Now block is point-in-time, not an append-only log). Frozen provenance lives in `docs/archive/handoff-history.md` (created on first archive). The build plan is `docs/superpowers/plans/2026-05-31-fanops-real-build-v2.md`. Deviations from that plan made during the build are recorded in the auto-memory file `fanops-build-deviations.md`.

## §State (re-verify the Check commands before quoting)

- **Branch:** `main` (canonical line). Phases A, B, C, **and D are all MERGED into `main`** (PRs #1, #2, #4). **One open PR remains: #3 (Phase B follow-up, branch `phase-b-followup`)** — awaits human merge. Check: `git branch --show-current` / `gh pr list --repo Fleezyflo/fanops --state open` (expect only PR #3 OPEN).
- **Working tree:** clean (`## main...origin/main`). Check: `git status -sb`.
- **HEAD (of `main`):** `059b6c5` (code-review Minor #1: `_reconcile` 5xx-body id via the alias-aware extractor + plan D3-header fix) — on top of the Phase D merge `dabbce5`. Phase D code (D1 token, D2 extract, D3 MCP auth + fix, D4 jitter, reconcile resilience) is ON `main`. Check: `git log --oneline -1`.
- **Remote:** `origin` → `github.com/Fleezyflo/fanops` (**PRIVATE**). PR #1 (Phase B) MERGED 2026-06-01T17:38; PR #2 (Phase C) MERGED 2026-06-01T19:52 (`dc540ca`); **PR #4 (Phase D) MERGED 2026-06-01T21:58 (`dabbce5`), CI green**; PR #3 (Phase B follow-up) still OPEN. Check: `git remote -v && gh pr list --repo Fleezyflo/fanops --state open`.
- **Unit/full tests:** on `main` (post-Phase-D-merge + code-review fix): **272 passed, 1 skipped** (full; +14 over Phase C's 258 — the Phase D tests + the code-review `test_5xx_body_id_captured_via_alias_or_nested`). Check: `source .venv/bin/activate && python -m pytest -q`. The 1 skip = creds-gated Blotato smoke.
- **Integration tests:** 4 passed, 1 skipped (live Blotato smoke skips without creds) — same on `main` and the branch. Lock/deadlock tests run under `pytest-timeout` (global `timeout=60` in `pyproject` + a stricter `--timeout=20` for the lock tests); a hang is a hard FAIL.
- **Module/test parity:** 34 src modules (Phase A added `llm.py` + `prompts.py`), 37 test files (Phase A added `test_llm.py` + `test_prompts.py`). The integration *marker* selects 5 tests: the 3 in `tests/integration/` (`test_e2e_real.py` ×2 — the transcript-drives-moment golden path + `test_blotato_smoke.py` creds-skip + `test_publish_track_loop.py`) PLUS `tests/test_adjust.py::test_amplify_then_ingest_then_render_produces_new_clip` (marked `integration` because it shells real ffmpeg — CI-1) — i.e. `-m integration --collect-only` = 5. Check: `ls src/fanops/*.py src/fanops/post/*.py | wc -l && ls tests/*.py tests/integration/*.py | wc -l` (34 / 37) and `python -m pytest -q -m integration --collect-only | grep -c "::"` (5).
- **Real tooling present:** ffmpeg 8.0.1, ffprobe, whisper CLI, `say` (macOS TTS) all on PATH; whisper `tiny.pt` cached (model downloads blocked by proxy in this env). The E2E pins `tiny` itself, so no env var is required; to override the model for a real `fanops` run, set `FANOPS_WHISPER_MODEL`. Check: `for b in ffmpeg ffprobe whisper say; do command -v $b; done`.
- **Posting backend default:** `dryrun` (writes payload JSON to `05_scheduled/`, posts nothing). Switch via `FANOPS_POSTER=rest|mcp` + `BLOTATO_API_KEY`. Check: `grep -n "FANOPS_POSTER" src/fanops/config.py`.
- **CI:** `.github/workflows/ci.yml` — `unit` job (no toolchain) + `e2e` job (installs toolchain, `FANOPS_REQUIRE_E2E=1`, audit H10). Runs on push to `main` AND on `pull_request` into `main`. **Phase D merge `dabbce5` CI green (run `26784568446`); the code-review follow-up `059b6c5` CI green (run `26803371692` = `completed success`, both jobs incl. real-tooling E2E, watched + re-queried).** Check: `gh run list --repo Fleezyflo/fanops --branch main --limit 3`. (Non-fatal annotation: "Node.js 20 actions are deprecated" — cosmetic.)
- **Toolchain-absent behavior:** every `subprocess.run` of a media binary (ffmpeg/ffprobe/whisper/yt-dlp) is guarded — `check=False` does NOT catch a pre-launch `FileNotFoundError`, so absent binaries used to crash. Now: pre-Source/one-shot sites (ffprobe at ingest, yt-dlp at `pull`) raise typed `errors.ToolchainMissingError` → `cli.main` exit 2 + "install …", no traceback; pipeline-quarantined sites (ffmpeg render, whisper transcribe, ffmpeg signals) record a retriable per-unit `error`. Check: `grep -rn "subprocess.run" src/fanops/ | grep -v "_run_ffprobe\|_run_ffmpeg\|try:"` (expect only the lines INSIDE the guards) and `grep -n "ToolchainMissingError" src/fanops/errors.py`.
- **Concurrency (Phase B):** `advance()` runs the WHOLE load→mutate→save inside ONE `Ledger.transaction(cfg)` holding the `fcntl.flock` — acquired BEFORE load, single atomic save on clean exit (audit B4). A 2nd live run is excluded with a typed `LockBusyError`. `publish_due(in_transaction=True)` uses the unlocked save (no self-deadlock); crosspost+publish are wrapped (log+continue) so a non-auth raise can't roll back the pass, while a fatal `BlotatoAuthError` still escapes (F52). The slow `claude -p` responder runs OUTSIDE the lock (`request_id` correlation + capture-and-recheck against a mid-call re-seed, audit A3). Check: `grep -n "def transaction\|_save_unlocked" src/fanops/ledger.py && grep -n "rid_before\|rid_after" src/fanops/responder.py`.

## 1. What this is

An autonomous fan-account engine for Moh Flow (bilingual EN/AR rapper). It ingests his videos, decides which moments are worth posting (transcript + audio/scene signals → an agent decision with a recorded reason), cuts platform-ready clips with agent-written EN/AR captions, cross-posts to every fan account × platform via Blotato (staggered for opsec, with a subtle non-synchronized artist @mention), then pulls real performance back to make more of what works. See `README.md` (front door), `MohFlow-FanOps/00_control/RUNTIME.md` (operating loop + production seams), `MohFlow-FanOps/00_control/RISK.md` (recorded operator risk-acceptance for the multi-account model), `MohFlow-FanOps/00_control/context.md` (functional — injected as agent guidance).

## 2. Architecture

- **Unit chain:** `Source → Moment → Clip → Post`, over one git-versioned JSON ledger (`00_control/ledger.json`, atomic temp+os.replace write under a file lock).
- **Identity:** content-addressed SHA-based ids everywhere (`ids.py`: `make_id`/`child_id`/`surface_key`) — NEVER builtin `hash()` (the #1 v1 bug; cross-process stability is proven by a subprocess idempotency test).
- **Agent gates:** generative steps (`decide_moments`, `write_captions`) cross a file contract in `04_agent_io/requests/` — code writes a `*.request.json` (stamped with a `request_id`), the agent/responder writes a validated `*.response.json` echoing that id, code resumes. Stale responses can never be applied (request_id correlation).
- **Two responders** (`responder.py`): `ManualResponder` (no-op; a human/cron writes responses) vs `LlmResponder` (`FANOPS_RESPONDER=llm`; **autonomous via `claude -p`** — default model shells the Claude Code CLI through `llm.claude_json` with committed `prompts.py` templates + the gate's pydantic JSON schema, validates against `MomentDecision`/`CaptionSet`, **per-request quarantine** so one bad/failed gate logs + stays pending without halting the rest). Needs `claude` on PATH (authenticated); model is injectable for tests.
- **Three posters** (`post/`): `dryrun` (default), `rest` (`BlotatoRestPoster`, retry/backoff/typed-errors), `mcp` (`BlotatoMcpPoster`, injected `tool_caller`).
- **Pipeline** (`pipeline.py` `advance()`): per-unit error quarantine (one bad source/moment/clip → `error` state, never wedges the pass). **CLI** (`cli.py`): `status/ingest/pull/advance/respond/track/adjust/gc/run/digest`.
- **State machines** (`models.py`): separate `SourceState`/`MomentState`/`ClipState`/`PostState`. `failed`≠`analyzed`; `held` (brand-risk) and `retired` are first-class.

## 3. Gotchas / non-obvious invariants

- **`--base-time` is the SCHEDULE ANCHOR, not the publish cutoff.** crosspost staggers posts to times AFTER base-time; `publish_due(now=None)` publishes whatever is due as of REAL wall-clock now. To publish in the same pass (backfill), pass a `--base-time` in the past. (This was a fixed plan contradiction.)
- **Conservative retirement:** `adjust` amplifies top `--winner-pct` (0.3) but retires only the bottom `--retire-pct` (0.2) AND below `--lift-floor` (20.0) — objectively-fine clips above the floor are never retired. Retiring a clip also retires its moment (so it can't be re-rendered).
- **Three surfaced-but-auto-unrecovered stuck states** (all in the digest + counts, no silent loss): a post stranded in `submitting` by a mid-publish crash; a post in **`needs_reconcile`** (REST hit an ambiguous 5xx/timeout after sending the body — it may be live, so it is NOT re-POSTed; Blotato has no idempotency key); a `published` post Blotato never returned metrics for. The first two need a poll step (`GET /v2/posts/:id` → promote/reset) — backlog item (a). `needs_reconcile` only ever arises under the `rest`/`mcp` backends, never `dryrun`.
- **`dryrun` posts are trackable** (audit C4): the dryrun poster stamps `submission_id = f"dryrun_<post_id>"`, so the `publish → track → analyzed → adjust` loop is fully exercisable offline by feeding `track` a metrics row keyed on that id. (CLI `track` still skips in dryrun — no live metrics source — but the seam is proven by `tests/integration/test_publish_track_loop.py`.)
- **Whisper model:** default `turbo`; falls back to the best cached `~/.cache/whisper/*.pt` when the requested model can't be downloaded (offline/proxied). In THIS env only `tiny` is cached.

## 4. Health checks

Run the §State Check commands. The integration E2E (`tests/integration/test_e2e_real.py`) is the golden path: real `say` TTS → real whisper transcript (asserts real whisper segment structure + ≥4 words + a vocoder-robust "anymore" anchor — CI-2) → real ffmpeg 1080×1920 vertical clip → 2 dryrun posts published. It pins the `tiny` whisper model in-test (no env var needed) and skips cleanly on a host with no cached checkpoint, so a fresh checkout / CI runner sees a clear skip rather than a cryptic `assert 0 == 1`.

## 5. Now (rewritten each session)

**As of 2026-06-02 (rev 12).**

**Most recent shipped (this session — Phase D of the live+autonomous plan: the live Blotato seam. MERGED to `main` via PR #4, merge commit `dabbce5`, 2026-06-01T21:58; CI green on the merge; the human merged it. Closes the audit's #3 + #4 block-live findings):**
- `21378b0` — **D1 (audit H1): client idempotency token at crosspost.** Every Post is stamped `submission_id=f"fanops_{_hash('idemp', pid)}"` at construction (added `_hash` to crosspost's `fanops.ids` import), so a crash/timeout never strands an id-less maybe-live post. `blotato_rest._reconcile` guard changed (`if resp is not None` alone) so a real body `postSubmissionId` OVERWRITES the token.
- `074d76e` — **D2 (audit B2): robust 2xx submission-id extraction.** Shared `_extract_submission_id` (postSubmissionId | submissionId | id + nested `data`); a 2xx with NO recognizable id → `needs_reconcile` (may be live), NEVER `failed`. Rewrote the test that encoded the old `failed` behaviour.
- `ec73e08` — **D3 (audit B3): MCP poster auth + error mapping.** Auth-class failures → `BlotatoAuthError`; non-auth → per-post `needs_reconcile` (not raw-raise, not failed); imports (not copies) D2's helper.
- `6c7d145` — **D4: jittered 429 backoff** (`time.sleep(delay + random.uniform(0, delay)); delay *= 2`), still bounded by `_MAX_RETRIES`.
- `2472c24` — **D3 ADVERSARIAL REMEDIATION** (both skeptics REFUTED D3; Workflow correctly BLOCKED rather than green-wash): the broad `except Exception` swallowed a caller-raised typed `BlotatoAuthError` (and reworded auth messages the 6-substring net misses — even "BLOTATO_API_KEY missing", since `'api key' != 'blotato_api_key'`), defeating run.py's halt-by-type. Fix: `except BlotatoAuthError: raise` FIRST, before the substring net. Mutation-proven two ways.
- `7ace060` — **reconcile per-post resilience (D1 fallout).** D1's token made `reconcile_posts` poll every parked post; a `fanops_` token 404s and the uncaught raise aborted the whole pass (order-dependent: stranded every published post later in iteration). Now contains the per-post error (parked, never failed); `BlotatoAuthError` still halts. Surfaced by a skeptic, independently re-verified + mutation-proven by me, committed with provenance.
- `4c0a686` — **docs sync** (README/RUNTIME + module docstrings): 2xx/MCP-no-id `failed`→`needs_reconcile`; D4 jitter; D5 live-verified contracts; D1 token + reconcile refinement.

Enforcement: dedicated worktree `/Users/molhamhomsi/fanops-phase-d` (own venv, **python3.12.8** — default `python3` is 3.14.5, out of the `<3.14` range; `import fanops` verified into the worktree). Driven through the **Workflow** engine (7 phases: Preflight→Frame→Implement→Verify→Adversarial→Integrate+D5→Close), structured-output schemas, independent verifier + ≥2 independent skeptics per task (majority vote), mutation proof per new test (serialized destructive git ops). Run 1 `wf_3d83c491-e2c` (16 agents) BLOCKED on D3; I root-caused + fixed (`2472c24`); re-confirm `wf_fdcf6b50-661` (4 agents) confirmed D3 2/2. Full suite **258→271 passed, 1 skipped** (now on `main`). CI run `26784233917` (PR) + `26784568446` (merge `dabbce5`) both = `completed success` (both jobs).

**What works right now (on `main`):**
- **Every crossposted Post is reconcilable from birth (H1 closed):** a stable `fanops_<hash>` `submission_id` is stamped before any network call; a crash/timeout no longer strands an id-less post. A real Blotato `postSubmissionId` overwrites the token.
- **A successful live post is never mis-marked `failed` (B2 closed):** a 2xx with no recognizable id → `needs_reconcile` (parked, surfaced, never auto-requeued); the id is matched across `postSubmissionId`/`submissionId`/`id` (+ nested `data`).
- **The MCP poster honors the auth-halt contract (B3 closed):** a typed `BlotatoAuthError` propagates so `run.py` halts the queue by type; non-auth errors park `needs_reconcile`; untyped auth messages are substring-matched as a fallback.
- **429 backoff is jittered (no thundering herd), still bounded.**
- **reconcile survives a per-post poll error:** one post's 404 no longer aborts the whole pass; `BlotatoAuthError` still halts.
- **PRIME DIRECTIVE holds:** NO path (REST 2xx-no-id / 5xx / network / MCP non-auth / MCP no-id) marks a maybe-live post `failed` — the only REST `failed` is exhausted-429 (rejected pre-processing = safe re-queue). `run.py` re-drives only `queued`.
- Everything Phase A/B/C still holds (autonomous `claude -p` + per-request quarantine; B4 transaction lock; H4/H5/H6 validation). A shared per-clip media URL across accounts is intended — do NOT "fix" it.

**Live state caveats:**
- **Phase D is MERGED to `main` (`dabbce5`); the merge-commit CI is green.** One open PR remains: #3 (Phase B follow-up, `phase-b-followup`) — awaits the human's merge. The Phase D worktree `/Users/molhamhomsi/fanops-phase-d` + branch `phase-d-blotato` are still on disk (cleanup pending — see Pick up here).
- **D5 live verification was READ-ONLY against the connected MCP's tool SCHEMAS, not a data call.** A Blotato MCP server connected this session; its tool schemas confirmed the contract (postSubmissionId field, status enum, `publicUrl`-on-get_post_status vs `postUrl`-on-list_posts split, presign keys, per-platform fields — all MATCH; the `postUrl`/`publicUrl` divergence is reconciled: FanOps reads the URL only from `get_post_status` so it reads the right key — no live bug). BUT a data-returning call (`blotato_get_user`/`list_accounts`) returns **"Invalid API key or auth session"** — connected but unauthenticated. So a **live test post is DEFERRED** pending valid Blotato auth + an operator-named throwaway account (`blotato_create_post` posts to a REAL account, no dry-run — never fired autonomously). See `[[blotato-mcp-not-reachable-headless]]`.
- **The metrics ENGAGEMENT-FIELDS checkpoint honestly remains** (the endpoint + postSubmissionId key are D5-verified; which fields Blotato's analytics exposes is not — only confirmable with a live authed call / a real published post).
- **`claude -p` still needs `ANTHROPIC_API_KEY`** in the `fanops` env (`--bare` ignores OAuth/keychain). Unchanged Phase-A/B deploy caveat.

**Open items, in priority order:**
1. **Phase E — learning-loop-in-cron + dead-man's-switch (THE NEXT PHASE; the last 2 block-live findings).** #2: `advance()`/`run` never call `pull_metrics`/`classify_outcomes`/`amplify`/`retire` → the learning loop is not autonomous. #6: no dead-man's-switch (`run` exits 0 whether healthy-idle or silently-dead). Both credential-free. Plan: `docs/superpowers/plans/2026-06-01-fanops-live-autonomous.md` Phase E. **Fold in:** the bounded-retry note (`run` retries a bad gate up to `range(10)`/invocation, no backoff) + a startup preflight warning when `claude` is present but `ANTHROPIC_API_KEY` is unset.
2. **Worktree cleanup:** `git worktree remove /Users/molhamhomsi/fanops-phase-d` + delete branch `phase-d-blotato` (local + `origin`) — Phase D is merged. (Human may want to do this on the GitHub side too.)
3. **PR #3 (Phase B follow-up) still open** — the human owns the merge; transaction-wraps the standalone CLI write commands (B4 everywhere). Independent of Phase D/E.
4. **LIVE Blotato verification, OPERATOR-GATED:** a data-returning MCP call + a live test post need valid Blotato auth + an operator-named throwaway account. Then the metrics-fields checkpoint can close.
4. **`claude -p` live answer (operator-gated):** in an `ANTHROPIC_API_KEY`-set env, drive one real gate end-to-end (plan A4 recipe).
5. **Phase F** (operator-recovery verbs). **New feature (audit-C2 adjudication):** per-account creative variation (own spec→plan→build).
6. **Pre-existing backlog** (`RUNTIME.md §Backlog`): externalize brand-risk lists + lift weights; per-platform max-duration clamp; `fanops unhold`; `_parse`/`BASE_URL` triplication; per-source ranking; media size cap; C2 caption language *detection*. (Done this phase: 429-jitter.) (Cosmetic: bump CI actions off Node-20.)

**Pick up here:**
- **Start Phase E** of `docs/superpowers/plans/2026-06-01-fanops-live-autonomous.md` (learning-loop-in-cron + dead-man's-switch — the last 2 block-live findings) on a FRESH worktree off `main` (`dabbce5`), same Workflow-enforced loop (Frame→Implement→Verify→Adversarial→Integrate→Close). E1 first: fold track→adjust into the cron `run` pass (guarded like reconcile — only fires with a live backend + key) + bound amplification per source (`adjust.py` cooldown/budget so an autonomous LLM can't drive unbounded clip growth). E2: the dead-man's-switch (`run` must distinguish healthy-idle from silently-dead). Both credential-free.
- **Phase D worktree + branch already cleaned up** (worktree removed; `phase-d-blotato` deleted local + `origin`, after confirming it's an ancestor of `main`). Phase D workflow scripts live on `main` at `.claude/workflows/fanops-phase-d.js` + `fanops-phase-d-confirm.js`. Only the `phase-b-followup` worktree remains (PR #3 open).
- **LIVE Blotato is operator-gated:** the MCP is connected but unauthenticated this session — a live test post needs valid auth + an operator-named throwaway account (NEVER autonomous; `blotato_create_post` posts to a real account, no dry-run). The 3 human-only steps still apply (create accounts → connect in Blotato → paste account_ids + set active; `RUNTIME.md`).
- Standing: `main` at `dabbce5`, in sync with `origin/main`; PR #3 (Phase B follow-up) open. **Do not re-raise audit-C2 (byte-identical media)** — adjudicated. **Do NOT autonomously fire `blotato_create_post`** — it posts to a real account. When verifying through the real CLI, **`cd` to the project/scratch root first** (`Config()` resolves the ledger from cwd; no `FANOPS_ROOT`); a non-held clip ADVANCES past `captioned` to `queued`. A fresh worktree needs its OWN editable venv (`pip install -e ".[dev]"`, python3.12 not 3.14) — symlinking the parent `.venv` tests the WRONG src tree. **The Workflow JS parser rejects `arr.map((x,i)=>()=>agent(...))` with a `${x}` interpolation inside the agent prompt** — use explicit `parallel([()=>agent(plainStringFn(a)), ...])` with string-concatenation prompts (no escaped backticks in template literals). **Audit/skeptic agents can leave the worktree modified** (a D1 skeptic implemented the reconcile fix mid-run) — always `git status` + independently verify/mutation-prove before trusting or committing their work.
