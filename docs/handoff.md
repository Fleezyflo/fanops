# HANDOFF — MOH FLOW FAN OPS

This file is the cross-session source of truth. It is **rewritten each session** (the §Now block is point-in-time, not an append-only log). Frozen provenance lives in `docs/archive/handoff-history.md` (created on first archive). The build plan is `docs/superpowers/plans/2026-05-31-fanops-real-build-v2.md`. Deviations from that plan made during the build are recorded in the auto-memory file `fanops-build-deviations.md`.

## §State (re-verify the Check commands before quoting)

- **Branch:** `main` (canonical line). Phases A, B, C, D, E + the Phase B follow-up + **Phase F + the G finale are ALL MERGED into `main` (PRs #1–#6).** **NO open PRs; the entire `docs/superpowers/plans/2026-06-01-fanops-live-autonomous.md` plan is COMPLETE.** The `phase-f` branch + its worktree have been deleted (local + remote). Check: `git branch --show-current` / `gh pr list --repo Fleezyflo/fanops --state open` (expect empty).
- **Working tree:** clean, in sync with origin. Check: `git status -sb` (expect `## main...origin/main` with nothing below).
- **HEAD (of `main`):** `ef3e5d0` (Merge pull request #6 from Fleezyflo/phase-f) → this rev-17 handoff commit lands on top. ALL of A–F are on `main`. Check: `git log --oneline -1`.
- **Remote:** `origin` → `github.com/Fleezyflo/fanops` (**PRIVATE**). PRs #1–#6 ALL MERGED; no open PRs. Check: `git remote -v && gh pr list --repo Fleezyflo/fanops --state open`.
- **Unit/full tests:** on `main`: **299 passed, 1 skipped** (Phase F added +8 = 4 F-task tests + 4 mutation-proven hardening tests, all now on `main`). Check: `source .venv/bin/activate && python -m pytest -q`. The 1 skip = creds-gated Blotato smoke. NOTE: dev-deps include **`pytest-timeout`** + a global `timeout=60` in `pyproject`; a fresh/local venv must `pip install -e ".[dev]"` (python3.12) or pytest warns "Unknown config option: timeout".
- **Integration tests:** 4 passed, 1 skipped (live Blotato smoke skips without creds) — same on `main` and the branch. Lock/deadlock tests run under `pytest-timeout` (global `timeout=60` in `pyproject` + a stricter `--timeout=20` for the lock tests); a hang is a hard FAIL.
- **Module/test parity:** 34 src modules (Phase A added `llm.py` + `prompts.py`), 37 test files (Phase A added `test_llm.py` + `test_prompts.py`). The integration *marker* selects 5 tests: the 3 in `tests/integration/` (`test_e2e_real.py` ×2 — the transcript-drives-moment golden path + `test_blotato_smoke.py` creds-skip + `test_publish_track_loop.py`) PLUS `tests/test_adjust.py::test_amplify_then_ingest_then_render_produces_new_clip` (marked `integration` because it shells real ffmpeg — CI-1) — i.e. `-m integration --collect-only` = 5. Check: `ls src/fanops/*.py src/fanops/post/*.py | wc -l && ls tests/*.py tests/integration/*.py | wc -l` (34 / 37) and `python -m pytest -q -m integration --collect-only | grep -c "::"` (5).
- **Real tooling present:** ffmpeg 8.0.1, ffprobe, whisper CLI, `say` (macOS TTS) all on PATH; whisper `tiny.pt` cached (model downloads blocked by proxy in this env). The E2E pins `tiny` itself, so no env var is required; to override the model for a real `fanops` run, set `FANOPS_WHISPER_MODEL`. Check: `for b in ffmpeg ffprobe whisper say; do command -v $b; done`.
- **Posting backend default:** `dryrun` (writes payload JSON to `05_scheduled/`, posts nothing). Switch via `FANOPS_POSTER=rest|mcp` + `BLOTATO_API_KEY`. Check: `grep -n "FANOPS_POSTER" src/fanops/config.py`.
- **CI:** `.github/workflows/ci.yml` — `unit` job (no toolchain) + `e2e` job (installs toolchain, `FANOPS_REQUIRE_E2E=1`, audit H10). Runs on push to `main` AND on `pull_request` into `main`. **`main` CI GREEN on the merge commit: run `26847493387` at `ef3e5d0` = `completed success`, both jobs (`unit` + real-tooling `e2e` with FANOPS_REQUIRE_E2E so it genuinely ran), watched to completion (`WATCH_EXIT=0`).** Check: `gh run list --repo Fleezyflo/fanops --branch main --limit 2`. (Non-fatal annotation: "Node.js 20 actions are deprecated" — cosmetic; backlog to bump the action versions.)
- **Toolchain-absent behavior:** every `subprocess.run` of a media binary (ffmpeg/ffprobe/whisper/yt-dlp) is guarded — `check=False` does NOT catch a pre-launch `FileNotFoundError`, so absent binaries used to crash. Now: pre-Source/one-shot sites (ffprobe at ingest, yt-dlp at `pull`) raise typed `errors.ToolchainMissingError` → `cli.main` exit 2 + "install …", no traceback; pipeline-quarantined sites (ffmpeg render, whisper transcribe, ffmpeg signals) record a retriable per-unit `error`. Check: `grep -rn "subprocess.run" src/fanops/ | grep -v "_run_ffprobe\|_run_ffmpeg\|try:"` (expect only the lines INSIDE the guards) and `grep -n "ToolchainMissingError" src/fanops/errors.py`.
- **Concurrency (Phase B + follow-up):** `advance()` AND **every standalone write command** (`track`, `reconcile`, `adjust`, `ingest`, `pull`) run the WHOLE load→mutate→save inside ONE `Ledger.transaction(cfg)` holding the `fcntl.flock` — acquired BEFORE load, single atomic save on clean exit (audit B4, now everywhere). A 2nd live run is excluded with a typed `LockBusyError`. Slow I/O stays OUTSIDE the lock: `publish_due(in_transaction=True)` uses the unlocked save; `track`'s metrics fetch + `reconcile`'s per-post polls + `pull`'s yt-dlp download all run BEFORE the transaction (only the in-memory apply is locked) — so a 30s network call never serializes behind the ledger lock. crosspost+publish are wrapped (log+continue) so a non-auth raise can't roll back the pass, while a fatal `BlotatoAuthError` still escapes (F52). The slow `claude -p` responder runs OUTSIDE the lock (`request_id` correlation + capture-and-recheck against a mid-call re-seed, audit A3). Check: `grep -n "def transaction\|_save_unlocked" src/fanops/ledger.py && grep -ln "Ledger.transaction" src/fanops/cli.py`.

## 1. What this is

An autonomous fan-account engine for Moh Flow (bilingual EN/AR rapper). It ingests his videos, decides which moments are worth posting (transcript + audio/scene signals → an agent decision with a recorded reason), cuts platform-ready clips with agent-written EN/AR captions, cross-posts to every fan account × platform via Blotato (staggered for opsec, with a subtle non-synchronized artist @mention), then pulls real performance back to make more of what works. See `README.md` (front door), `MohFlow-FanOps/00_control/RUNTIME.md` (operating loop + production seams), `MohFlow-FanOps/00_control/RISK.md` (recorded operator risk-acceptance for the multi-account model), `MohFlow-FanOps/00_control/context.md` (functional — injected as agent guidance).

## 2. Architecture

- **Unit chain:** `Source → Moment → Clip → Post`, over one git-versioned JSON ledger (`00_control/ledger.json`, atomic temp+os.replace write under a file lock).
- **Identity:** content-addressed SHA-based ids everywhere (`ids.py`: `make_id`/`child_id`/`surface_key`) — NEVER builtin `hash()` (the #1 v1 bug; cross-process stability is proven by a subprocess idempotency test).
- **Agent gates:** generative steps (`decide_moments`, `write_captions`) cross a file contract in `04_agent_io/requests/` — code writes a `*.request.json` (stamped with a `request_id`), the agent/responder writes a validated `*.response.json` echoing that id, code resumes. Stale responses can never be applied (request_id correlation).
- **Two responders** (`responder.py`): `ManualResponder` (no-op; a human/cron writes responses) vs `LlmResponder` (`FANOPS_RESPONDER=llm`; **autonomous via `claude -p`** — default model shells the Claude Code CLI through `llm.claude_json` with committed `prompts.py` templates + the gate's pydantic JSON schema, validates against `MomentDecision`/`CaptionSet`, **per-request quarantine** so one bad/failed gate logs + stays pending without halting the rest). Needs `claude` on PATH (authenticated); model is injectable for tests.
- **Three posters** (`post/`): `dryrun` (default), `rest` (`BlotatoRestPoster`, retry/backoff/typed-errors), `mcp` (`BlotatoMcpPoster`, injected `tool_caller`).
- **Pipeline** (`pipeline.py` `advance()`): per-unit error quarantine (one bad source/moment/clip → `error` state, never wedges the pass). **CLI** (`cli.py`): `status/ingest/pull/advance/respond/track/adjust/gc/run/digest`.
- **State machines** (`models.py`): separate `SourceState`/`MomentState`/`ClipState`/`PostState`. `failed`≠`analyzed`; `held` (brand-risk) and `retired` are first-class.

## 3. Gotchas / non-obvious invariants

- **`--base-time` is the SCHEDULE ANCHOR, not the publish cutoff.** crosspost staggers posts to times AFTER base-time; `publish_due(now=None)` publishes whatever is due as of REAL wall-clock now. To publish in the same pass (backfill), pass a `--base-time` in the past. (This was a fixed plan contradiction.)
- **Conservative retirement:** `adjust` amplifies top `--winner-pct` (0.3) but retires only the bottom `--retire-pct` (0.2) AND below `--lift-floor` (20.0) — objectively-fine clips above the floor are never retired. Retiring a clip also retires its moment (so it can't be re-rendered).
- **Three surfaced-but-auto-unrecovered stuck states** (all in the digest + counts, no silent loss): a post stranded in `submitting` by a mid-publish crash; a post in **`needs_reconcile`** (REST hit an ambiguous 5xx/timeout after sending the body — it may be live, so it is NOT re-POSTed; Blotato has no idempotency key); a `published` post Blotato never returned metrics for. The first two need a poll step (`GET /v2/posts/:id` → promote/reset) — backlog item (a). `needs_reconcile` only ever arises under the `rest`/`mcp` backends, never `dryrun`.
- **`dryrun` posts are trackable** (audit C4): the dryrun poster stamps `submission_id = f"dryrun_<post_id>"`, so the `publish → track → analyzed → adjust` loop is fully exercisable offline by feeding `track` a metrics row keyed on that id. (CLI `track` still skips in dryrun — no live metrics source — but the seam is proven by `tests/integration/test_publish_track_loop.py`.)
- **Whisper model:** default `turbo`; falls back to the best cached `~/.cache/whisper/*.pt` when the requested model can't be downloaded (offline/proxied). In THIS env only `tiny` is cached.

## 4. Health checks

Run the §State Check commands. The integration E2E (`tests/integration/test_e2e_real.py`) is the golden path: real `say` TTS → real whisper transcript (asserts real whisper segment structure + ≥4 words + a vocoder-robust "anymore" anchor — CI-2) → real ffmpeg 1080×1920 vertical clip → 2 dryrun posts published. It pins the `tiny` whisper model in-test (no env var needed) and skips cleanly on a host with no cached checkpoint, so a fresh checkout / CI runner sees a clear skip rather than a cryptic `assert 0 == 1`.

## 5. Now (rewritten each session)

**As of 2026-06-02 (rev 17 — Phase F + finale MERGED; the plan is COMPLETE and on `main`).**

**Most recent shipped (this session):** **Phase F + the G finale — the LAST phase of `docs/superpowers/plans/2026-06-01-fanops-live-autonomous.md` — is now MERGED to `main` as `ef3e5d0` (Merge pull request #6 from Fleezyflo/phase-f). With it, all 19 audit gap items are closed and the entire plan is DONE.** Delivered as a deterministic 7-phase `Workflow` (`wf_5183eba2-c0f`, script at `.claude/workflows/fanops-phase-f.js`), then code-reviewed clean (0 Critical / 0 Important) and merged. Suite **291→299, 1 skipped**; `main` CI green on the merge commit (run `26847493387`, both jobs, E2E genuinely ran, `WATCH_EXIT=0`). The branch + worktree were deleted after a `git diff phase-f main` confirmed nothing was branch-only. The verb commits + PII fix + hardening + docs + review fix (all on `main`):
- `5660dec` — **F1 (audit H1): `fanops resolve <post_id> <published|failed> [--url]`** — the documented human-reconcile escape hatch; inside `Ledger.transaction`; unknown id → exit 2; never auto-requeues a maybe-live post (the human chooses).
- `c950d28` — **F2: `fanops unhold <clip_id>`** — clears `held`/`held_reason` AND resets `state=captions_requested` (re-enters the caption gate); replaces the old `ledger.json` hand-edit.
- `6503394` — **F3: `fanops retry-source` + `fanops retry-metrics`** — retry-source → `catalogued` + `error_reason=None` + `meta["transcribed"]=False` (forces a real re-transcribe; a state-only reset would SKIP transcribe per transcribe.py:82); retry-metrics → published exits 0 (next `track` re-pulls), else exit 2.
- `c970f1f` — **F4 (audit C3-sibling, PII): drop `meta.original_name`** — the SHA id is identity; the operator's private filename no longer lands in the ledger. (Also rewrote 2 pre-existing `test_ingest.py` assertions that read the dropped key — a plan/reality drift caught in preflight. Backward-compatible: an OLD ledger carrying the key still loads, `Source.meta` is an open dict.)
- `ea6a160` — **test hardening (the adversarial pass earned its keep):** all 8 skeptics confirmed the verbs CORRECT but flagged that the plan's literal tests under-pinned them (the H9 blind spot) — 4 mutation-proven regression tests added for the `failed` branch, the unknown-id guards, the re-transcribe flag, and the retry-metrics split. Suite 295→299.
- `41bb4ab` — **G2/G3 docs:** README + RUNTIME document the four verbs; new **operator runbook** ("fresh checkout → first real post", 7 steps) in RUNTIME.md; backlog cleaned (recovery verbs / jitter / reconcile marked DONE; per-account-creative-variation recorded as (j)).
- `84d77cf` — **review fix:** the code review's one Minor doc nit — runbook step 5 called the default `manual` responder a "deterministic stub"; reworded to "a no-op — a human/cron writes the response files" (it returns 0). The other Minor (local enum re-imports in dispatch) was deferred as plan-faithful and zero-cost.

Full Phase F provenance (the workflow Close-agent fumble + the shared-worktree-skeptic contamination + the 19-gap completeness map + every skeptic verdict + the 4 mutation proofs) is in the auto-memory `fanops-build-deviations.md` (§Phase F).

**The 19-gap completeness check PASSED (Integrate phase, every gap → a landed commit, all now on `main`):** Tier 0 #1–#6, Tier 1 (H1/H2/H4/H5/H6/N1), Tier 2 (resolve/unhold/retry-metrics/retry-source = F1/F2/F3), Tier 3 (reconcile-logging/responder-digest/jitter/live-checkpoints), + C3-sibling PII (F4)/M1/M2. **all_gaps_closed = True, no regressions.**

**What works right now (all on `main`):**
- **All 6 original block-live findings are CODE-CLOSED** (A1 responder, B4 race, B2 submission-id, B3 MCP auth, A2 learning loop, B5 dead-man's-switch) + the HIGH validation set (H1/H4/H5/H6) + observability (H3) + the operator-recovery verbs (F1–F3).
- The learning loop is autonomous (A2); a silently-dead pipeline is detectable via the heartbeat (B5); the digest surfaces pending agent gates (H3); reconcile logs every per-post branch (E4); every crossposted Post is reconcilable from birth (H1); a live post is never mis-marked `failed` (B2); the MCP poster honors the auth-halt contract (B3); 429 backoff is jittered+bounded (D4).
- **Four operator-recovery verbs** (`resolve`/`unhold`/`retry-source`/`retry-metrics`) mean no recovery state requires hand-editing `ledger.json` — each a tight local-only `Ledger.transaction`, unknown id → exit 2. All four in `fanops --help`; `fanops status` exits 0.
- **PRIME DIRECTIVE holds:** no path marks a maybe-live post `failed`. A shared per-clip media URL across accounts is intended — do NOT "fix" it.

**Live state caveats:**
- **The plan is code-complete and merged; what remains to go LIVE is PURELY OPERATOR-GATED** (the G3 runbook in `RUNTIME.md` makes it executable): (1–3) the 3 human-only steps — create real fan accounts, connect each in Blotato, paste numeric `account_id`s into `accounts.json` + `status: active`; (4) `BLOTATO_API_KEY` in `.env` (sandbox first); (5) `ANTHROPIC_API_KEY` EXPORTED for the `--bare` responder; (6) the `[GATED]` D5 Blotato smoke; (7) cron `fanops run` + the dead-man's-switch monitor.
- **`claude -p` needs `ANTHROPIC_API_KEY`** exported in the `fanops` env (`--bare` ignores OAuth/keychain). E2's heartbeat (published_in_run=0 forever) + E3's pending-gates digest are how a silently-unauthed responder becomes operator-visible.
- **LIVE Blotato is operator-gated:** the MCP connects but a data-returning call returns "Invalid API key or auth session". D5 was verified READ-ONLY against the tool schemas (contract MATCHES; `postUrl`/`publicUrl` split reconciled). A live test post is DEFERRED (needs valid auth + an operator-named throwaway account; `blotato_create_post` posts to a REAL account, no dry-run — NEVER fired autonomously). The metrics ENGAGEMENT-FIELDS checkpoint remains (only a real authed published post confirms which fields analytics exposes). See `[[blotato-mcp-not-reachable-headless]]`.

**Open items, in priority order:**
1. **No code work remains on this plan — it is COMPLETE and merged.** The next move is the operator's: the **live cutover**, following the G3 runbook in `RUNTIME.md` ("Operator runbook — fresh checkout → first real post"). No more build phases.
2. **Optional future feature (audit-C2 reframing, backlog (j)):** per-account creative variation (A/B content learning) — its own spec→plan→build cycle when prioritized.
3. **Pre-existing backlog** (`RUNTIME.md §Backlog`): externalize brand-risk lists + lift weights (b); per-platform max-duration clamp (g); externalize the YouTube title fallback (h); lint + `_parse`/`BASE_URL` dedup (i); per-source ranking (d); media size cap (e); C2 caption language *detection*. (Cosmetic: bump CI actions off the deprecated Node-20 — `actions/checkout@v4`, `actions/setup-python@v5`, `actions/cache@v4`.)

**Pick up here:**
- **The plan is COMPLETE and on `main` — there is no next build phase and no open PR.** If asked to resume code work, there is none on this plan. The remaining path is the operator-gated **live cutover** (the G3 runbook in `RUNTIME.md`), or the optional per-account-creative-variation feature (its own spec→plan→build).
- **`main` is clean and in sync** (`ef3e5d0` + this handoff on top); the `phase-f` branch + worktree are gone. Nothing to clean up.
- **Standing process gotchas (durable multi-agent wisdom, carried forward for any FUTURE workflow):** (a) a FRESH worktree needs its OWN editable venv (`pip install -e ".[dev]"`, **python3.12 not 3.14**, pulls `pytest-timeout`) — symlinking the parent `.venv` tests the WRONG src tree. (b) Verifying through the real CLI: **`cd` to the project/scratch root first** (`Config()` resolves the ledger from cwd; no `FANOPS_ROOT`). (c) **Skeptic/mutation-proving agents run in `parallel()` against ONE shared worktree contaminate each other's tree** (Phase F: skeptics saw each other's throwaway mutation files; self-healed but it was luck) — give each mutation agent its OWN worktree or run them sequentially, and always `git status` + independently verify before trusting. (d) **A long Close/mega-agent that does several outward actions THEN must emit a large structured JSON is failure-prone** (Phase F: the Close agent fumbled the StructuredOutput return after editing docs; the orchestrator finished Close directly from the cached verified phases — the right recovery) — split Close into small single-action agents, or finish-directly. (e) **A subagent can land a commit on the branch after you push** — `git diff <branch> main` before deleting any merged branch; never blind-delete (this was honored at the #6 merge — diff confirmed nothing branch-only).
- Standing invariants: **Do not re-raise audit-C2 (byte-identical media)** — adjudicated (the *valuable* reframing is backlog (j)). **Do NOT autonomously fire `blotato_create_post`** — it posts to a real account. **A fresh/local venv must `pip install -e ".[dev]"`** (python3.12).
