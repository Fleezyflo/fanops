# HANDOFF — MOH FLOW FAN OPS

This file is the cross-session source of truth. It is **rewritten each session** (the §Now block is point-in-time, not an append-only log). Frozen provenance lives in `docs/archive/handoff-history.md` (created on first archive). The build plan is `docs/superpowers/plans/2026-05-31-fanops-real-build-v2.md`. Deviations from that plan made during the build are recorded in the auto-memory file `fanops-build-deviations.md`.

## §State (re-verify the Check commands before quoting)

- **Branch:** `main` (canonical line). Phases A, B, C, D, E + the Phase B follow-up + Phase F + the G finale + **the credential-free TAIL hardening are ALL MERGED into `main` (PRs #1–#7).** **NO open PRs; the plan is COMPLETE and the credential-free backlog tail is DONE.** The `phase-f` and `tail-hardening` branches + their worktrees are deleted (local + remote). Check: `git branch --show-current` / `gh pr list --repo Fleezyflo/fanops --state open` (expect empty).
- **Working tree:** clean, in sync with origin. Check: `git status -sb` (expect `## main...origin/main` with nothing below).
- **HEAD (of `main`):** `1b0ec19` (Merge pull request #7 from Fleezyflo/tail-hardening) → this rev-18 handoff commit lands on top. ALL of A–F + the tail are on `main`. Check: `git log --oneline -1`.
- **Remote:** `origin` → `github.com/Fleezyflo/fanops` (**PRIVATE**). PRs #1–#7 ALL MERGED; no open PRs. Check: `git remote -v && gh pr list --repo Fleezyflo/fanops --state open`.
- **Unit/full tests:** on `main`: **321 passed, 1 skipped** (the tail added +22: T1 preflight, T2 tuning.json, T3 media-cap, T4 duration-clamp, T5 yt-title, T6 timeutil — all on `main`). Check: `source .venv/bin/activate && python -m pytest -q`. The 1 skip = creds-gated Blotato smoke. NOTE: dev-deps now include **`pytest-timeout`** AND **`ruff>=0.5`** (+ a global `timeout=60` in `pyproject`); a fresh/local venv must `pip install -e ".[dev]"` (python3.12) or pytest warns "Unknown config option: timeout" and `ruff` is absent. KNOWN FLAKE: the offline-`whisper` E2E (`test_e2e_real.py`) can "fail" via the 60s pytest-timeout under heavy parallel load (it hangs on a real whisper subprocess), but SKIPS cleanly under a normal `pytest -q` run and runs for real (green) in CI where the toolchain is present — not a regression.
- **Lint:** `ruff check src/` is GREEN (`[tool.ruff.lint] select=["E","F"]`, 4 style E-rules ignored, F bug-class fully retained). Check: `source .venv/bin/activate && ruff check src/` (expect "All checks passed!"). A fresh venv needs `pip install -e ".[dev]"` first (ruff is a dev-dep).
- **Integration tests:** 4 passed, 1 skipped (live Blotato smoke skips without creds). Lock/deadlock tests run under `pytest-timeout` (global `timeout=60` in `pyproject` + a stricter `--timeout=20` for the lock tests); a hang is a hard FAIL.
- **Module/test parity:** 36 src modules (the tail added `timeutil.py` + `post/blotato_base.py`), 39 test files (the tail added `test_timeutil.py`; +`test_llm.py`/`test_prompts.py` from Phase A). Check: `ls src/fanops/*.py src/fanops/post/*.py | wc -l && ls tests/*.py tests/integration/*.py | wc -l` (36 / 39) and `python -m pytest -q -m integration --collect-only | grep -c "::"` (5).
- **Real tooling present:** ffmpeg 8.0.1, ffprobe, whisper CLI, `say` (macOS TTS) all on PATH; whisper `tiny.pt` cached (model downloads blocked by proxy in this env). The E2E pins `tiny` itself, so no env var is required; to override the model for a real `fanops` run, set `FANOPS_WHISPER_MODEL`. Check: `for b in ffmpeg ffprobe whisper say; do command -v $b; done`.
- **Posting backend default:** `dryrun` (writes payload JSON to `05_scheduled/`, posts nothing). Switch via `FANOPS_POSTER=rest|mcp` + `BLOTATO_API_KEY`. Check: `grep -n "FANOPS_POSTER" src/fanops/config.py`.
- **CI:** `.github/workflows/ci.yml` — `unit` job (no toolchain) + `e2e` job (installs toolchain, `FANOPS_REQUIRE_E2E=1`, audit H10). Runs on push to `main` AND on `pull_request` into `main`. **`main` CI GREEN on the tail merge commit: run `26924650797` at `1b0ec19` = `completed success`, both jobs (`unit` + real-tooling `e2e` with FANOPS_REQUIRE_E2E so it genuinely ran), watched to completion (`WATCH_EXIT=0`).** Check: `gh run list --repo Fleezyflo/fanops --branch main --limit 2`. (Non-fatal annotation: "Node.js 20 actions are deprecated" — cosmetic; backlog to bump `actions/checkout@v4`/`setup-python@v5`/`cache@v4`.)
- **Toolchain-absent behavior:** every `subprocess.run` of a media binary (ffmpeg/ffprobe/whisper/yt-dlp) is guarded — `check=False` does NOT catch a pre-launch `FileNotFoundError`, so absent binaries used to crash. Now: pre-Source/one-shot sites (ffprobe at ingest, yt-dlp at `pull`) raise typed `errors.ToolchainMissingError` → `cli.main` exit 2 + "install …", no traceback; pipeline-quarantined sites (ffmpeg render, whisper transcribe, ffmpeg signals) record a retriable per-unit `error`. Check: `grep -rn "subprocess.run" src/fanops/ | grep -v "_run_ffprobe\|_run_ffmpeg\|try:"` (expect only the lines INSIDE the guards) and `grep -n "ToolchainMissingError" src/fanops/errors.py`.
- **Concurrency (Phase B + follow-up):** `advance()` AND **every standalone write command** (`track`, `reconcile`, `adjust`, `ingest`, `pull`) run the WHOLE load→mutate→save inside ONE `Ledger.transaction(cfg)` holding the `fcntl.flock` — acquired BEFORE load, single atomic save on clean exit (audit B4, now everywhere). A 2nd live run is excluded with a typed `LockBusyError`. Slow I/O stays OUTSIDE the lock: `publish_due(in_transaction=True)` uses the unlocked save; `track`'s metrics fetch + `reconcile`'s per-post polls + `pull`'s yt-dlp download all run BEFORE the transaction (only the in-memory apply is locked) — so a 30s network call never serializes behind the ledger lock. crosspost+publish are wrapped (log+continue) so a non-auth raise can't roll back the pass, while a fatal `BlotatoAuthError` still escapes (F52). The slow `claude -p` responder runs OUTSIDE the lock (`request_id` correlation + capture-and-recheck against a mid-call re-seed, audit A3). Check: `grep -n "def transaction\|_save_unlocked" src/fanops/ledger.py && grep -ln "Ledger.transaction" src/fanops/cli.py`.

## 1. What this is

An autonomous fan-account engine for Moh Flow (bilingual EN/AR rapper). It ingests his videos, decides which moments are worth posting (transcript + audio/scene signals → an agent decision with a recorded reason), cuts platform-ready clips with agent-written EN/AR captions, cross-posts to every fan account × platform via Blotato (staggered for opsec, with a subtle non-synchronized artist @mention), then pulls real performance back to make more of what works. See `README.md` (front door), `MohFlow-FanOps/00_control/RUNTIME.md` (operating loop + production seams), `MohFlow-FanOps/00_control/RISK.md` (recorded operator risk-acceptance for the multi-account model), `MohFlow-FanOps/00_control/context.md` (functional — injected as agent guidance).

## 2. Architecture

- **Unit chain:** `Source → Moment → Clip → Post`, over one git-versioned JSON ledger (`00_control/ledger.json`, atomic temp+os.replace write under a file lock).
- **Identity:** content-addressed SHA-based ids everywhere (`ids.py`: `make_id`/`child_id`/`surface_key`) — NEVER builtin `hash()` (the #1 v1 bug; cross-process stability is proven by a subprocess idempotency test).
- **Agent gates:** generative steps (`decide_moments`, `write_captions`) cross a file contract in `04_agent_io/requests/` — code writes a `*.request.json` (stamped with a `request_id`), the agent/responder writes a validated `*.response.json` echoing that id, code resumes. Stale responses can never be applied (request_id correlation).
- **Two responders** (`responder.py`): `ManualResponder` (no-op; a human/cron writes responses) vs `LlmResponder` (`FANOPS_RESPONDER=llm`; **autonomous via `claude -p`** — default model shells the Claude Code CLI through `llm.claude_json` with committed `prompts.py` templates + the gate's pydantic JSON schema, validates against `MomentDecision`/`CaptionSet`, **per-request quarantine** so one bad/failed gate logs + stays pending without halting the rest). Needs `claude` on PATH (authenticated); model is injectable for tests.
- **Three posters** (`post/`): `dryrun` (default), `rest` (`BlotatoRestPoster`, retry/backoff/typed-errors), `mcp` (`BlotatoMcpPoster`, injected `tool_caller`).
- **Pipeline** (`pipeline.py` `advance()`): per-unit error quarantine (one bad source/moment/clip → `error` state, never wedges the pass). **CLI** (`cli.py`): `status/ingest/pull/advance/respond/track/adjust/gc/run/digest`.
- **State machines** (`models.py`): separate `SourceState`/`MomentState`/`ClipState`/`PostState`. `failed`≠`analyzed`; `held` (brand-risk) and `retired` are first-class.

## 3. Gotchas / non-obvious invariants

- **`--base-time` is the SCHEDULE ANCHOR, not the publish cutoff.** crosspost staggers posts to times AFTER base-time; `publish_due(now=None)` publishes whatever is due as of REAL wall-clock now. To publish in the same pass (backfill), pass a `--base-time` in the past. (This was a fixed plan contradiction.)
- **Conservative retirement:** `adjust` amplifies top `--winner-pct` (0.3) but retires only the bottom `--retire-pct` (0.2) AND below `--lift-floor` (20.0) — objectively-fine clips above the floor are never retired. Retiring a clip also retires its moment (so it can't be re-rendered).
- **Three surfaced-but-auto-unrecovered stuck states** (all in the digest + counts, no silent loss): a post stranded in `submitting` by a mid-publish crash; a post in **`needs_reconcile`** (REST hit an ambiguous 5xx/timeout after sending the body — it may be live, so it is NOT re-POSTed; Blotato has no idempotency key); a `published` post Blotato never returned metrics for. The first two need a poll step (`GET /v2/posts/:id` → promote/reset) — backlog item (a). `needs_reconcile` only ever arises under the `rest`/`mcp` backends, never `dryrun`.
- **`dryrun` posts are trackable** (audit C4): the dryrun poster stamps `submission_id = f"dryrun_<post_id>"`, so the `publish → track → analyzed → adjust` loop is fully exercisable offline by feeding `track` a metrics row keyed on that id. (CLI `track` still skips in dryrun — no live metrics source — but the seam is proven by `tests/integration/test_publish_track_loop.py`.)
- **Whisper model:** default `turbo`; falls back to the best cached `~/.cache/whisper/*.pt` when the requested model can't be downloaded (offline/proxied). In THIS env only `tiny` is cached.

## 4. Health checks

Run the §State Check commands. The integration E2E (`tests/integration/test_e2e_real.py`) is the golden path: real `say` TTS → real whisper transcript (asserts real whisper segment structure + ≥4 words + a vocoder-robust "anymore" anchor — CI-2) → real ffmpeg 1080×1920 vertical clip → 2 dryrun posts published. It pins the `tiny` whisper model in-test (no env var needed) and skips cleanly on a host with no cached checkpoint, so a fresh checkout / CI runner sees a clear skip rather than a cryptic `assert 0 == 1`.

## 5. Now (rewritten each session)

**As of 2026-06-04 (rev 18 — credential-free TAIL hardening MERGED; plan COMPLETE + backlog tail DONE).**

**Most recent shipped (this session):** **The credential-free backlog TAIL — preflight cutover-safety guard + backlog (b)(e)(g)(h)(i) — is now MERGED to `main` as `1b0ec19` (Merge pull request #7 from Fleezyflo/tail-hardening).** Built after the operator said "no Blotato subscription yet — build everything that's last." (The #1-value item, the live Blotato contract smoke, stays operator-gated: a read-only probe of the connected MCP returned "Invalid API key or auth session" — connected but unauthenticated, verified not assumed.) Delivered as a deterministic 7-phase `Workflow` (`wf_24b97dcf-71f`, script `.claude/workflows/fanops-tail.js`); independently re-verified on the committed artifact (321/1, ruff clean, preflight working, no mutation residue) before merge; `main` CI green on the merge commit (run `26924650797`, both jobs, `WATCH_EXIT=0`). Branch+worktree deleted after the `git diff` guardrail. Suite **299→321, 1 skipped** (+22). The 6 commits (each TDD RED→GREEN, independent verify, ≥1 mutation proof):
- `26984b8` — **T1 startup preflight (cutover-safety).** `_check_preflight(cfg)` in cli.py (sibling to `_check_accounts`), called in BOTH `run`+`advance`: `FANOPS_RESPONDER=llm` w/o `ANTHROPIC_API_KEY` → exit 2; `FANOPS_POSTER` rest/mcp w/o `BLOTATO_API_KEY` → exit 2. Catches the #1 silent-zero-output cutover trap in 1s. Added `Config.anthropic_api_key`. Verified end-to-end on the real CLI (both arms; key-present passes; status/ingest don't gate).
- `8736b27` — **T2 externalize tunable lists (b).** Optional `00_control/tuning.json` overrides `_OFFBRAND_EN/_AR` (REPLACE) + `_W` lift weights; fail-open to defaults when absent/corrupt.
- `32c9eb1` — **T3 media size cap + size-aware PUT timeout (e).** `_MAX_UPLOAD_BYTES=500MB` rejects pre-network; PUT timeout `min(600, 60+size_mb*2)`; presign stays 30s.
- `9c68302` — **T4 per-platform duration clamp (g).** `PLATFORM_MAX_SECONDS` real enforcement; per-surface SKIP (not all-or-nothing), fail-open on unknown duration (clip dur = its moment's `end-start`; there is NO `Clip.duration` — a Preflight-caught plan drift).
- `1795787` — **T5 externalize YouTube title (h).** `cfg.artist_name` = `FANOPS_ARTIST_NAME` or "Moh Flow", threaded through all 3 responders.
- `e77ced5` — **T6 ruff + dedup (i).** `_parse`→one `timeutil.parse_iso`; `BASE_URL`→one `post/blotato_base.py`; `ruff>=0.5` dev-dep + `[tool.ruff.lint] select=["E","F"]`; `ruff check src/` green.

Full tail provenance (the Preflight drift-catch + resume, every mutation proof, the RECURRING shared-worktree contamination, the 2 Minor test nits) is in `fanops-build-deviations.md` (§Tail hardening).

**What works right now (all on `main`):**
- **The entire live-autonomous plan (all 19 audit gaps) + the credential-free backlog tail are CODE-COMPLETE.** Block-live findings all closed (A1 responder, B4 race, B2 submission-id, B3 MCP auth, A2 learning loop, B5 dead-man's-switch), HIGH validation (H1/H4/H5/H6), observability (H3), the 4 operator-recovery verbs (F1–F3), and now: a **cutover-safety preflight** (refuses to run a config that would silently produce nothing), operator-tunable HOLD gate + lift weights (`tuning.json`), media size cap, real per-platform duration clamp, configurable artist name, and a lint-clean dedup'd codebase.
- **PRIME DIRECTIVE holds:** no path marks a maybe-live post `failed`. A shared per-clip media URL across accounts is intended — do NOT "fix" it.

**Live state caveats:**
- **What remains to go LIVE is PURELY OPERATOR-GATED** (the G3 runbook in `RUNTIME.md` makes it executable, and the new preflight now hard-blocks the two silent-misconfig traps at step 4/5): (1–3) create real fan accounts, connect each in Blotato, paste numeric `account_id`s into `accounts.json` + `status: active`; (4) `BLOTATO_API_KEY` in `.env`; (5) `ANTHROPIC_API_KEY` EXPORTED for the `--bare` responder; (6) the `[GATED]` D5 Blotato smoke; (7) cron `fanops run` + the dead-man's-switch monitor.
- **Blotato subscription NOT yet acquired (operator, 2026-06-04):** the connected MCP returns "Invalid API key or auth session" on any data call. The live-contract smoke (the highest-value remaining technical item — verifies the `postSubmissionId` field, media/analytics shapes against a REAL API vs mocks) is BLOCKED until the operator gets a key + re-auths the MCP. A live test post stays DEFERRED on valid auth + an operator-named throwaway account; `blotato_create_post` posts to a REAL account, NEVER fired autonomously.

**Open items, in priority order:**
1. **No credential-free code work remains.** The plan is complete AND the credential-free backlog tail is done. The next moves both need the operator: (a) get a **Blotato subscription/key** → then I can run the live-contract smoke (read-only) + fix any field mismatch TDD; (b) the **live cutover** itself (G3 runbook).
2. **Optional future feature (backlog (j)):** per-account creative variation (A/B content learning) — its own spec→plan→build. This is the ONE remaining buildable feature, but it's net-new capability, not finishing work.
3. **Remaining backlog** (`RUNTIME.md §Backlog`): (d) per-source ranking [deliberate, low-stakes] + the deferred-from-plan items (burned-in subtitles, trending-audio, daypart scheduling, per-surface best-window, multi-artist tenancy, richer secrets manager) — all by-design "not built." 2 Minor test nits (T3 `match="size"` coincidental; T1 exact-string `responder_mode=="llm"`). Cosmetic: bump CI off deprecated Node-20 actions.

**Pick up here:**
- **`main` (`1b0ec19` + this handoff on top) is clean, in sync, no open PR.** Everything buildable without the operator's Blotato credentials is DONE. If asked for more code work: the only options are the **C2 creative-variation feature** (backlog (j), needs its own spec→plan→build) or operator-credential-gated work (the live-contract smoke / live cutover).
- **When the operator gets a Blotato key:** re-probe the MCP (`blotato_get_user` read-only) to confirm auth; if it returns real data, run the live-contract verification (read-only `blotato_list_accounts` etc., diff against FanOps's `_extract_submission_id`/media/analytics assumptions, fix any mismatch TDD). NEVER autonomously fire `blotato_create_post` (real account, no dry-run).
- **Standing process gotchas (durable multi-agent wisdom):** (a) a FRESH worktree needs its OWN editable venv (`pip install -e ".[dev]"`, **python3.12 not 3.14**; now pulls `pytest-timeout` AND `ruff`). (b) Verifying through the real CLI: **`cd` to a scratch root first** (`Config()` resolves from cwd; no `FANOPS_ROOT`). (c) **THIRD OCCURRENCE this session:** skeptics in `parallel()` against ONE shared worktree contaminate each other (the tail's T4 skeptics saw a foreign fail-CLOSED mutation; self-healed, verdicts valid, committed branch clean — but luck, not design). **Next workflow: give each mutation-proving skeptic `isolation: 'worktree'` or run them sequentially.** Always `git status` + independently re-run the committed artifact before trusting a workflow's green. (d) Split a long Close/mega-agent into small single-action agents (it fumbled the StructuredOutput return in Phase F). (e) `git diff <branch> main` before deleting any merged branch — honored at #6 and #7.
- Standing invariants: **Do not re-raise audit-C2 (byte-identical media)** — adjudicated (the valuable reframing is (j)). **Do NOT autonomously fire `blotato_create_post`.** **A fresh/local venv must `pip install -e ".[dev]"`** (python3.12).
