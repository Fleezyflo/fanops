# HANDOFF — MOH FLOW FAN OPS

This file is the cross-session source of truth. It is **rewritten each session** (the §Now block is point-in-time, not an append-only log). Frozen provenance lives in `docs/archive/handoff-history.md` (created on first archive). The build plan is `docs/superpowers/plans/2026-05-31-fanops-real-build-v2.md`. Deviations from that plan made during the build are recorded in the auto-memory file `fanops-build-deviations.md`.

## §State (re-verify the Check commands before quoting)

- **Branch:** `main` (canonical line). Phases A–F + the G finale + the credential-free TAIL + burned-in subtitles (PR #8) + per-account creative variation v1 (PR #9) + content discovery (PR #12) + **creative-variation v2 / the A/B learning loop (PRs #13 spec + #14 impl) are ALL MERGED into `main`.** **NO open PRs.** The plan is COMPLETE, the credential-free backlog is DONE, both deferred-from-plan features shipped (subtitles + creative variation), AND the v2 learning loop (the "dumb variation can't learn" gap) is closed. All feature branches + worktrees deleted. Check: `git branch --show-current` / `gh pr list --repo Fleezyflo/fanops --state open` (expect empty).
- **Working tree:** clean, in sync with origin. Check: `git status -sb` (expect `## main...origin/main` with nothing below).
- **HEAD (of `main`):** `5f275fd` (Merge pull request #14 — Creative Variation v2) → this rev-24 handoff commit lands on top. Check: `git log --oneline -1`.
- **Remote:** `origin` → `github.com/Fleezyflo/fanops` (**PRIVATE**). PRs #1–#9, #11, #12, #13, #14 MERGED; #10 CLOSED (duplicate of #9); no open PRs. Check: `git remote -v && gh pr list --repo Fleezyflo/fanops --state open`.
- **Responder auth (rev-21, IMPORTANT):** the autonomous LLM responder uses the operator's **EXISTING `claude` subscription** via plain `claude -p` (NOT `--bare`) — it needs `claude login` on the host, **NOT `ANTHROPIC_API_KEY`**. Proven: the real autonomous pipeline ran end-to-end on the subscription (`FANOPS_RESPONDER=llm`, dryrun) — `sources→moments→clips→posts→published=1`, no longer parks at `awaiting_moments`. Check: `grep -n "strict-mcp-config" src/fanops/llm.py` (the new flag; `--bare` is gone).
- **Unit/full tests:** on `main`: **387 passed, 1 skipped** (content discovery brought 351→363; creative-variation v2 brought 363→387: +24 = config toggle/thresholds, variant_learning.best_hooks gate tests incl. the comparative-runner-up + single-variant-empty hardening, caption_prompt learned-hint, request_captions wiring + fail-open, digest gate-state, the AST amplify-isolation + best_hooks-caller-lock tests, and the real on-disk loop-closing integration). Check: `source .venv/bin/activate && python -m pytest -q`. The 1 skip = creds-gated Blotato smoke. NOTE: dev-deps include **`pytest-timeout`** AND **`ruff>=0.5`** (+ a global `timeout=60` in `pyproject`); a fresh/local venv must `pip install -e ".[dev]"` (python3.12). KNOWN FLAKE: the offline-`whisper` E2E (`test_e2e_real.py`) can "fail" via the 60s pytest-timeout under heavy parallel load, but SKIPS cleanly under a normal `pytest -q` run and runs green in CI. RECURRING FALSE ALARM: a subagent running bare `pytest` (no venv) reports "pytest-mock not installed / N errors" — ALWAYS verify with `source .venv/bin/activate && python -m pytest -q` first (387/1).
- **ffmpeg (toolchain):** the system `ffmpeg` is now **`ffmpeg-full`** (8.1.1, `brew link --overwrite --force`'d) — it has the text filters (`drawtext`/`subtitles`/`ass`, libass/freetype) the burned-in-subtitles feature needs. The default `ffmpeg` formula on this box is a STRIPPED build WITHOUT them; if it ever gets relinked, subtitles FAIL OPEN (clip renders without text + logs a warning, never blocks). Check: `ffmpeg -hide_banner -h filter=drawtext | head -1` (must NOT say "Unknown filter"). Local-machine fix only; CI's Linux runner has full ffmpeg via apt.
- **Lint:** `ruff check src/` is GREEN (`[tool.ruff.lint] select=["E","F"]`, 4 style E-rules ignored, F bug-class fully retained). Check: `source .venv/bin/activate && ruff check src/` (expect "All checks passed!"). A fresh venv needs `pip install -e ".[dev]"` first (ruff is a dev-dep).
- **Integration tests:** 7 passed, 1 skipped (live Blotato smoke skips without creds). Includes `test_discover_real.py` (discovery) and `test_variant_learning_real.py` (the v2 loop-closing proof: a winning hook reaches the real on-disk caption request). Lock/deadlock tests run under `pytest-timeout` (global `timeout=60` in `pyproject` + a stricter `--timeout=20` for the lock tests); a hang is a hard FAIL. Check: `source .venv/bin/activate && python -m pytest tests/integration -q`.
- **Module/test parity:** 39 src modules (v2 added `variant_learning.py`), 45 test files (v2 added `test_variant_learning.py` + `tests/integration/test_variant_learning_real.py`). Check: `ls src/fanops/*.py src/fanops/post/*.py | wc -l && ls tests/*.py tests/integration/*.py | wc -l` (39 / 45).
- **Real tooling present:** ffmpeg 8.1.1 (now the **ffmpeg-full** build — text filters/libass), ffprobe, whisper CLI, `say` (macOS TTS) all on PATH; whisper `tiny.pt` cached. The E2E pins `tiny` itself; override with `FANOPS_WHISPER_MODEL`. Check: `for b in ffmpeg ffprobe whisper say; do command -v $b; done`.
- **Posting backend default:** `dryrun` (writes payload JSON to `05_scheduled/`, posts nothing). Switch via `FANOPS_POSTER=rest|mcp` + `BLOTATO_API_KEY`. Check: `grep -n "FANOPS_POSTER" src/fanops/config.py`.
- **CI:** `.github/workflows/ci.yml` — `unit` job (no toolchain) + `e2e` job (installs toolchain, `FANOPS_REQUIRE_E2E=1`, audit H10). Runs on push to `main` AND on `pull_request` into `main`. **`main` CI GREEN on the v2 merge commit `5f275fd` — push run `26964159930` `completed/success` (2m55s, both jobs); the PR run `26963977652` was also green (unit 22s + real-tooling E2E 2m42s).** Prior main greens: `8c69b5b` (discovery), `98f7d7a`, `01292cd`. Check: `gh run list --repo Fleezyflo/fanops --branch main --limit 2`. (Non-fatal annotation: "Node.js 20 actions are deprecated" — cosmetic; backlog to bump `actions/checkout@v4`/`setup-python@v5`/`cache@v4`.)
- **Toolchain-absent behavior:** every `subprocess.run` of a media binary (ffmpeg/ffprobe/whisper/yt-dlp) is guarded — `check=False` does NOT catch a pre-launch `FileNotFoundError`, so absent binaries used to crash. Now: pre-Source/one-shot sites (ffprobe at ingest, yt-dlp at `pull`) raise typed `errors.ToolchainMissingError` → `cli.main` exit 2 + "install …", no traceback; pipeline-quarantined sites (ffmpeg render, whisper transcribe, ffmpeg signals) record a retriable per-unit `error`. Check: `grep -rn "subprocess.run" src/fanops/ | grep -v "_run_ffprobe\|_run_ffmpeg\|try:"` (expect only the lines INSIDE the guards) and `grep -n "ToolchainMissingError" src/fanops/errors.py`.
- **Concurrency (Phase B + follow-up):** `advance()` AND **every standalone write command** (`track`, `reconcile`, `adjust`, `ingest`, `pull`) run the WHOLE load→mutate→save inside ONE `Ledger.transaction(cfg)` holding the `fcntl.flock` — acquired BEFORE load, single atomic save on clean exit (audit B4, now everywhere). A 2nd live run is excluded with a typed `LockBusyError`. Slow I/O stays OUTSIDE the lock: `publish_due(in_transaction=True)` uses the unlocked save; `track`'s metrics fetch + `reconcile`'s per-post polls + `pull`'s yt-dlp download all run BEFORE the transaction (only the in-memory apply is locked) — so a 30s network call never serializes behind the ledger lock. crosspost+publish are wrapped (log+continue) so a non-auth raise can't roll back the pass, while a fatal `BlotatoAuthError` still escapes (F52). The slow `claude -p` responder runs OUTSIDE the lock (`request_id` correlation + capture-and-recheck against a mid-call re-seed, audit A3). Check: `grep -n "def transaction\|_save_unlocked" src/fanops/ledger.py && grep -ln "Ledger.transaction" src/fanops/cli.py`.

## 1. What this is

An autonomous fan-account engine for Moh Flow (bilingual EN/AR rapper). It ingests his videos, decides which moments are worth posting (transcript + audio/scene signals → an agent decision with a recorded reason), cuts platform-ready clips with agent-written EN/AR captions, cross-posts to every fan account × platform via Blotato (staggered for opsec, with a subtle non-synchronized artist @mention), then pulls real performance back to make more of what works. See `README.md` (front door), `MohFlow-FanOps/00_control/RUNTIME.md` (operating loop + production seams), `MohFlow-FanOps/00_control/RISK.md` (recorded operator risk-acceptance for the multi-account model), `MohFlow-FanOps/00_control/context.md` (functional — injected as agent guidance).

## 2. Architecture

- **Unit chain:** `Source → Moment → Clip → Post`, over one git-versioned JSON ledger (`00_control/ledger.json`, atomic temp+os.replace write under a file lock).
- **Identity:** content-addressed SHA-based ids everywhere (`ids.py`: `make_id`/`child_id`/`surface_key`) — NEVER builtin `hash()` (the #1 v1 bug; cross-process stability is proven by a subprocess idempotency test).
- **Agent gates:** generative steps (`decide_moments`, `write_captions`) cross a file contract in `04_agent_io/requests/` — code writes a `*.request.json` (stamped with a `request_id`), the agent/responder writes a validated `*.response.json` echoing that id, code resumes. Stale responses can never be applied (request_id correlation).
- **Two responders** (`responder.py`): `ManualResponder` (no-op; a human/cron writes responses) vs `LlmResponder` (`FANOPS_RESPONDER=llm`; **autonomous via `claude -p`** — default model shells the Claude Code CLI through `llm.claude_json` with committed `prompts.py` templates + the gate's pydantic JSON schema, validates against `MomentDecision`/`CaptionSet`, **per-request quarantine** so one bad/failed gate logs + stays pending without halting the rest). Needs `claude` on PATH (authenticated); model is injectable for tests.
- **Three posters** (`post/`): `dryrun` (default), `rest` (`BlotatoRestPoster`, retry/backoff/typed-errors), `mcp` (`BlotatoMcpPoster`, injected `tool_caller`).
- **Pipeline** (`pipeline.py` `advance()`): per-unit error quarantine (one bad source/moment/clip → `error` state, never wedges the pass). **CLI** (`cli.py`): `status/ingest/pull/advance/respond/track/adjust/gc/run/digest`.
- **State machines** (`models.py`): separate `SourceState`/`MomentState`/`ClipState`/`PostState`. `failed`≠`analyzed`; `held` (brand-risk) and `retired` are first-class.

## 3. Gotchas / non-obvious invariants

- **`--base-time` is the SCHEDULE ANCHOR, not the publish cutoff.** crosspost staggers posts to times AFTER base-time; `publish_due(now=None)` publishes whatever is due as of REAL wall-clock now. To publish in the same pass (backfill), pass a `--base-time` in the past. (This was a fixed plan contradiction.)
- **Conservative retirement:** `adjust` amplifies top `--winner-pct` (0.3) but retires only the bottom `--retire-pct` (0.2) AND below `--lift-floor` (20.0) — objectively-fine clips above the floor are never retired. Retiring a clip also retires its moment (so it can't be re-rendered).
- **Three surfaced-but-auto-unrecovered stuck states** (all in the digest + counts, no silent loss): a post stranded in `submitting` by a mid-publish crash; a post in **`needs_reconcile`** (REST hit an ambiguous 5xx/timeout after sending the body — it may be live, so it is NOT re-POSTed; Blotato has no idempotency key); a `published` post Blotato never returned metrics for. The first two need a poll step (`GET /v2/posts/:id` → promote/reset) — backlog item (a). `needs_reconcile` only ever arises under the `rest`/`mcp` backends, never `dryrun`.
- **`dryrun` posts are trackable** (audit C4): the dryrun poster stamps `submission_id = f"dryrun_<post_id>"`, so the `publish → track → analyzed → adjust` loop is fully exercisable offline by feeding `track` a metrics row keyed on that id. (CLI `track` still skips in dryrun — no live metrics source — but the seam is proven by `tests/integration/test_publish_track_loop.py`.)
- **Whisper model:** default `turbo`; falls back to the best cached `~/.cache/whisper/*.pt` when the requested model can't be downloaded (offline/proxied). In THIS env only `tiny` is cached.

## 4. Health checks

Run the §State Check commands. The integration E2E (`tests/integration/test_e2e_real.py`) is the golden path: real `say` TTS → real whisper transcript (asserts real whisper segment structure + ≥4 words + a vocoder-robust "anymore" anchor — CI-2) → real ffmpeg 1080×1920 vertical clip → 2 dryrun posts published. It pins the `tiny` whisper model in-test (no env var needed) and skips cleanly on a host with no cached checkpoint, so a fresh checkout / CI runner sees a clear skip rather than a cryptic `assert 0 == 1`.

## 5. Now (rewritten each session)

**As of 2026-06-04 (rev 24 — creative-variation v2 / the A/B learning loop shipped; built via an ultracode workflow whose adversarial phase caught + fixed two real safety issues; three v2 follow-ups spawned).**

**Most recent shipped:** **Creative Variation v2 — the A/B learning loop — MERGED to `main` as `5f275fd` (PR #14; spec/plan in PR #13 `888ae6e`).** Closes the gap the operator named: v1 *measured* which per-account hook variant wins but *acted on nothing* (an A/B test with no feedback). v2 closes the loop on the SAFE, reversible side — a new pure read-only scorer **`variant_learning.best_hooks`** reads the lift data v1 records and, when a hook has earned a TRUSTWORTHY win, feeds its STYLE back into the next caption request (`request_captions` appends a "lean toward this, don't copy verbatim" hint to `guidance`; `caption_prompt` renders it). **Gated** (`FANOPS_VARIANT_LEARNING`, default OFF): emits only when the leader has ≥ `FANOPS_VARIANT_MIN_POSTS` (3) analyzed posts AND beats a REAL runner-up by ≥ `FANOPS_VARIANT_MIN_GAP` (10) — below either, `[]` (still exploring). Touches NONE of amplify/classify_outcomes/_delete_moment_cascade (C1). Fail-open + deterministic everywhere. Suite 363→**387, 1 skipped**; ruff clean; **both CI jobs green** (unit 22s + real-tooling E2E 2m42s, PR run `26963977652`). The loop closing is PROVEN on disk (`tests/integration/test_variant_learning_real.py`: a winning hook reaches the real caption-request file). Spec+plan: `docs/superpowers/{specs,plans}/2026-06-04-creative-variation-v2-feedback*.md`. Build workflow committed at `.claude/workflows/variant-learning-v2.js`.

**HOW IT WAS BUILT + THE LOAD-BEARING EPISODE LESSON (the adversarial phase earned its keep):** v2 was built by an **ultracode Workflow** = sequential TDD over the 5 plan tasks → 3 parallel adversarial skeptics → completeness critic. **Two skeptics returned `holds=false`, and BOTH were legitimate** (fixed in commit `9b5e3a7`, mutation-proven):
- **Trust gate was not fully COMPARATIVE.** A single variant with ≥min_posts + high lift returned a hint even with NO runner-up (it compared against an implicit zero) — violating the stated "beats the runner-up by min_gap" guarantee and silently collapsing the per-account exploration variation exists to create. Fix: require an actual runner-up (`len(ranked) >= 2`); no competitor → `[]`. Strictly MORE conservative.
- **The amplify-isolation TEST guarded a FALSE guarantee.** It string-grepped `track.py`/`pipeline.py` for `"variant_learning"` — but `import fanops.pipeline` legitimately DOES pull in `variant_learning` transitively (`pipeline → caption → variant_learning`; caption is where the loop closes). "No transitive import" was never true or desirable. Replaced with the REAL data-flow invariant (AST-based): the amplify/classify_outcomes/retire fns (`adjust.py`) + `_delete_moment_cascade` (`ledger.py`) must reference none of variant_key/variant_hook/best_hooks, plus a positive lock that `best_hooks` is called ONLY from the safe `caption.py`/`digest.py` surfaces. **Mutation-proven** (injecting `post.variant_key` into `amplify()` turns it red). The substantive C1 safety always held; this was a wrong test, not a wrong feature.
- **RULE (durable): an adversarial skeptic's `holds=false` is a lead to INVESTIGATE against the real code, not a verdict to reflexively obey OR dismiss. One finding here was a real code edge; the other was a wrong test. Both improved the result. The workflow self-reported all-green; independent re-run + reading the artifacts is what surfaced the truth (suite 384→387 after the fixes).**

**Prior shipped:** content discovery + folder-review intake (PR #12 `8c69b5b`); responder-auth fix — drop `--bare`, use the operator's existing `claude` subscription (PR #11 `98f7d7a`); per-account creative variation v1 / observe-only (PR #9 `01292cd`). Full detail for v1's two episode lessons (DUPLICATE-WORK COLLISION / OCR-vs-VISUAL) in `fanops-build-deviations.md`.

**What works right now (all on `main`):**
- **The entire live-autonomous plan (all 19 audit gaps) + the credential-free backlog tail + BOTH deferred features (burned-in subtitles PR #8, creative variation PR #9) + content-discovery intake (PR #12) + the v2 learning loop (PR #14) are CODE-COMPLETE.** Block-live findings closed; HIGH validation; observability; 4 recovery verbs; cutover-safety preflight; tunable HOLD gate + lift weights; media cap; duration clamp; configurable artist name; lint-clean dedup'd codebase; **on-screen subtitles + hook** (`FANOPS_BURN_SUBS`); **per-account A/B creative variation** (`FANOPS_CREATIVE_VARIATION`) with lift-by-variant attribution; **the A/B learning loop** (`FANOPS_VARIANT_LEARNING`) that biases the next caption toward gated, comparative winners; and the **content-intake gate** (`fanops discover` → Finder approve → `fanops intake`).
- **PRIME DIRECTIVE holds:** no path marks a maybe-live post `failed`. The C1 amplify/delete-cascade path stays blind to variant signal (mechanized, mutation-proven).

**Live state caveats:**
- **What remains to go LIVE is PURELY OPERATOR-GATED** (G3 runbook in `RUNTIME.md`): (1–3) create real fan accounts, connect in Blotato, paste numeric `account_id`s + `status: active`; (4) `BLOTATO_API_KEY` in `.env`; (5) **`claude login` on the host** (NO `ANTHROPIC_API_KEY`); (6) the `[GATED]` D5 Blotato smoke; (7) cron `fanops run` + the dead-man's-switch monitor. **The BRAIN is unblocked (proven end-to-end on the subscription); the ONLY remaining gate is BLOTATO (posting).**
- **Blotato subscription NOT yet acquired (operator, 2026-06-04):** the connected MCP returns "Invalid API key or auth session" on any data call. The live-contract smoke is BLOCKED until a key + MCP re-auth. `blotato_create_post` posts to a REAL account, NEVER fired autonomously.
- **v2 learning is OPT-IN + needs DATA:** `FANOPS_VARIANT_LEARNING` is default OFF, and even ON it emits nothing until a surface has ≥3 analyzed posts with a real lift gap — which only accrues AFTER going live and pulling real metrics. Pre-live it is correctly inert. It also requires `FANOPS_CREATIVE_VARIATION=1` to have variants to learn from.
- **ffmpeg is `ffmpeg-full` (local fix):** subtitles + creative-variation hooks need libass text filters; a stripped relink → both FAIL OPEN (plain clip + warning). CI's Linux runner has full ffmpeg via apt.

**Open items, in priority order:**
1. **Operator-credential-gated (Blotato):** (a) get a **Blotato subscription/key** → then run the live-contract smoke (read-only) + fix any field mismatch TDD; (b) the **live cutover** (G3 runbook). THE ONLY HIGH-VALUE PATH LEFT.
2. **Three v2 FOLLOW-UPS — SPAWNED AS TASK CHIPS this session (each gated on "v2 merged first", which is now TRUE; each its own executor — do NOT also run a same-session Workflow on them, PROCESS LESSON 4):** (i) **auto-propagate winners into amplify** — the v3 that touches the C1 delete-cascade path; flagged HIGH RISK, needs far stronger gates than v2; (ii) **cross-account winner transfer** — let one account's winner inform others WITHOUT homogenizing the per-account diversity; (iii) **bandit exploration/exploitation** — replace v2's gated-greedy with a DETERMINISTIC bandit (the content-addressed/no-RNG constraint is the central design challenge). These are scoped as spawned chips; the operator owns whether/when to start them.
3. **Low-value backlog** (operator-deprioritized): trending-audio (integration-gated), daypart/timezone scheduling, per-surface best-window learning, (d) per-source ranking. By-design "not built": multi-artist tenancy, richer secrets manager. 2 Minor test nits. Cosmetic: bump CI off deprecated Node-20 actions.

**Pick up here:**
- **`main` (`5f275fd` + this rev-24 handoff on top) is clean, in sync, NO open PRs, only `main` exists remotely.** Verify with the §State Check commands (expect 387/1, ruff clean, 39 src / 45 tests). ALL high-value prioritized work is DONE — plan, both deferred features, responder-on-subscription, content-discovery intake, AND now the v2 learning loop. The only high-value path left is the operator-credential-gated Blotato live smoke + cutover.
- **The three v2 follow-up chips are waiting** (auto-amplify v3 / cross-account transfer / bandit). If the operator picks one up, treat the chip as the SOLE executor for that task. The v3/auto-amplify one is HIGH RISK (first feature to touch the C1 path) — bias toward more guards + adversarial verification than feels necessary.
- **When the operator gets a Blotato key:** re-probe the MCP (`blotato_get_user` read-only) to confirm auth; if real data returns, run the live-contract verification (diff against `_extract_submission_id`/media/analytics, fix any mismatch TDD). NEVER autonomously fire `blotato_create_post`.
- **Standing process gotchas (durable multi-agent wisdom):** (a) a FRESH worktree needs its OWN editable venv (`pip install -e ".[dev]"`, **python3.12 not 3.14**). This session built v2 directly on a feature branch in the MAIN tree (existing `.venv`) precisely to skip the worktree-venv bootstrap. (b) Verifying through the real CLI / scratch render: **`cd` to a scratch root first** (`Config()` resolves from cwd). (c) **Skeptics must NOT `parallel()` against ONE shared worktree** — run them read-only (this session's verify phase was read-only) or give each `isolation: 'worktree'`. **ALWAYS independently re-run the committed artifact before trusting a workflow's green** (this session: the workflow said all-green; my re-run + reading the code is what confirmed the two skeptic findings were real). (d) **ONE executor per task** (PROCESS LESSON 4). (e) `git diff <branch> main` before deleting any merged branch. (f) **VERIFY THE TOOLCHAIN before building a feature that depends on it.** (g) **For "did the pixels render", VIEW the image (Read the PNG) — OCR is a flaky probe.** (h) **NEW (v2): an adversarial `holds=false` is a lead to investigate against real code, not an automatic verdict — one finding was a real edge, one a wrong test; both improved the result.**
- Standing invariants: **Do not re-raise audit-C2 (byte-identical media)** — adjudicated; the valuable reframing (creative variation v1+v2) is BUILT. **The C1 amplify/delete path must stay blind to variant signal** (now mechanized + mutation-proven; the v3 follow-up is the ONLY thing that may touch it, behind heavy gates). **Do NOT autonomously fire `blotato_create_post`.** **A fresh/local venv must `pip install -e ".[dev]"`** (python3.12).
