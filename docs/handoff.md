# HANDOFF — MOH FLOW FAN OPS

This file is the cross-session source of truth. It is **rewritten each session** (the §Now block is point-in-time, not an append-only log). Frozen provenance lives in `docs/archive/handoff-history.md` (created on first archive). The build plan is `docs/superpowers/plans/2026-05-31-fanops-real-build-v2.md`. Deviations from that plan made during the build are recorded in the auto-memory file `fanops-build-deviations.md`.

## §State (re-verify the Check commands before quoting)

- **Branch:** `main`. Check: `git branch --show-current`.
- **Working tree:** clean. Check: `git status -sb`.
- **HEAD:** `f0af461` (+ this handoff commit on top). Check: `git log --oneline -1`.
- **Remote:** `origin` → `github.com/Fleezyflo/fanops` (**PRIVATE**), `main` tracks `origin/main`. Pushed, in sync (0 ahead / 0 behind). Check: `git remote -v && gh repo view Fleezyflo/fanops --json isPrivate`.
- **Unit tests:** 232 passed, 5 deselected. Check: `source .venv/bin/activate && python -m pytest -q -m "not integration"`. (Phase A added net +13: llm 6, prompts 3, responder net +4.)
- **Integration tests:** 4 passed, 1 skipped (live Blotato smoke skips without creds; the publish→track→analyzed loop test, the real-tooling E2E, AND the amplify→ingest→real-render test all run). Check: `source .venv/bin/activate && python -m pytest -q -m integration`. (The E2E pins its own whisper model in-test — no `FANOPS_WHISPER_MODEL` needed; it skips cleanly if no checkpoint is cached.) Full suite (both markers): `236 passed, 1 skipped`.
- **Module/test parity:** 34 src modules (Phase A added `llm.py` + `prompts.py`), 37 test files (Phase A added `test_llm.py` + `test_prompts.py`). The integration *marker* selects 5 tests: the 3 in `tests/integration/` (`test_e2e_real.py` ×2 — the transcript-drives-moment golden path + `test_blotato_smoke.py` creds-skip + `test_publish_track_loop.py`) PLUS `tests/test_adjust.py::test_amplify_then_ingest_then_render_produces_new_clip` (marked `integration` because it shells real ffmpeg — CI-1) — i.e. `-m integration --collect-only` = 5. Check: `ls src/fanops/*.py src/fanops/post/*.py | wc -l && ls tests/*.py tests/integration/*.py | wc -l` (34 / 37) and `python -m pytest -q -m integration --collect-only | grep -c "::"` (5).
- **Real tooling present:** ffmpeg 8.0.1, ffprobe, whisper CLI, `say` (macOS TTS) all on PATH; whisper `tiny.pt` cached (model downloads blocked by proxy in this env). The E2E pins `tiny` itself, so no env var is required; to override the model for a real `fanops` run, set `FANOPS_WHISPER_MODEL`. Check: `for b in ffmpeg ffprobe whisper say; do command -v $b; done`.
- **Posting backend default:** `dryrun` (writes payload JSON to `05_scheduled/`, posts nothing). Switch via `FANOPS_POSTER=rest|mcp` + `BLOTATO_API_KEY`. Check: `grep -n "FANOPS_POSTER" src/fanops/config.py`.
- **CI:** `.github/workflows/ci.yml` — unit job + an e2e job that installs the toolchain and runs the integration suite with `FANOPS_REQUIRE_E2E=1` (turns a tooling-absent skip into a failure, audit H10). **GREEN on GitHub** — the Phase-A push (HEAD `f0af461`) ran as `26751803054` = `completed success` (both jobs, 2m36s). (Non-fatal annotation on every run: "Node.js 20 actions are deprecated" for checkout/setup-python/cache@v4 — cosmetic, bump to newer action majors when convenient.) Check: `gh run list --repo Fleezyflo/fanops --limit 3`.
- **Toolchain-absent behavior:** every `subprocess.run` of a media binary (ffmpeg/ffprobe/whisper/yt-dlp) is guarded — `check=False` does NOT catch a pre-launch `FileNotFoundError`, so absent binaries used to crash. Now: pre-Source/one-shot sites (ffprobe at ingest, yt-dlp at `pull`) raise typed `errors.ToolchainMissingError` → `cli.main` exit 2 + "install …", no traceback; pipeline-quarantined sites (ffmpeg render, whisper transcribe, ffmpeg signals) record a retriable per-unit `error`. Check: `grep -rn "subprocess.run" src/fanops/ | grep -v "_run_ffprobe\|_run_ffmpeg\|try:"` (expect only the lines INSIDE the guards) and `grep -n "ToolchainMissingError" src/fanops/errors.py`.

## 1. What this is

An autonomous fan-account engine for Moh Flow (bilingual EN/AR rapper). It ingests his videos, decides which moments are worth posting (transcript + audio/scene signals → an agent decision with a recorded reason), cuts platform-ready clips with agent-written EN/AR captions, cross-posts to every fan account × platform via Blotato (staggered for opsec, with a subtle non-synchronized artist @mention), then pulls real performance back to make more of what works. See `README.md` (front door), `MohFlow-FanOps/00_control/RUNTIME.md` (operating loop + production seams), `MohFlow-FanOps/00_control/RISK.md` (recorded operator risk-acceptance for the multi-account model), `MohFlow-FanOps/00_control/context.md` (functional — injected as agent guidance).

## 2. Architecture

- **Unit chain:** `Source → Moment → Clip → Post`, over one git-versioned JSON ledger (`00_control/ledger.json`, atomic temp+os.replace write under a file lock).
- **Identity:** content-addressed SHA-based ids everywhere (`ids.py`: `make_id`/`child_id`/`surface_key`) — NEVER builtin `hash()` (the #1 v1 bug; cross-process stability is proven by a subprocess idempotency test).
- **Agent gates:** generative steps (`decide_moments`, `write_captions`) cross a file contract in `04_agent_io/requests/` — code writes a `*.request.json` (stamped with a `request_id`), the agent/responder writes a validated `*.response.json` echoing that id, code resumes. Stale responses can never be applied (request_id correlation).
- **Two responders** (`responder.py`): `ManualResponder` (no-op; a human/cron writes responses) vs `LlmResponder` (`FANOPS_RESPONDER=llm`; **autonomous via `claude -p`** — default model shells the Claude Code CLI through `llm.claude_json` with committed `prompts.py` templates + the gate's pydantic JSON schema, validates against `MomentDecision`/`CaptionSet`, **per-request quarantine** so one bad/failed gate logs + stays pending without halting the rest). Needs `claude` on PATH (authenticated); model is injectable for tests.
- **Three posters** (`post/`): `dryrun` (default), `rest` (`BlotatoRestPoster`, retry/backoff/typed-errors), `mcp` (`BlotatoMcpPoster`, injected `tool_caller`).
- **Pipeline** (`pipeline.py` `advance()`): per-unit error quarantine (one bad source/moment/clip → `error` state, never wedges the pass). **CLI** (`cli.py`): `status/ingest/pull/advance/respond/track/adjust/gc/run/digest`.
- **State machines** (`models.py`): separate `SourceState`/`MomentState`/`ClipState`/`PostState`. `failed`≠`analyzed`; `held` (brand-risk) and `retired` are first-class.

## 3. Gotchas / non-obvious invariants

- **`--base-time` is the SCHEDULE ANCHOR, not the publish cutoff.** crosspost staggers posts to times AFTER base-time; `publish_due(now=None)` publishes whatever is due as of REAL wall-clock now. To publish in the same pass (backfill), pass a `--base-time` in the past. (This was a fixed plan contradiction.)
- **Conservative retirement:** `adjust` amplifies top `--winner-pct` (0.3) but retires only the bottom `--retire-pct` (0.2) AND below `--lift-floor` (20.0) — objectively-fine clips above the floor are never retired. Retiring a clip also retires its moment (so it can't be re-rendered).
- **Three surfaced-but-auto-unrecovered stuck states** (all in the digest + counts, no silent loss): a post stranded in `submitting` by a mid-publish crash; a post in **`needs_reconcile`** (REST hit an ambiguous 5xx/timeout after sending the body — it may be live, so it is NOT re-POSTed; Blotato has no idempotency key); a `published` post Blotato never returned metrics for. The first two need a poll step (`GET /v2/posts/:id` → promote/reset) — backlog item (a). `needs_reconcile` only ever arises under the `rest`/`mcp` backends, never `dryrun`.
- **`dryrun` posts are trackable** (audit C4): the dryrun poster stamps `submission_id = f"dryrun_<post_id>"`, so the `publish → track → analyzed → adjust` loop is fully exercisable offline by feeding `track` a metrics row keyed on that id. (CLI `track` still skips in dryrun — no live metrics source — but the seam is proven by `tests/integration/test_publish_track_loop.py`.)
- **Whisper model:** default `turbo`; falls back to the best cached `~/.cache/whisper/*.pt` when the requested model can't be downloaded (offline/proxied). In THIS env only `tiny` is cached.

## 4. Health checks

Run the §State Check commands. The integration E2E (`tests/integration/test_e2e_real.py`) is the golden path: real `say` TTS → real whisper transcript (asserts real whisper segment structure + ≥4 words + a vocoder-robust "anymore" anchor — CI-2) → real ffmpeg 1080×1920 vertical clip → 2 dryrun posts published. It pins the `tiny` whisper model in-test (no env var needed) and skips cleanly on a host with no cached checkpoint, so a fresh checkout / CI runner sees a clear skip rather than a cryptic `assert 0 == 1`.

## 5. Now (rewritten each session)

**As of 2026-06-01 (rev 9).**

**Most recent shipped (this session — Phase A of the live+autonomous plan: wired the autonomous LLM responder to `claude -p`, fixing the audit's #1 block-live defect; all pushed to `origin/main`, CI green):**
- `78ee5e7` + `9b30470` — **A1: `src/fanops/llm.py` — the absence-guarded `claude -p` helper (TDD).** `claude_json(prompt, schema, *, timeout=180)` shells `claude --bare -p <prompt> --output-format json --json-schema <schema> --allowedTools ""`; prefers envelope `structured_output`, falls back to `json.loads(result)`. `claude` absent → `ToolchainMissingError`; nonzero/timeout/unparseable → `RuntimeError`. Review fix `9b30470`: valid-JSON-but-non-object stdout (`null`/array/number/string) now routes through the clean "could not parse" `RuntimeError` instead of a raw `AttributeError`.
- `a3d11b8` + `2d0e371` — **A2: `src/fanops/prompts.py` — committed EN/AR `moment_prompt`/`caption_prompt` (TDD).** Pure string-builders; `ensure_ascii=False` so AR round-trips as real UTF-8; instruction-before-data ordering. Review fix `2d0e371`: JSON-quote the semi-trusted WHISPER `transcript_excerpt` in `caption_prompt` (injection isolation — a crafted excerpt could otherwise forge a flush-left `HARD RULES:` block); the guarding test is mutation-proven.
- `6a643f1` + `26448e0` — **A3 (KEYSTONE): wired `LlmResponder` to `claude -p` with per-request quarantine (audit B1/H2/N1, TDD).** `get_responder(llm)` now returns a WORKING responder (default model = `_default_claude_model` → `claude_json` + committed prompts + the gate's pydantic JSON schema), NOT the old stub that raised. `answer_pending` quarantines per request: a transient `RuntimeError`/`ToolchainMissingError` logs `error`, a `ValidationError` logs `invalid` (distinct — audit N1), both leave the gate pending with NO half-written response file. Review fix `26448e0`: the GATE is authoritative for `source_id` (forced overwrite, not `setdefault`, so a model-hallucinated `source_id` can't win).
- `f0af461` — **docs sync** (README + RUNTIME.md): the responder is no longer an SDK stub to wire; it works out of the box via `claude -p` with `claude` on PATH.

Tests green (re-derived this session): unit `232 passed, 5 deselected` (219→232, +13); full suite `236 passed, 1 skipped`. **CI run `26751803054` = `completed success`** (both jobs).

**What works right now:**
- **The autonomous LLM responder is LIVE-wired:** `FANOPS_RESPONDER=llm` returns a working responder that answers its own moment/caption gates via `claude -p` (headless Claude Code CLI, reuses Claude Code auth, no app API key). The #1 block-live defect (`get_responder` built a stub that raised on the first gate) is GONE.
- **Per-request quarantine** (audit H2/N1): one bad/failed gate logs and stays pending; it never halts the rest of the tick, and a present-but-schema-invalid answer (`invalid`) is distinct from a transient/CLI failure (`error`). Validation precedes the write → no half-written `*.response.json` ever.
- Full pipeline runs (dryrun): ingest → transcribe → signals → moment gate → clip render → caption gate → crosspost → publish, pausing at each agent gate; per-unit quarantine inside the 3 explicit loops.
- **CI is real and GREEN** (H10 `ci.yml`, both jobs); **toolchain robustness is complete** (every ffmpeg/ffprobe/whisper/yt-dlp shell guarded — clean exit-2 or retriable per-unit error, never a raw traceback).
- **The unattended path is hardened end-to-end:** orphaned lock self-heals (H6); a responder exception degrades cleanly (H7); auth failures halt-by-type (H8); stranded posts auto-reconcile where the API allows (H4); staggering is monotonic + collision-free (H1/H2); @mention window holds across re-tags (H3); learning loop dryrun-exercisable (C4) with pinned scoring (H9).
- **A shared per-clip media URL across accounts is intended** (C2): NOT a defect; do not "fix" it.

**Live state caveats:** **`claude -p` could NOT produce a live answer in THIS environment — it is present (v2.1.147) but UNAUTHENTICATED here** (`claude -p` → `{"is_error":true,"result":"Not logged in · Please run /login"}`; no API key/OAuth token reaches the headless subprocess, and interactive `/login` isn't available to a spawned process). What WAS proven against the REAL CLI: the full `fanops run FANOPS_RESPONDER=llm` pipeline ran end-to-end on real `say`/ffmpeg/whisper inputs, called the real `claude -p`, **quarantined the real auth failure, logged it `error`, wrote no response file, left the gate awaiting, and exited cleanly with no traceback** — i.e. correct graceful-degradation-under-cron. The SUCCESS path is fully proven by the 16 mocked unit tests (llm 6 + prompts 3 + responder 7); the live success answer is **pending CLI auth in a real (authenticated) deployment** — recorded honestly, not faked. — Still nothing run against REAL Blotato (`dryrun` default; `accounts.json` = `@TBD`); INTEGRATION CHECKPOINTs (Blotato presign/metrics/reconcile shapes, MCP tool name) UNVERIFIED against a live call. **No "Block LIVE" defect and no open audit-HIGH remain.** (CI annotation noise: "Node.js 20 actions are deprecated" — cosmetic.)

**Open items, in priority order:**
1. **Phase B — Concurrency correctness (next phase, no creds; `docs/superpowers/plans/2026-06-01-fanops-live-autonomous.md` Phase B).** Single advisory lock spanning the WHOLE `advance()` load-mutate-save (audit B4) + fix M1 (`reconcile_moments` un-retires a retired moment) + M2 (now subsumed by the transaction). **NEW for Phase B (recorded this session):** the **answer-stale TOCTOU** found in A3 review — `answer_pending` reads `rid = latest_request_id()` AFTER the slow `claude -p` call, so an overlapping `run` re-seeding the gate mid-call yields a response built from the OLD payload but stamped with the NEW rid (passes the freshness check, answers the wrong request). The agent-gate files live OUTSIDE the ledger flock; Phase B's lock work is the natural home (capture rid before the call + re-verify after, or bring `agent_io` under a lock). See `fanops-build-deviations.md` "Phase A … Recorded-for-later".
2. **LIVE prerequisite, OPERATOR-GATED (needs creds):** run `tests/integration/test_blotato_smoke.py` against the Blotato sandbox (`BLOTATO_API_KEY`+`BLOTATO_SMOKE_ACCOUNT_ID`).
3. **`claude -p` live verification (operator-gated):** in an environment where `claude` is authenticated, drive one real gate end-to-end and confirm a `moments__*.response.json` with real picks (the plan's A4 scratch recipe). The wiring is done + real-CLI-degradation-verified; only a live authed answer remains.
4. **Phases C/D/E/F** of the live+autonomous plan: C (NaN/caption validation hardening), D (live Blotato seam — `[GATED]` smoke verification), E (learning-loop-in-cron + dead-man's-switch + responder backoff), F (operator-recovery verbs). **Bounded-retry note for E (recorded this session):** `fanops run` retries a persistently-failing gate up to `for _ in range(10)`/invocation with no backoff — bounded per-run, but a backoff / "stop retrying a deterministically-rejected gate" policy is a natural E refinement.
5. **New feature (C2 adjudication):** per-account creative variation for A/B content learning. Own spec → plan → build cycle.
6. **Pre-existing backlog** (`RUNTIME.md §Backlog`): REST backoff jitter; externalize brand-risk lists + lift weights; per-platform max-duration clamp; `fanops unhold`; `_parse`/`BASE_URL` triplication consolidation; per-source ranking; media size cap. (Cosmetic: bump CI action majors off Node-20.)

**Pick up here:**
- **Recommended next: Phase B** of `docs/superpowers/plans/2026-06-01-fanops-live-autonomous.md` (concurrency correctness — single transaction lock + M1/M2). Same subagent-driven loop as Phase A. The plan has literal TDD steps (B1 `Ledger.transaction` at `src/fanops/ledger.py`, B2 `publish_due(in_transaction=)`, B3 the M1 retire guard). **Fold in the A3 answer-stale TOCTOU** (item 1) while the agent-gate/lock boundary is being designed.
- **`claude -p` live answer is operator-gated, not agent-gated:** it needs an authenticated `claude` CLI (run `claude /login` interactively in the deploy env). The wiring is complete; verify with the plan's A4 scratch recipe once authed.
- **LIVE Blotato path is operator-gated:** sandbox smoke test (item 2, needs creds) + the 3 human-only steps (create accounts → connect in Blotato → paste account_ids + set active; `RUNTIME.md` "three human-only steps").
- Standing: `main` pushed to private `origin` (Fleezyflo/fanops), in sync; working tree clean after the handoff commit. **Do not re-raise C2 from a future audit** — adjudicated, not missed. When verifying through the real CLI, **`cd` to the project root first** (`Config()` resolves the ledger from cwd; no `FANOPS_ROOT`) — and when a real-CLI check claims to test an absent/unauthed tool, make sure the tool is *actually* absent/unauthed (the venv bin carries `whisper`/`yt-dlp`; `claude` was present-but-unauthed this session — a leftover PATH entry or an assumed-auth caused false-greens before). Audit workflow at `.claude/workflows/fanops-deep-audit.js` (tighten its agents to read-only before re-running).
