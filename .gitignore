# secrets
.env
.env.*
*.bak
*.key
*-credentials.json
.mcp-credentials/

# live data lives at FANOPS_ROOT; backstop if a stale process recreates a ghost tree in-repo
MohFlow-FanOps/

# python / venv
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
.coverage
.coverage.*
coverage.json

# review scratch
.fanops-*.json
.fanops-*.md
.fanops-*.js

# claude harness — keep workflows, ignore transient locks + ephemeral planning artifacts
.claude/scheduled_tasks.lock
.claude/plan/
.claude/plans/
.claude/prds/
.claude/PRPs/
.claude/*.local.md

# browser-QA / playwright scratch (screenshots, traces) — regenerated, never committed
.playwright-mcp/
.ui_audit/
/*.png

# git worktrees (isolated dev workspaces) — never tracked
.worktrees/

# macOS
.DS_Store

# superseded plans (kept on disk, not tracked)
2026-05-31-fanops-real-build.md
2026-05-31-mohflow-fanops.md

# Google OAuth client secret downloads (never commit)
client_secret_*.json

# runtime/working artifacts kept on disk, never tracked
*.docx
docs/superpowers/
tiktok-submission/

# session/tooling scratch at repo root — machine-local, never committed
.agents/
.reports/
.codemap-cache/
.codemap-cache-test/
skills-lock.json
